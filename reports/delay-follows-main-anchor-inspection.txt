TEST PROJECT 2 — DELAY FOLLOWS MAIN ANCHOR INSPECTION

Generated: 2026-06-09T20:58:52.3588234+01:00

----- BRANCH -----
full-rules-build

----- GIT STATUS -----
?? "reusable chat files/"

----- LATEST COMMITS -----
ac6d714 Add dormant grouped activation support
c2277a9 Add grouped activation patch anchor check
4496d69 Add renderer relationship anchor inspection
9d9ba2d Add generic relationship rule review template
4611c96 Add safe reviewed rule profile apply script
203edc6 Add rule profile fast slow split report
8a05cfb Add rule profile batch candidate generator
f3ed97f Add supported rule fields audit

----- DELAY / SCHEDULING / DECISION TERMS IN full-renderer.js -----

LINE 256: function isBarOpportunityAllowedForKey(key, section, localBarIndex = 0) {
----- CONTEXT -----

    return trackBarNumber;
  }

  function isBarOpportunityAllowedForKey(key, section, localBarIndex = 0) {
    const trackBarNumber = getTrackBarNumber(section, localBarIndex);
    const opportunityIndex = getValidOpportunityIndexForKeyAtBar(key, trackBarNumber);

    if (opportunityIndex === null) return false;

    const interval = getOpportunityIntervalFromKey(key);
    return interval <= 1 || opportunityIndex % interval === 0;
  }

LINE 265: function getAllowedLocalBarIndexesForKey(key, section) {
----- CONTEXT -----

    const interval = getOpportunityIntervalFromKey(key);
    return interval <= 1 || opportunityIndex % interval === 0;
  }
  function getAllowedLocalBarIndexesForKey(key, section) {
    const bars = Math.max(0, Number(section?.bars || 0));
    const indexes = [];

    for (let localBarIndex = 0; localBarIndex < bars; localBarIndex++) {
      if (isBarOpportunityAllowedForKey(key, section, localBarIndex)) {
        indexes.push(localBarIndex);
      }
    }

LINE 270: if (isBarOpportunityAllowedForKey(key, section, localBarIndex)) {
----- CONTEXT -----
    const bars = Math.max(0, Number(section?.bars || 0));
    const indexes = [];

    for (let localBarIndex = 0; localBarIndex < bars; localBarIndex++) {
      if (isBarOpportunityAllowedForKey(key, section, localBarIndex)) {
        indexes.push(localBarIndex);
      }
    }

    return indexes;
  }

  function chooseAllowedLocalBarIndexForKey(random, key, section) {

LINE 278: function chooseAllowedLocalBarIndexForKey(random, key, section) {
----- CONTEXT -----

    return indexes;
  }

  function chooseAllowedLocalBarIndexForKey(random, key, section) {
    const allowedIndexes = getAllowedLocalBarIndexesForKey(key, section);
    if (!allowedIndexes.length) return null;
    return chooseOne(random, allowedIndexes);
  }
  function keyHasFilenameToken(key, token) {
    const escapedToken = String(token || "").replace(/[.*+?^${}()|[\]\\]/g, "\\$&");
    const pattern = new RegExp(`(?:^|[\\/_.\\-\\s])${escapedToken}(?:[\\/_.\\-\\s]|$)`, "i");
    return pattern.test(String(key || ""));

LINE 279: const allowedIndexes = getAllowedLocalBarIndexesForKey(key, section);
----- CONTEXT -----
    return indexes;
  }

  function chooseAllowedLocalBarIndexForKey(random, key, section) {
    const allowedIndexes = getAllowedLocalBarIndexesForKey(key, section);
    if (!allowedIndexes.length) return null;
    return chooseOne(random, allowedIndexes);
  }
  function keyHasFilenameToken(key, token) {
    const escapedToken = String(token || "").replace(/[.*+?^${}()|[\]\\]/g, "\\$&");
    const pattern = new RegExp(`(?:^|[\\/_.\\-\\s])${escapedToken}(?:[\\/_.\\-\\s]|$)`, "i");
    return pattern.test(String(key || ""));
  }

LINE 280: if (!allowedIndexes.length) return null;
----- CONTEXT -----
  }

  function chooseAllowedLocalBarIndexForKey(random, key, section) {
    const allowedIndexes = getAllowedLocalBarIndexesForKey(key, section);
    if (!allowedIndexes.length) return null;
    return chooseOne(random, allowedIndexes);
  }
  function keyHasFilenameToken(key, token) {
    const escapedToken = String(token || "").replace(/[.*+?^${}()|[\]\\]/g, "\\$&");
    const pattern = new RegExp(`(?:^|[\\/_.\\-\\s])${escapedToken}(?:[\\/_.\\-\\s]|$)`, "i");
    return pattern.test(String(key || ""));
  }


LINE 281: return chooseOne(random, allowedIndexes);
----- CONTEXT -----

  function chooseAllowedLocalBarIndexForKey(random, key, section) {
    const allowedIndexes = getAllowedLocalBarIndexesForKey(key, section);
    if (!allowedIndexes.length) return null;
    return chooseOne(random, allowedIndexes);
  }
  function keyHasFilenameToken(key, token) {
    const escapedToken = String(token || "").replace(/[.*+?^${}()|[\]\\]/g, "\\$&");
    const pattern = new RegExp(`(?:^|[\\/_.\\-\\s])${escapedToken}(?:[\\/_.\\-\\s]|$)`, "i");
    return pattern.test(String(key || ""));
  }

  function isHookSection(section) {

LINE 441: function normalizeRuleDecisionToken(value) {
----- CONTEXT -----
    setLifecycleAvailability(lifecycleStates, id, true);
    return getLifecycleState(lifecycleStates, id);
  }

  function normalizeRuleDecisionToken(value) {
    return String(value || "")
      .toLowerCase()
      .trim()
      .replace(/^#/, "")
      .replace(/[\s\-]+/g, "_");
  }

  function addRuleDecisionTags(targetSet, values, prefix = "") {

LINE 449: function addRuleDecisionTags(targetSet, values, prefix = "") {
----- CONTEXT -----
      .replace(/^#/, "")
      .replace(/[\s\-]+/g, "_");
  }

  function addRuleDecisionTags(targetSet, values, prefix = "") {
    if (!targetSet || !values) return targetSet;

    const list = Array.isArray(values) ? values : [values];

    for (const value of list) {
      const token = normalizeRuleDecisionToken(value);
      if (!token) continue;
      targetSet.add(prefix + token);

LINE 455: const token = normalizeRuleDecisionToken(value);
----- CONTEXT -----

    const list = Array.isArray(values) ? values : [values];

    for (const value of list) {
      const token = normalizeRuleDecisionToken(value);
      if (!token) continue;
      targetSet.add(prefix + token);
    }

    return targetSet;
  }

  function getEntryRuleDecisionTags(entry) {

LINE 463: function getEntryRuleDecisionTags(entry) {
----- CONTEXT -----

    return targetSet;
  }

  function getEntryRuleDecisionTags(entry) {
    const tags = new Set();

    if (!entry) return tags;

    addRuleDecisionTags(tags, entry.tags);
    addRuleDecisionTags(tags, entry.ruleTags);
    addRuleDecisionTags(tags, entry.manualTags);
    addRuleDecisionTags(tags, entry.family, "family:");

LINE 468: addRuleDecisionTags(tags, entry.tags);
----- CONTEXT -----
    const tags = new Set();

    if (!entry) return tags;

    addRuleDecisionTags(tags, entry.tags);
    addRuleDecisionTags(tags, entry.ruleTags);
    addRuleDecisionTags(tags, entry.manualTags);
    addRuleDecisionTags(tags, entry.family, "family:");
    addRuleDecisionTags(tags, entry.families, "family:");
    addRuleDecisionTags(tags, entry.folder, "folder:");
    addRuleDecisionTags(tags, entry.type, "type:");

    return tags;

LINE 469: addRuleDecisionTags(tags, entry.ruleTags);
----- CONTEXT -----

    if (!entry) return tags;

    addRuleDecisionTags(tags, entry.tags);
    addRuleDecisionTags(tags, entry.ruleTags);
    addRuleDecisionTags(tags, entry.manualTags);
    addRuleDecisionTags(tags, entry.family, "family:");
    addRuleDecisionTags(tags, entry.families, "family:");
    addRuleDecisionTags(tags, entry.folder, "folder:");
    addRuleDecisionTags(tags, entry.type, "type:");

    return tags;
  }

LINE 470: addRuleDecisionTags(tags, entry.manualTags);
----- CONTEXT -----
    if (!entry) return tags;

    addRuleDecisionTags(tags, entry.tags);
    addRuleDecisionTags(tags, entry.ruleTags);
    addRuleDecisionTags(tags, entry.manualTags);
    addRuleDecisionTags(tags, entry.family, "family:");
    addRuleDecisionTags(tags, entry.families, "family:");
    addRuleDecisionTags(tags, entry.folder, "folder:");
    addRuleDecisionTags(tags, entry.type, "type:");

    return tags;
  }


LINE 471: addRuleDecisionTags(tags, entry.family, "family:");
----- CONTEXT -----

    addRuleDecisionTags(tags, entry.tags);
    addRuleDecisionTags(tags, entry.ruleTags);
    addRuleDecisionTags(tags, entry.manualTags);
    addRuleDecisionTags(tags, entry.family, "family:");
    addRuleDecisionTags(tags, entry.families, "family:");
    addRuleDecisionTags(tags, entry.folder, "folder:");
    addRuleDecisionTags(tags, entry.type, "type:");

    return tags;
  }

  function getSectionRuleDecisionTags(section) {

LINE 472: addRuleDecisionTags(tags, entry.families, "family:");
----- CONTEXT -----
    addRuleDecisionTags(tags, entry.tags);
    addRuleDecisionTags(tags, entry.ruleTags);
    addRuleDecisionTags(tags, entry.manualTags);
    addRuleDecisionTags(tags, entry.family, "family:");
    addRuleDecisionTags(tags, entry.families, "family:");
    addRuleDecisionTags(tags, entry.folder, "folder:");
    addRuleDecisionTags(tags, entry.type, "type:");

    return tags;
  }

  function getSectionRuleDecisionTags(section) {
    const tags = new Set();

LINE 473: addRuleDecisionTags(tags, entry.folder, "folder:");
----- CONTEXT -----
    addRuleDecisionTags(tags, entry.ruleTags);
    addRuleDecisionTags(tags, entry.manualTags);
    addRuleDecisionTags(tags, entry.family, "family:");
    addRuleDecisionTags(tags, entry.families, "family:");
    addRuleDecisionTags(tags, entry.folder, "folder:");
    addRuleDecisionTags(tags, entry.type, "type:");

    return tags;
  }

  function getSectionRuleDecisionTags(section) {
    const tags = new Set();


LINE 474: addRuleDecisionTags(tags, entry.type, "type:");
----- CONTEXT -----
    addRuleDecisionTags(tags, entry.manualTags);
    addRuleDecisionTags(tags, entry.family, "family:");
    addRuleDecisionTags(tags, entry.families, "family:");
    addRuleDecisionTags(tags, entry.folder, "folder:");
    addRuleDecisionTags(tags, entry.type, "type:");

    return tags;
  }

  function getSectionRuleDecisionTags(section) {
    const tags = new Set();

    if (!section) return tags;

LINE 479: function getSectionRuleDecisionTags(section) {
----- CONTEXT -----

    return tags;
  }

  function getSectionRuleDecisionTags(section) {
    const tags = new Set();

    if (!section) return tags;

    addRuleDecisionTags(tags, section.tags);
    addRuleDecisionTags(tags, section.type, "section_type:");
    addRuleDecisionTags(tags, section.lyrixSectionId, "lyrix_section:");


LINE 484: addRuleDecisionTags(tags, section.tags);
----- CONTEXT -----
    const tags = new Set();

    if (!section) return tags;

    addRuleDecisionTags(tags, section.tags);
    addRuleDecisionTags(tags, section.type, "section_type:");
    addRuleDecisionTags(tags, section.lyrixSectionId, "lyrix_section:");

    return tags;
  }

  function getRuleDecisionItemKey({ key = "", entry = null, pattern = null } = {}) {
    return String(key || entry?.key || pattern?.file || "");

LINE 485: addRuleDecisionTags(tags, section.type, "section_type:");
----- CONTEXT -----

    if (!section) return tags;

    addRuleDecisionTags(tags, section.tags);
    addRuleDecisionTags(tags, section.type, "section_type:");
    addRuleDecisionTags(tags, section.lyrixSectionId, "lyrix_section:");

    return tags;
  }

  function getRuleDecisionItemKey({ key = "", entry = null, pattern = null } = {}) {
    return String(key || entry?.key || pattern?.file || "");
  }

LINE 486: addRuleDecisionTags(tags, section.lyrixSectionId, "lyrix_section:");
----- CONTEXT -----
    if (!section) return tags;

    addRuleDecisionTags(tags, section.tags);
    addRuleDecisionTags(tags, section.type, "section_type:");
    addRuleDecisionTags(tags, section.lyrixSectionId, "lyrix_section:");

    return tags;
  }

  function getRuleDecisionItemKey({ key = "", entry = null, pattern = null } = {}) {
    return String(key || entry?.key || pattern?.file || "");
  }


LINE 491: function getRuleDecisionItemKey({ key = "", entry = null, pattern = null } = {}) {
----- CONTEXT -----

    return tags;
  }

  function getRuleDecisionItemKey({ key = "", entry = null, pattern = null } = {}) {
    return String(key || entry?.key || pattern?.file || "");
  }

  function createRuleDecisionContext({
    kind = "audio",
    key = "",
    entry = null,
    pattern = null,

LINE 495: function createRuleDecisionContext({
----- CONTEXT -----
  function getRuleDecisionItemKey({ key = "", entry = null, pattern = null } = {}) {
    return String(key || entry?.key || pattern?.file || "");
  }

  function createRuleDecisionContext({
    kind = "audio",
    key = "",
    entry = null,
    pattern = null,
    section = null,
    lifecycleStates = null,
    localBarIndex = null,
    startSeconds = null,

LINE 503: startSeconds = null,
----- CONTEXT -----
    pattern = null,
    section = null,
    lifecycleStates = null,
    localBarIndex = null,
    startSeconds = null,
    baseChance = 1
  } = {}) {
    const itemKey = getRuleDecisionItemKey({ key, entry, pattern });
    const lifecycleId = itemKey
      ? (kind === "midi" ? getMidiLifecycleId(itemKey) : getAudioLifecycleId(itemKey))
      : null;

    const lifecycleState = lifecycleStates && lifecycleId

LINE 506: const itemKey = getRuleDecisionItemKey({ key, entry, pattern });
----- CONTEXT -----
    localBarIndex = null,
    startSeconds = null,
    baseChance = 1
  } = {}) {
    const itemKey = getRuleDecisionItemKey({ key, entry, pattern });
    const lifecycleId = itemKey
      ? (kind === "midi" ? getMidiLifecycleId(itemKey) : getAudioLifecycleId(itemKey))
      : null;

    const lifecycleState = lifecycleStates && lifecycleId
      ? getLifecycleState(lifecycleStates, lifecycleId)
      : null;


LINE 527: startSeconds,
----- CONTEXT -----
      localBarIndex,
      trackBarNumber: section && localBarIndex !== null
        ? getTrackBarNumber(section, localBarIndex)
        : null,
      startSeconds,
      baseChance: clampProbability(baseChance, 1),
      lifecycleId,
      lifecycleState,
      itemTags: getEntryRuleDecisionTags(entry),
      sectionTags: getSectionRuleDecisionTags(section)
    };
  }


LINE 531: itemTags: getEntryRuleDecisionTags(entry),
----- CONTEXT -----
      startSeconds,
      baseChance: clampProbability(baseChance, 1),
      lifecycleId,
      lifecycleState,
      itemTags: getEntryRuleDecisionTags(entry),
      sectionTags: getSectionRuleDecisionTags(section)
    };
  }

  function createRuleDecisionResult(context, options = {}) {
    const baseChance = clampProbability(options.baseChance ?? context?.baseChance ?? 1, 1);

    return {

LINE 532: sectionTags: getSectionRuleDecisionTags(section)
----- CONTEXT -----
      baseChance: clampProbability(baseChance, 1),
      lifecycleId,
      lifecycleState,
      itemTags: getEntryRuleDecisionTags(entry),
      sectionTags: getSectionRuleDecisionTags(section)
    };
  }

  function createRuleDecisionResult(context, options = {}) {
    const baseChance = clampProbability(options.baseChance ?? context?.baseChance ?? 1, 1);

    return {
      kind: String(context?.kind || ""),

LINE 536: function createRuleDecisionResult(context, options = {}) {
----- CONTEXT -----
      sectionTags: getSectionRuleDecisionTags(section)
    };
  }

  function createRuleDecisionResult(context, options = {}) {
    const baseChance = clampProbability(options.baseChance ?? context?.baseChance ?? 1, 1);

    return {
      kind: String(context?.kind || ""),
      itemKey: String(context?.itemKey || ""),
      lifecycleId: String(context?.lifecycleId || ""),
      sectionId: String(context?.sectionId || ""),
      sectionType: String(context?.sectionType || ""),

LINE 547: startSeconds: context?.startSeconds ?? null,
----- CONTEXT -----
      sectionId: String(context?.sectionId || ""),
      sectionType: String(context?.sectionType || ""),
      localBarIndex: context?.localBarIndex ?? null,
      trackBarNumber: context?.trackBarNumber ?? null,
      startSeconds: context?.startSeconds ?? null,
      allowed: true,
      blocked: false,
      droppedOut: false,
      baseChance,
      chanceMultiplier: 1,
      finalChance: baseChance,
      roll: null,
      reasons: []

LINE 548: allowed: true,
----- CONTEXT -----
      sectionType: String(context?.sectionType || ""),
      localBarIndex: context?.localBarIndex ?? null,
      trackBarNumber: context?.trackBarNumber ?? null,
      startSeconds: context?.startSeconds ?? null,
      allowed: true,
      blocked: false,
      droppedOut: false,
      baseChance,
      chanceMultiplier: 1,
      finalChance: baseChance,
      roll: null,
      reasons: []
    };

LINE 553: finalChance: baseChance,
----- CONTEXT -----
      blocked: false,
      droppedOut: false,
      baseChance,
      chanceMultiplier: 1,
      finalChance: baseChance,
      roll: null,
      reasons: []
    };
  }

  function addRuleDecisionReason(decision, code, details = {}) {
    if (!decision) return decision;
    if (!Array.isArray(decision.reasons)) decision.reasons = [];

LINE 559: function addRuleDecisionReason(decision, code, details = {}) {
----- CONTEXT -----
      reasons: []
    };
  }

  function addRuleDecisionReason(decision, code, details = {}) {
    if (!decision) return decision;
    if (!Array.isArray(decision.reasons)) decision.reasons = [];

    decision.reasons.push({
      code,
      ...details
    });


LINE 560: if (!decision) return decision;
----- CONTEXT -----
    };
  }

  function addRuleDecisionReason(decision, code, details = {}) {
    if (!decision) return decision;
    if (!Array.isArray(decision.reasons)) decision.reasons = [];

    decision.reasons.push({
      code,
      ...details
    });

    return decision;

LINE 561: if (!Array.isArray(decision.reasons)) decision.reasons = [];
----- CONTEXT -----
  }

  function addRuleDecisionReason(decision, code, details = {}) {
    if (!decision) return decision;
    if (!Array.isArray(decision.reasons)) decision.reasons = [];

    decision.reasons.push({
      code,
      ...details
    });

    return decision;
  }

LINE 563: decision.reasons.push({
----- CONTEXT -----
  function addRuleDecisionReason(decision, code, details = {}) {
    if (!decision) return decision;
    if (!Array.isArray(decision.reasons)) decision.reasons = [];

    decision.reasons.push({
      code,
      ...details
    });

    return decision;
  }

  function blockRuleDecision(decision, code, details = {}) {

LINE 568: return decision;
----- CONTEXT -----
      code,
      ...details
    });

    return decision;
  }

  function blockRuleDecision(decision, code, details = {}) {
    if (!decision) return decision;

    decision.allowed = false;
    decision.blocked = true;
    decision.finalChance = 0;

LINE 571: function blockRuleDecision(decision, code, details = {}) {
----- CONTEXT -----

    return decision;
  }

  function blockRuleDecision(decision, code, details = {}) {
    if (!decision) return decision;

    decision.allowed = false;
    decision.blocked = true;
    decision.finalChance = 0;

    return addRuleDecisionReason(decision, code, details);
  }

LINE 572: if (!decision) return decision;
----- CONTEXT -----
    return decision;
  }

  function blockRuleDecision(decision, code, details = {}) {
    if (!decision) return decision;

    decision.allowed = false;
    decision.blocked = true;
    decision.finalChance = 0;

    return addRuleDecisionReason(decision, code, details);
  }


LINE 574: decision.allowed = false;
----- CONTEXT -----

  function blockRuleDecision(decision, code, details = {}) {
    if (!decision) return decision;

    decision.allowed = false;
    decision.blocked = true;
    decision.finalChance = 0;

    return addRuleDecisionReason(decision, code, details);
  }

  function multiplyRuleDecisionChance(decision, multiplier, code, details = {}) {
    if (!decision) return decision;

LINE 575: decision.blocked = true;
----- CONTEXT -----
  function blockRuleDecision(decision, code, details = {}) {
    if (!decision) return decision;

    decision.allowed = false;
    decision.blocked = true;
    decision.finalChance = 0;

    return addRuleDecisionReason(decision, code, details);
  }

  function multiplyRuleDecisionChance(decision, multiplier, code, details = {}) {
    if (!decision) return decision;


LINE 576: decision.finalChance = 0;
----- CONTEXT -----
    if (!decision) return decision;

    decision.allowed = false;
    decision.blocked = true;
    decision.finalChance = 0;

    return addRuleDecisionReason(decision, code, details);
  }

  function multiplyRuleDecisionChance(decision, multiplier, code, details = {}) {
    if (!decision) return decision;

    const safeMultiplier = Number.isFinite(Number(multiplier))

LINE 578: return addRuleDecisionReason(decision, code, details);
----- CONTEXT -----
    decision.allowed = false;
    decision.blocked = true;
    decision.finalChance = 0;

    return addRuleDecisionReason(decision, code, details);
  }

  function multiplyRuleDecisionChance(decision, multiplier, code, details = {}) {
    if (!decision) return decision;

    const safeMultiplier = Number.isFinite(Number(multiplier))
      ? Math.max(0, Number(multiplier))
      : 1;

LINE 581: function multiplyRuleDecisionChance(decision, multiplier, code, details = {}) {
----- CONTEXT -----

    return addRuleDecisionReason(decision, code, details);
  }

  function multiplyRuleDecisionChance(decision, multiplier, code, details = {}) {
    if (!decision) return decision;

    const safeMultiplier = Number.isFinite(Number(multiplier))
      ? Math.max(0, Number(multiplier))
      : 1;

    decision.chanceMultiplier *= safeMultiplier;
    decision.finalChance = clampProbability(decision.baseChance * decision.chanceMultiplier);

LINE 582: if (!decision) return decision;
----- CONTEXT -----
    return addRuleDecisionReason(decision, code, details);
  }

  function multiplyRuleDecisionChance(decision, multiplier, code, details = {}) {
    if (!decision) return decision;

    const safeMultiplier = Number.isFinite(Number(multiplier))
      ? Math.max(0, Number(multiplier))
      : 1;

    decision.chanceMultiplier *= safeMultiplier;
    decision.finalChance = clampProbability(decision.baseChance * decision.chanceMultiplier);


LINE 588: decision.chanceMultiplier *= safeMultiplier;
----- CONTEXT -----
    const safeMultiplier = Number.isFinite(Number(multiplier))
      ? Math.max(0, Number(multiplier))
      : 1;

    decision.chanceMultiplier *= safeMultiplier;
    decision.finalChance = clampProbability(decision.baseChance * decision.chanceMultiplier);

    return addRuleDecisionReason(decision, code, {
      multiplier: safeMultiplier,
      ...details
    });
  }


LINE 589: decision.finalChance = clampProbability(decision.baseChance * decision.chanceMultiplier);
----- CONTEXT -----
      ? Math.max(0, Number(multiplier))
      : 1;

    decision.chanceMultiplier *= safeMultiplier;
    decision.finalChance = clampProbability(decision.baseChance * decision.chanceMultiplier);

    return addRuleDecisionReason(decision, code, {
      multiplier: safeMultiplier,
      ...details
    });
  }

  function markRuleDecisionDropout(decision, code, details = {}) {

LINE 591: return addRuleDecisionReason(decision, code, {
----- CONTEXT -----

    decision.chanceMultiplier *= safeMultiplier;
    decision.finalChance = clampProbability(decision.baseChance * decision.chanceMultiplier);

    return addRuleDecisionReason(decision, code, {
      multiplier: safeMultiplier,
      ...details
    });
  }

  function markRuleDecisionDropout(decision, code, details = {}) {
    if (!decision) return decision;


LINE 597: function markRuleDecisionDropout(decision, code, details = {}) {
----- CONTEXT -----
      ...details
    });
  }

  function markRuleDecisionDropout(decision, code, details = {}) {
    if (!decision) return decision;

    decision.allowed = false;
    decision.droppedOut = true;

    return addRuleDecisionReason(decision, code, details);
  }


LINE 598: if (!decision) return decision;
----- CONTEXT -----
    });
  }

  function markRuleDecisionDropout(decision, code, details = {}) {
    if (!decision) return decision;

    decision.allowed = false;
    decision.droppedOut = true;

    return addRuleDecisionReason(decision, code, details);
  }

  function finalizeRuleDecision(random, decision) {

LINE 600: decision.allowed = false;
----- CONTEXT -----

  function markRuleDecisionDropout(decision, code, details = {}) {
    if (!decision) return decision;

    decision.allowed = false;
    decision.droppedOut = true;

    return addRuleDecisionReason(decision, code, details);
  }

  function finalizeRuleDecision(random, decision) {
    if (!decision) return false;


LINE 601: decision.droppedOut = true;
----- CONTEXT -----
  function markRuleDecisionDropout(decision, code, details = {}) {
    if (!decision) return decision;

    decision.allowed = false;
    decision.droppedOut = true;

    return addRuleDecisionReason(decision, code, details);
  }

  function finalizeRuleDecision(random, decision) {
    if (!decision) return false;

    if (decision.blocked || decision.droppedOut || decision.finalChance <= 0) {

LINE 603: return addRuleDecisionReason(decision, code, details);
----- CONTEXT -----

    decision.allowed = false;
    decision.droppedOut = true;

    return addRuleDecisionReason(decision, code, details);
  }

  function finalizeRuleDecision(random, decision) {
    if (!decision) return false;

    if (decision.blocked || decision.droppedOut || decision.finalChance <= 0) {
      decision.allowed = false;
      return false;

LINE 606: function finalizeRuleDecision(random, decision) {
----- CONTEXT -----

    return addRuleDecisionReason(decision, code, details);
  }

  function finalizeRuleDecision(random, decision) {
    if (!decision) return false;

    if (decision.blocked || decision.droppedOut || decision.finalChance <= 0) {
      decision.allowed = false;
      return false;
    }

    const roll = random();

LINE 607: if (!decision) return false;
----- CONTEXT -----
    return addRuleDecisionReason(decision, code, details);
  }

  function finalizeRuleDecision(random, decision) {
    if (!decision) return false;

    if (decision.blocked || decision.droppedOut || decision.finalChance <= 0) {
      decision.allowed = false;
      return false;
    }

    const roll = random();
    decision.roll = roll;

LINE 609: if (decision.blocked || decision.droppedOut || decision.finalChance <= 0) {
----- CONTEXT -----

  function finalizeRuleDecision(random, decision) {
    if (!decision) return false;

    if (decision.blocked || decision.droppedOut || decision.finalChance <= 0) {
      decision.allowed = false;
      return false;
    }

    const roll = random();
    decision.roll = roll;

    if (roll <= decision.finalChance) {

LINE 610: decision.allowed = false;
----- CONTEXT -----
  function finalizeRuleDecision(random, decision) {
    if (!decision) return false;

    if (decision.blocked || decision.droppedOut || decision.finalChance <= 0) {
      decision.allowed = false;
      return false;
    }

    const roll = random();
    decision.roll = roll;

    if (roll <= decision.finalChance) {
      return true;

LINE 615: decision.roll = roll;
----- CONTEXT -----
      return false;
    }

    const roll = random();
    decision.roll = roll;

    if (roll <= decision.finalChance) {
      return true;
    }

    decision.allowed = false;
    addRuleDecisionReason(decision, "chance_failed", {
      roll,

LINE 617: if (roll <= decision.finalChance) {
----- CONTEXT -----

    const roll = random();
    decision.roll = roll;

    if (roll <= decision.finalChance) {
      return true;
    }

    decision.allowed = false;
    addRuleDecisionReason(decision, "chance_failed", {
      roll,
      finalChance: decision.finalChance
    });

LINE 621: decision.allowed = false;
----- CONTEXT -----
    if (roll <= decision.finalChance) {
      return true;
    }

    decision.allowed = false;
    addRuleDecisionReason(decision, "chance_failed", {
      roll,
      finalChance: decision.finalChance
    });

    return false;
  }


LINE 622: addRuleDecisionReason(decision, "chance_failed", {
----- CONTEXT -----
      return true;
    }

    decision.allowed = false;
    addRuleDecisionReason(decision, "chance_failed", {
      roll,
      finalChance: decision.finalChance
    });

    return false;
  }

  function recordRuleDecisionDebug(plan, decision) {

LINE 624: finalChance: decision.finalChance
----- CONTEXT -----

    decision.allowed = false;
    addRuleDecisionReason(decision, "chance_failed", {
      roll,
      finalChance: decision.finalChance
    });

    return false;
  }

  function recordRuleDecisionDebug(plan, decision) {
    if (!plan || !decision) return;


LINE 630: function recordRuleDecisionDebug(plan, decision) {
----- CONTEXT -----

    return false;
  }

  function recordRuleDecisionDebug(plan, decision) {
    if (!plan || !decision) return;

    if (!Array.isArray(plan.ruleDecisionDebug)) {
      plan.ruleDecisionDebug = [];
    }

    plan.ruleDecisionDebug.push({
      ...decision,

LINE 631: if (!plan || !decision) return;
----- CONTEXT -----
    return false;
  }

  function recordRuleDecisionDebug(plan, decision) {
    if (!plan || !decision) return;

    if (!Array.isArray(plan.ruleDecisionDebug)) {
      plan.ruleDecisionDebug = [];
    }

    plan.ruleDecisionDebug.push({
      ...decision,
      reasons: Array.isArray(decision.reasons)

LINE 633: if (!Array.isArray(plan.ruleDecisionDebug)) {
----- CONTEXT -----

  function recordRuleDecisionDebug(plan, decision) {
    if (!plan || !decision) return;

    if (!Array.isArray(plan.ruleDecisionDebug)) {
      plan.ruleDecisionDebug = [];
    }

    plan.ruleDecisionDebug.push({
      ...decision,
      reasons: Array.isArray(decision.reasons)
        ? decision.reasons.map(reason => ({ ...reason }))
        : []

LINE 634: plan.ruleDecisionDebug = [];
----- CONTEXT -----
  function recordRuleDecisionDebug(plan, decision) {
    if (!plan || !decision) return;

    if (!Array.isArray(plan.ruleDecisionDebug)) {
      plan.ruleDecisionDebug = [];
    }

    plan.ruleDecisionDebug.push({
      ...decision,
      reasons: Array.isArray(decision.reasons)
        ? decision.reasons.map(reason => ({ ...reason }))
        : []
    });

LINE 637: plan.ruleDecisionDebug.push({
----- CONTEXT -----
    if (!Array.isArray(plan.ruleDecisionDebug)) {
      plan.ruleDecisionDebug = [];
    }

    plan.ruleDecisionDebug.push({
      ...decision,
      reasons: Array.isArray(decision.reasons)
        ? decision.reasons.map(reason => ({ ...reason }))
        : []
    });
  }

  function createPlaybackRuleState() {

LINE 638: ...decision,
----- CONTEXT -----
      plan.ruleDecisionDebug = [];
    }

    plan.ruleDecisionDebug.push({
      ...decision,
      reasons: Array.isArray(decision.reasons)
        ? decision.reasons.map(reason => ({ ...reason }))
        : []
    });
  }

  function createPlaybackRuleState() {
    return {

LINE 639: reasons: Array.isArray(decision.reasons)
----- CONTEXT -----
    }

    plan.ruleDecisionDebug.push({
      ...decision,
      reasons: Array.isArray(decision.reasons)
        ? decision.reasons.map(reason => ({ ...reason }))
        : []
    });
  }

  function createPlaybackRuleState() {
    return {
      activeItems: new Map(),

LINE 640: ? decision.reasons.map(reason => ({ ...reason }))
----- CONTEXT -----

    plan.ruleDecisionDebug.push({
      ...decision,
      reasons: Array.isArray(decision.reasons)
        ? decision.reasons.map(reason => ({ ...reason }))
        : []
    });
  }

  function createPlaybackRuleState() {
    return {
      activeItems: new Map(),
      blockedWindows: [],

LINE 651: groupedActivationDecisions: new Map(),
----- CONTEXT -----
      activeItems: new Map(),
      blockedWindows: [],
      cutoffEvents: [],
      familyLocks: new Map(),
      groupedActivationDecisions: new Map(),
      nextItemId: 1
    };
  }

  function getPlaybackItemId(kind, key) {
    return `${normalizeRuleDecisionToken(kind)}:${String(key || "")}`;
  }


LINE 657: return `${normalizeRuleDecisionToken(kind)}:${String(key || "")}`;
----- CONTEXT -----
    };
  }

  function getPlaybackItemId(kind, key) {
    return `${normalizeRuleDecisionToken(kind)}:${String(key || "")}`;
  }

  function registerActivePlaybackItem(playbackState, {
    kind = "audio",
    key = "",
    family = "",
    startSeconds = 0,
    endSeconds = null,

LINE 660: function registerActivePlaybackItem(playbackState, {
----- CONTEXT -----
  function getPlaybackItemId(kind, key) {
    return `${normalizeRuleDecisionToken(kind)}:${String(key || "")}`;
  }

  function registerActivePlaybackItem(playbackState, {
    kind = "audio",
    key = "",
    family = "",
    startSeconds = 0,
    endSeconds = null,
    source = null,
    gainNode = null,
    sectionId = "",

LINE 664: startSeconds = 0,
----- CONTEXT -----
  function registerActivePlaybackItem(playbackState, {
    kind = "audio",
    key = "",
    family = "",
    startSeconds = 0,
    endSeconds = null,
    source = null,
    gainNode = null,
    sectionId = "",
    tags = []
  } = {}) {
    if (!playbackState) return null;


LINE 681: family: normalizeRuleDecisionToken(family),
----- CONTEXT -----
    const item = {
      id,
      kind,
      key,
      family: normalizeRuleDecisionToken(family),
      startSeconds: Number(startSeconds) || 0,
      endSeconds: Number.isFinite(Number(endSeconds)) ? Number(endSeconds) : null,
      source,
      gainNode,
      sectionId: String(sectionId || ""),
      tags: Array.isArray(tags)
        ? tags.map(tag => normalizeRuleDecisionToken(tag)).filter(Boolean)
        : []

LINE 682: startSeconds: Number(startSeconds) || 0,
----- CONTEXT -----
      id,
      kind,
      key,
      family: normalizeRuleDecisionToken(family),
      startSeconds: Number(startSeconds) || 0,
      endSeconds: Number.isFinite(Number(endSeconds)) ? Number(endSeconds) : null,
      source,
      gainNode,
      sectionId: String(sectionId || ""),
      tags: Array.isArray(tags)
        ? tags.map(tag => normalizeRuleDecisionToken(tag)).filter(Boolean)
        : []
    };

LINE 688: ? tags.map(tag => normalizeRuleDecisionToken(tag)).filter(Boolean)
----- CONTEXT -----
      source,
      gainNode,
      sectionId: String(sectionId || ""),
      tags: Array.isArray(tags)
        ? tags.map(tag => normalizeRuleDecisionToken(tag)).filter(Boolean)
        : []
    };

    playbackState.activeItems.set(id, item);

    if (item.family) {
      playbackState.familyLocks.set(item.family, id);
    }

LINE 701: function unregisterActivePlaybackItem(playbackState, idOrItem) {
----- CONTEXT -----

    return item;
  }

  function unregisterActivePlaybackItem(playbackState, idOrItem) {
    if (!playbackState || !idOrItem) return;

    const id = typeof idOrItem === "string" ? idOrItem : idOrItem.id;
    const item = playbackState.activeItems.get(id);

    if (item?.family && playbackState.familyLocks.get(item.family) === id) {
      playbackState.familyLocks.delete(item.family);
    }

LINE 722: unregisterActivePlaybackItem(playbackState, item);
----- CONTEXT -----
    let expiredCount = 0;

    for (const item of [...playbackState.activeItems.values()]) {
      if (item.endSeconds !== null && item.endSeconds <= time) {
        unregisterActivePlaybackItem(playbackState, item);
        expiredCount += 1;
      }
    }

    return expiredCount;
  }

  function getEntryPrimaryFamily(entry) {

LINE 733: if (entry.family) return normalizeRuleDecisionToken(entry.family);
----- CONTEXT -----

  function getEntryPrimaryFamily(entry) {
    if (!entry) return "";

    if (entry.family) return normalizeRuleDecisionToken(entry.family);

    if (Array.isArray(entry.families) && entry.families.length) {
      return normalizeRuleDecisionToken(entry.families[0]);
    }

    return "";
  }


LINE 736: return normalizeRuleDecisionToken(entry.families[0]);
----- CONTEXT -----

    if (entry.family) return normalizeRuleDecisionToken(entry.family);

    if (Array.isArray(entry.families) && entry.families.length) {
      return normalizeRuleDecisionToken(entry.families[0]);
    }

    return "";
  }

  function getEntryPlaybackTags(entry) {
    const tags = [...getEntryRuleDecisionTags(entry)]
      .map(tag => normalizeRuleDecisionToken(tag))

LINE 743: const tags = [...getEntryRuleDecisionTags(entry)]
----- CONTEXT -----
    return "";
  }

  function getEntryPlaybackTags(entry) {
    const tags = [...getEntryRuleDecisionTags(entry)]
      .map(tag => normalizeRuleDecisionToken(tag))
      .filter(Boolean);

    return [...new Set(tags)];
  }

  function registerScheduledPlaybackHandle(playbackState, {
    kind = "audio",

LINE 744: .map(tag => normalizeRuleDecisionToken(tag))
----- CONTEXT -----
  }

  function getEntryPlaybackTags(entry) {
    const tags = [...getEntryRuleDecisionTags(entry)]
      .map(tag => normalizeRuleDecisionToken(tag))
      .filter(Boolean);

    return [...new Set(tags)];
  }

  function registerScheduledPlaybackHandle(playbackState, {
    kind = "audio",
    key = "",

LINE 760: return registerActivePlaybackItem(playbackState, {
----- CONTEXT -----
    family = ""
  } = {}) {
    if (!playbackState || !scheduleHandle?.scheduled) return null;

    return registerActivePlaybackItem(playbackState, {
      kind,
      key,
      family: family || getEntryPrimaryFamily(entry),
      startSeconds: scheduleHandle.startTime,
      endSeconds: scheduleHandle.endTime,
      source: scheduleHandle.source,
      gainNode: scheduleHandle.gainNode,
      sectionId: section?.id || "",

LINE 764: startSeconds: scheduleHandle.startTime,
----- CONTEXT -----
    return registerActivePlaybackItem(playbackState, {
      kind,
      key,
      family: family || getEntryPrimaryFamily(entry),
      startSeconds: scheduleHandle.startTime,
      endSeconds: scheduleHandle.endTime,
      source: scheduleHandle.source,
      gainNode: scheduleHandle.gainNode,
      sectionId: section?.id || "",
      tags: getEntryPlaybackTags(entry)
    });
  }
  function getActivePlaybackItems(playbackState, filter = {}) {

LINE 775: const kind = filter.kind ? normalizeRuleDecisionToken(filter.kind) : "";
----- CONTEXT -----
  }
  function getActivePlaybackItems(playbackState, filter = {}) {
    if (!playbackState) return [];

    const kind = filter.kind ? normalizeRuleDecisionToken(filter.kind) : "";
    const family = filter.family ? normalizeRuleDecisionToken(filter.family) : "";
    const tag = filter.tag ? normalizeRuleDecisionToken(filter.tag) : "";
    const key = filter.key ? String(filter.key) : "";

    return [...playbackState.activeItems.values()].filter(item => {
      if (kind && normalizeRuleDecisionToken(item.kind) !== kind) return false;
      if (family && item.family !== family) return false;
      if (tag && !item.tags.includes(tag)) return false;

LINE 776: const family = filter.family ? normalizeRuleDecisionToken(filter.family) : "";
----- CONTEXT -----
  function getActivePlaybackItems(playbackState, filter = {}) {
    if (!playbackState) return [];

    const kind = filter.kind ? normalizeRuleDecisionToken(filter.kind) : "";
    const family = filter.family ? normalizeRuleDecisionToken(filter.family) : "";
    const tag = filter.tag ? normalizeRuleDecisionToken(filter.tag) : "";
    const key = filter.key ? String(filter.key) : "";

    return [...playbackState.activeItems.values()].filter(item => {
      if (kind && normalizeRuleDecisionToken(item.kind) !== kind) return false;
      if (family && item.family !== family) return false;
      if (tag && !item.tags.includes(tag)) return false;
      if (key && item.key !== key) return false;

LINE 777: const tag = filter.tag ? normalizeRuleDecisionToken(filter.tag) : "";
----- CONTEXT -----
    if (!playbackState) return [];

    const kind = filter.kind ? normalizeRuleDecisionToken(filter.kind) : "";
    const family = filter.family ? normalizeRuleDecisionToken(filter.family) : "";
    const tag = filter.tag ? normalizeRuleDecisionToken(filter.tag) : "";
    const key = filter.key ? String(filter.key) : "";

    return [...playbackState.activeItems.values()].filter(item => {
      if (kind && normalizeRuleDecisionToken(item.kind) !== kind) return false;
      if (family && item.family !== family) return false;
      if (tag && !item.tags.includes(tag)) return false;
      if (key && item.key !== key) return false;
      return true;

LINE 781: if (kind && normalizeRuleDecisionToken(item.kind) !== kind) return false;
----- CONTEXT -----
    const tag = filter.tag ? normalizeRuleDecisionToken(filter.tag) : "";
    const key = filter.key ? String(filter.key) : "";

    return [...playbackState.activeItems.values()].filter(item => {
      if (kind && normalizeRuleDecisionToken(item.kind) !== kind) return false;
      if (family && item.family !== family) return false;
      if (tag && !item.tags.includes(tag)) return false;
      if (key && item.key !== key) return false;
      return true;
    });
  }

  function doesPlaybackItemOverlapTime(item, timeSeconds) {

LINE 793: const start = Math.max(0, Number(item.startSeconds) || 0);
----- CONTEXT -----
  function doesPlaybackItemOverlapTime(item, timeSeconds) {
    if (!item) return false;

    const time = Math.max(0, Number(timeSeconds) || 0);
    const start = Math.max(0, Number(item.startSeconds) || 0);
    const end = item.endSeconds === null
      ? Infinity
      : Math.max(start, Number(item.endSeconds) || start);

    return start <= time && time < end;
  }

  function getActivePlaybackItemsAtTime(playbackState, timeSeconds, filter = {}) {

LINE 810: .filter(item => Math.max(0, Number(item.startSeconds) || 0) >= time);
----- CONTEXT -----
  function getFuturePlaybackItemsAfterTime(playbackState, timeSeconds, filter = {}) {
    const time = Math.max(0, Number(timeSeconds) || 0);

    return getActivePlaybackItems(playbackState, filter)
      .filter(item => Math.max(0, Number(item.startSeconds) || 0) >= time);
  }
  function cutOffPlaybackItem(playbackState, item, cutTimeSeconds, options = {}) {
    if (!playbackState || !item) return false;

    const cutTime = Math.max(0, Number(cutTimeSeconds) || 0);
    const fadeSeconds = Math.max(0, Number(options.fadeSeconds) || 0);

    if (item.gainNode && fadeSeconds > 0) {

LINE 844: unregisterActivePlaybackItem(playbackState, item);
----- CONTEXT -----
      fadeSeconds,
      reason: String(options.reason || "cutoff")
    });

    unregisterActivePlaybackItem(playbackState, item);

    return true;
  }

  function cutOffPlaybackItemsAtTime(playbackState, filter = {}, cutTimeSeconds = 0, options = {}) {
    const items = getActivePlaybackItemsAtTime(playbackState, cutTimeSeconds, filter);
    let count = 0;


LINE 953: kind: normalizeRuleDecisionToken(kind),
----- CONTEXT -----

    const window = {
      startsAtSeconds: Math.max(0, Number(startsAtSeconds) || 0),
      endsAtSeconds: Math.max(0, Number(endsAtSeconds) || 0),
      kind: normalizeRuleDecisionToken(kind),
      key: String(key || ""),
      family: normalizeRuleDecisionToken(family),
      tag: normalizeRuleDecisionToken(tag),
      reason: String(reason || "blocked_window")
    };

    if (window.endsAtSeconds < window.startsAtSeconds) {
      const temp = window.startsAtSeconds;

LINE 955: family: normalizeRuleDecisionToken(family),
----- CONTEXT -----
      startsAtSeconds: Math.max(0, Number(startsAtSeconds) || 0),
      endsAtSeconds: Math.max(0, Number(endsAtSeconds) || 0),
      kind: normalizeRuleDecisionToken(kind),
      key: String(key || ""),
      family: normalizeRuleDecisionToken(family),
      tag: normalizeRuleDecisionToken(tag),
      reason: String(reason || "blocked_window")
    };

    if (window.endsAtSeconds < window.startsAtSeconds) {
      const temp = window.startsAtSeconds;
      window.startsAtSeconds = window.endsAtSeconds;
      window.endsAtSeconds = temp;

LINE 956: tag: normalizeRuleDecisionToken(tag),
----- CONTEXT -----
      endsAtSeconds: Math.max(0, Number(endsAtSeconds) || 0),
      kind: normalizeRuleDecisionToken(kind),
      key: String(key || ""),
      family: normalizeRuleDecisionToken(family),
      tag: normalizeRuleDecisionToken(tag),
      reason: String(reason || "blocked_window")
    };

    if (window.endsAtSeconds < window.startsAtSeconds) {
      const temp = window.startsAtSeconds;
      window.startsAtSeconds = window.endsAtSeconds;
      window.endsAtSeconds = temp;
    }

LINE 973: const startSeconds = Number(context.startSeconds ?? 0);
----- CONTEXT -----

  function getMatchingActivationBlockWindow(playbackState, context) {
    if (!playbackState || !context) return null;

    const startSeconds = Number(context.startSeconds ?? 0);
    const kind = normalizeRuleDecisionToken(context.kind);
    const key = String(context.itemKey || "");
    const itemTags = context.itemTags || new Set();

    for (const window of playbackState.blockedWindows) {
      if (startSeconds < window.startsAtSeconds || startSeconds >= window.endsAtSeconds) continue;
      if (window.kind && window.kind !== kind) continue;
      if (window.key && window.key !== key) continue;

LINE 974: const kind = normalizeRuleDecisionToken(context.kind);
----- CONTEXT -----
  function getMatchingActivationBlockWindow(playbackState, context) {
    if (!playbackState || !context) return null;

    const startSeconds = Number(context.startSeconds ?? 0);
    const kind = normalizeRuleDecisionToken(context.kind);
    const key = String(context.itemKey || "");
    const itemTags = context.itemTags || new Set();

    for (const window of playbackState.blockedWindows) {
      if (startSeconds < window.startsAtSeconds || startSeconds >= window.endsAtSeconds) continue;
      if (window.kind && window.kind !== kind) continue;
      if (window.key && window.key !== key) continue;
      if (window.family && !itemTags.has(`family:${window.family}`) && !itemTags.has(window.family)) continue;

LINE 979: if (startSeconds < window.startsAtSeconds || startSeconds >= window.endsAtSeconds) continue;
----- CONTEXT -----
    const key = String(context.itemKey || "");
    const itemTags = context.itemTags || new Set();

    for (const window of playbackState.blockedWindows) {
      if (startSeconds < window.startsAtSeconds || startSeconds >= window.endsAtSeconds) continue;
      if (window.kind && window.kind !== kind) continue;
      if (window.key && window.key !== key) continue;
      if (window.family && !itemTags.has(`family:${window.family}`) && !itemTags.has(window.family)) continue;
      if (window.tag && !itemTags.has(window.tag)) continue;

      return window;
    }


LINE 991: function applyActivationBlockWindows(playbackState, context, decision) {
----- CONTEXT -----

    return null;
  }

  function applyActivationBlockWindows(playbackState, context, decision) {
    const window = getMatchingActivationBlockWindow(playbackState, context);

    if (!window) return decision;

    return blockRuleDecision(decision, "activation_block_window", {
      reason: window.reason,
      startsAtSeconds: window.startsAtSeconds,
      endsAtSeconds: window.endsAtSeconds

LINE 994: if (!window) return decision;
----- CONTEXT -----

  function applyActivationBlockWindows(playbackState, context, decision) {
    const window = getMatchingActivationBlockWindow(playbackState, context);

    if (!window) return decision;

    return blockRuleDecision(decision, "activation_block_window", {
      reason: window.reason,
      startsAtSeconds: window.startsAtSeconds,
      endsAtSeconds: window.endsAtSeconds
    });
  }


LINE 996: return blockRuleDecision(decision, "activation_block_window", {
----- CONTEXT -----
    const window = getMatchingActivationBlockWindow(playbackState, context);

    if (!window) return decision;

    return blockRuleDecision(decision, "activation_block_window", {
      reason: window.reason,
      startsAtSeconds: window.startsAtSeconds,
      endsAtSeconds: window.endsAtSeconds
    });
  }

  function isFamilyLockedByOtherItem(playbackState, family, itemId) {
    if (!playbackState || !family) return false;

LINE 1006: const normalizedFamily = normalizeRuleDecisionToken(family);
----- CONTEXT -----

  function isFamilyLockedByOtherItem(playbackState, family, itemId) {
    if (!playbackState || !family) return false;

    const normalizedFamily = normalizeRuleDecisionToken(family);
    const lockedItemId = playbackState.familyLocks.get(normalizedFamily);

    return Boolean(lockedItemId && lockedItemId !== itemId);
  }

  function applyFamilyLock(playbackState, context, decision, family) {
    if (!playbackState || !context || !family) return decision;


LINE 1012: function applyFamilyLock(playbackState, context, decision, family) {
----- CONTEXT -----

    return Boolean(lockedItemId && lockedItemId !== itemId);
  }

  function applyFamilyLock(playbackState, context, decision, family) {
    if (!playbackState || !context || !family) return decision;

    if (isFamilyLockedByOtherItem(playbackState, family, context.lifecycleId || context.itemKey)) {
      return blockRuleDecision(decision, "family_locked", {
        family: normalizeRuleDecisionToken(family),
        lockedBy: playbackState.familyLocks.get(normalizeRuleDecisionToken(family))
      });
    }

LINE 1013: if (!playbackState || !context || !family) return decision;
----- CONTEXT -----
    return Boolean(lockedItemId && lockedItemId !== itemId);
  }

  function applyFamilyLock(playbackState, context, decision, family) {
    if (!playbackState || !context || !family) return decision;

    if (isFamilyLockedByOtherItem(playbackState, family, context.lifecycleId || context.itemKey)) {
      return blockRuleDecision(decision, "family_locked", {
        family: normalizeRuleDecisionToken(family),
        lockedBy: playbackState.familyLocks.get(normalizeRuleDecisionToken(family))
      });
    }


LINE 1016: return blockRuleDecision(decision, "family_locked", {
----- CONTEXT -----
  function applyFamilyLock(playbackState, context, decision, family) {
    if (!playbackState || !context || !family) return decision;

    if (isFamilyLockedByOtherItem(playbackState, family, context.lifecycleId || context.itemKey)) {
      return blockRuleDecision(decision, "family_locked", {
        family: normalizeRuleDecisionToken(family),
        lockedBy: playbackState.familyLocks.get(normalizeRuleDecisionToken(family))
      });
    }

    return decision;
  }
  function getRuleProfilePool() {

LINE 1017: family: normalizeRuleDecisionToken(family),
----- CONTEXT -----
    if (!playbackState || !context || !family) return decision;

    if (isFamilyLockedByOtherItem(playbackState, family, context.lifecycleId || context.itemKey)) {
      return blockRuleDecision(decision, "family_locked", {
        family: normalizeRuleDecisionToken(family),
        lockedBy: playbackState.familyLocks.get(normalizeRuleDecisionToken(family))
      });
    }

    return decision;
  }
  function getRuleProfilePool() {
    const pools = catalog?.rulePools || {};

LINE 1018: lockedBy: playbackState.familyLocks.get(normalizeRuleDecisionToken(family))
----- CONTEXT -----

    if (isFamilyLockedByOtherItem(playbackState, family, context.lifecycleId || context.itemKey)) {
      return blockRuleDecision(decision, "family_locked", {
        family: normalizeRuleDecisionToken(family),
        lockedBy: playbackState.familyLocks.get(normalizeRuleDecisionToken(family))
      });
    }

    return decision;
  }
  function getRuleProfilePool() {
    const pools = catalog?.rulePools || {};


LINE 1022: return decision;
----- CONTEXT -----
        lockedBy: playbackState.familyLocks.get(normalizeRuleDecisionToken(family))
      });
    }

    return decision;
  }
  function getRuleProfilePool() {
    const pools = catalog?.rulePools || {};

    return (
      pools.ruleProfiles ||
      pools.stemRules ||
      pools.activationRules ||

LINE 1056: const normalizedKey = normalizeRuleDecisionToken(key);
----- CONTEXT -----
    if (exact && typeof exact === "object" && !Array.isArray(exact)) {
      return exact;
    }

    const normalizedKey = normalizeRuleDecisionToken(key);

    for (const [mapKey, value] of Object.entries(map)) {
      if (
        normalizeRuleDecisionToken(mapKey) === normalizedKey &&
        value &&
        typeof value === "object" &&
        !Array.isArray(value)
      ) {

LINE 1060: normalizeRuleDecisionToken(mapKey) === normalizedKey &&
----- CONTEXT -----
    const normalizedKey = normalizeRuleDecisionToken(key);

    for (const [mapKey, value] of Object.entries(map)) {
      if (
        normalizeRuleDecisionToken(mapKey) === normalizedKey &&
        value &&
        typeof value === "object" &&
        !Array.isArray(value)
      ) {
        return value;
      }
    }


LINE 1150: return normalizeRuleDecisionToken(cleanName);
----- CONTEXT -----
  function getMidiPatternSampleTag(pattern) {
    const samplePath = String(pattern?.samplePath || "");
    const fileName = samplePath.split(/[\\/]/).pop() || "";
    const cleanName = fileName.replace(/\.[^.]+$/, "");
    return normalizeRuleDecisionToken(cleanName);
  }

  function getMidiPatternRuleEntry(pattern) {
    if (!pattern) return null;

    const entry = getCatalogEntry(pattern.file);
    const sampleTag = getMidiPatternSampleTag(pattern);
    const tags = new Set(entry?.tags || []);

LINE 1382: tag: normalizeRuleDecisionToken(target),
----- CONTEXT -----
    if (typeof target === "string") {
      return {
        key: "",
        family: "",
        tag: normalizeRuleDecisionToken(target),
        kind: "",
        type: "",
        sectionType: ""
      };
    }

    if (!target || typeof target !== "object") {
      return {

LINE 1402: family: normalizeRuleDecisionToken(target.family || target.group || ""),
----- CONTEXT -----
    }

    return {
      key: String(target.key || target.file || target.stem || ""),
      family: normalizeRuleDecisionToken(target.family || target.group || ""),
      tag: normalizeRuleDecisionToken(target.tag || target.hasTag || target.tagged || ""),
      kind: normalizeRuleDecisionToken(target.kind || ""),
      type: normalizeRuleDecisionToken(target.type || ""),
      sectionType: normalizeRuleDecisionToken(target.sectionType || target.section || "")
    };
  }

  function getRuleTargets(rule) {

LINE 1403: tag: normalizeRuleDecisionToken(target.tag || target.hasTag || target.tagged || ""),
----- CONTEXT -----

    return {
      key: String(target.key || target.file || target.stem || ""),
      family: normalizeRuleDecisionToken(target.family || target.group || ""),
      tag: normalizeRuleDecisionToken(target.tag || target.hasTag || target.tagged || ""),
      kind: normalizeRuleDecisionToken(target.kind || ""),
      type: normalizeRuleDecisionToken(target.type || ""),
      sectionType: normalizeRuleDecisionToken(target.sectionType || target.section || "")
    };
  }

  function getRuleTargets(rule) {
    if (!rule) return [];

LINE 1404: kind: normalizeRuleDecisionToken(target.kind || ""),
----- CONTEXT -----
    return {
      key: String(target.key || target.file || target.stem || ""),
      family: normalizeRuleDecisionToken(target.family || target.group || ""),
      tag: normalizeRuleDecisionToken(target.tag || target.hasTag || target.tagged || ""),
      kind: normalizeRuleDecisionToken(target.kind || ""),
      type: normalizeRuleDecisionToken(target.type || ""),
      sectionType: normalizeRuleDecisionToken(target.sectionType || target.section || "")
    };
  }

  function getRuleTargets(rule) {
    if (!rule) return [];


LINE 1405: type: normalizeRuleDecisionToken(target.type || ""),
----- CONTEXT -----
      key: String(target.key || target.file || target.stem || ""),
      family: normalizeRuleDecisionToken(target.family || target.group || ""),
      tag: normalizeRuleDecisionToken(target.tag || target.hasTag || target.tagged || ""),
      kind: normalizeRuleDecisionToken(target.kind || ""),
      type: normalizeRuleDecisionToken(target.type || ""),
      sectionType: normalizeRuleDecisionToken(target.sectionType || target.section || "")
    };
  }

  function getRuleTargets(rule) {
    if (!rule) return [];

    if (Array.isArray(rule)) return rule.map(target => normalizeRuleTargetFilter(target));

LINE 1406: sectionType: normalizeRuleDecisionToken(target.sectionType || target.section || "")
----- CONTEXT -----
      family: normalizeRuleDecisionToken(target.family || target.group || ""),
      tag: normalizeRuleDecisionToken(target.tag || target.hasTag || target.tagged || ""),
      kind: normalizeRuleDecisionToken(target.kind || ""),
      type: normalizeRuleDecisionToken(target.type || ""),
      sectionType: normalizeRuleDecisionToken(target.sectionType || target.section || "")
    };
  }

  function getRuleTargets(rule) {
    if (!rule) return [];

    if (Array.isArray(rule)) return rule.map(target => normalizeRuleTargetFilter(target));


LINE 1435: const tags = getEntryRuleDecisionTags(entry);
----- CONTEXT -----
  function doesRuleTargetMatchEntry(target, entry) {
    if (!entry) return false;

    const filter = normalizeRuleTargetFilter(target);
    const tags = getEntryRuleDecisionTags(entry);

    if (filter.key && filter.key !== entry.key) return false;
    if (filter.family && normalizeRuleDecisionToken(entry.family) !== filter.family) return false;
    if (filter.tag && !tags.has(filter.tag)) return false;
    if (filter.kind && normalizeRuleDecisionToken(entry.type) !== filter.kind) return false;
    if (filter.type && normalizeRuleDecisionToken(entry.type) !== filter.type) return false;

    return Boolean(filter.key || filter.family || filter.tag || filter.kind || filter.type);

LINE 1438: if (filter.family && normalizeRuleDecisionToken(entry.family) !== filter.family) return false;
----- CONTEXT -----
    const filter = normalizeRuleTargetFilter(target);
    const tags = getEntryRuleDecisionTags(entry);

    if (filter.key && filter.key !== entry.key) return false;
    if (filter.family && normalizeRuleDecisionToken(entry.family) !== filter.family) return false;
    if (filter.tag && !tags.has(filter.tag)) return false;
    if (filter.kind && normalizeRuleDecisionToken(entry.type) !== filter.kind) return false;
    if (filter.type && normalizeRuleDecisionToken(entry.type) !== filter.type) return false;

    return Boolean(filter.key || filter.family || filter.tag || filter.kind || filter.type);
  }

  function doesRuleTargetMatchContext(target, context) {

LINE 1440: if (filter.kind && normalizeRuleDecisionToken(entry.type) !== filter.kind) return false;
----- CONTEXT -----

    if (filter.key && filter.key !== entry.key) return false;
    if (filter.family && normalizeRuleDecisionToken(entry.family) !== filter.family) return false;
    if (filter.tag && !tags.has(filter.tag)) return false;
    if (filter.kind && normalizeRuleDecisionToken(entry.type) !== filter.kind) return false;
    if (filter.type && normalizeRuleDecisionToken(entry.type) !== filter.type) return false;

    return Boolean(filter.key || filter.family || filter.tag || filter.kind || filter.type);
  }

  function doesRuleTargetMatchContext(target, context) {
    if (!context) return false;


LINE 1441: if (filter.type && normalizeRuleDecisionToken(entry.type) !== filter.type) return false;
----- CONTEXT -----
    if (filter.key && filter.key !== entry.key) return false;
    if (filter.family && normalizeRuleDecisionToken(entry.family) !== filter.family) return false;
    if (filter.tag && !tags.has(filter.tag)) return false;
    if (filter.kind && normalizeRuleDecisionToken(entry.type) !== filter.kind) return false;
    if (filter.type && normalizeRuleDecisionToken(entry.type) !== filter.type) return false;

    return Boolean(filter.key || filter.family || filter.tag || filter.kind || filter.type);
  }

  function doesRuleTargetMatchContext(target, context) {
    if (!context) return false;

    const filter = normalizeRuleTargetFilter(target);

LINE 1452: if (filter.kind && normalizeRuleDecisionToken(context.kind) !== filter.kind) return false;
----- CONTEXT -----

    const filter = normalizeRuleTargetFilter(target);

    if (filter.key && filter.key !== context.itemKey) return false;
    if (filter.kind && normalizeRuleDecisionToken(context.kind) !== filter.kind) return false;
    if (filter.sectionType && normalizeRuleDecisionToken(context.sectionType) !== filter.sectionType) return false;

    if (filter.family) {
      const familyTag = `family:${filter.family}`;
      if (!context.itemTags?.has(familyTag) && !context.itemTags?.has(filter.family)) return false;
    }

    if (filter.tag && !context.itemTags?.has(filter.tag)) return false;

LINE 1453: if (filter.sectionType && normalizeRuleDecisionToken(context.sectionType) !== filter.sectionType) return false;
----- CONTEXT -----
    const filter = normalizeRuleTargetFilter(target);

    if (filter.key && filter.key !== context.itemKey) return false;
    if (filter.kind && normalizeRuleDecisionToken(context.kind) !== filter.kind) return false;
    if (filter.sectionType && normalizeRuleDecisionToken(context.sectionType) !== filter.sectionType) return false;

    if (filter.family) {
      const familyTag = `family:${filter.family}`;
      if (!context.itemTags?.has(familyTag) && !context.itemTags?.has(filter.family)) return false;
    }

    if (filter.tag && !context.itemTags?.has(filter.tag)) return false;


LINE 1471: if (filter.kind && normalizeRuleDecisionToken(item.kind) !== filter.kind) return false;
----- CONTEXT -----

    const filter = normalizeRuleTargetFilter(target);

    if (filter.key && filter.key !== item.key) return false;
    if (filter.kind && normalizeRuleDecisionToken(item.kind) !== filter.kind) return false;
    if (filter.family && normalizeRuleDecisionToken(item.family) !== filter.family) return false;
    if (filter.tag && !item.tags.includes(filter.tag)) return false;

    return Boolean(filter.key || filter.family || filter.tag || filter.kind);
  }

  function getPlaybackFilterFromRuleTarget(target) {
    const filter = normalizeRuleTargetFilter(target);

LINE 1472: if (filter.family && normalizeRuleDecisionToken(item.family) !== filter.family) return false;
----- CONTEXT -----
    const filter = normalizeRuleTargetFilter(target);

    if (filter.key && filter.key !== item.key) return false;
    if (filter.kind && normalizeRuleDecisionToken(item.kind) !== filter.kind) return false;
    if (filter.family && normalizeRuleDecisionToken(item.family) !== filter.family) return false;
    if (filter.tag && !item.tags.includes(filter.tag)) return false;

    return Boolean(filter.key || filter.family || filter.tag || filter.kind);
  }

  function getPlaybackFilterFromRuleTarget(target) {
    const filter = normalizeRuleTargetFilter(target);


LINE 1508: function applyHardClashRulesToDecision(playbackState, context, decision, rules = []) {
----- CONTEXT -----
    }

    return matches;
  }
  function applyHardClashRulesToDecision(playbackState, context, decision, rules = []) {
    if (!playbackState || !context || !decision || !Array.isArray(rules)) return decision;

    for (const rule of rules) {
      const targets = getRuleTargets(rule);
      if (!targets.length) continue;

      const activeMatches = findActivePlaybackItemsForRuleTargets(
        playbackState,

LINE 1509: if (!playbackState || !context || !decision || !Array.isArray(rules)) return decision;
----- CONTEXT -----

    return matches;
  }
  function applyHardClashRulesToDecision(playbackState, context, decision, rules = []) {
    if (!playbackState || !context || !decision || !Array.isArray(rules)) return decision;

    for (const rule of rules) {
      const targets = getRuleTargets(rule);
      if (!targets.length) continue;

      const activeMatches = findActivePlaybackItemsForRuleTargets(
        playbackState,
        targets,

LINE 1518: context.startSeconds
----- CONTEXT -----

      const activeMatches = findActivePlaybackItemsForRuleTargets(
        playbackState,
        targets,
        context.startSeconds
      );

      if (activeMatches.length) {
        return blockRuleDecision(decision, "hard_clash", {
          ruleId: rule.id || "",
          targets,
          activeMatches: activeMatches.map(item => ({
            id: item.id,

LINE 1522: return blockRuleDecision(decision, "hard_clash", {
----- CONTEXT -----
        context.startSeconds
      );

      if (activeMatches.length) {
        return blockRuleDecision(decision, "hard_clash", {
          ruleId: rule.id || "",
          targets,
          activeMatches: activeMatches.map(item => ({
            id: item.id,
            key: item.key,
            family: item.family
          }))
        });

LINE 1534: return decision;
----- CONTEXT -----
        });
      }
    }

    return decision;
  }

  function applySoftMultiplierRulesToDecision(playbackState, context, decision, rules = []) {
    if (!playbackState || !context || !decision || !Array.isArray(rules)) return decision;

    for (const rule of rules) {
      const targets = getRuleTargets(rule);
      const multiplier = Number(rule.multiplier ?? rule.chanceMultiplier ?? rule.activationMultiplier ?? 1);

LINE 1537: function applySoftMultiplierRulesToDecision(playbackState, context, decision, rules = []) {
----- CONTEXT -----

    return decision;
  }

  function applySoftMultiplierRulesToDecision(playbackState, context, decision, rules = []) {
    if (!playbackState || !context || !decision || !Array.isArray(rules)) return decision;

    for (const rule of rules) {
      const targets = getRuleTargets(rule);
      const multiplier = Number(rule.multiplier ?? rule.chanceMultiplier ?? rule.activationMultiplier ?? 1);

      if (!targets.length || !Number.isFinite(multiplier)) continue;


LINE 1538: if (!playbackState || !context || !decision || !Array.isArray(rules)) return decision;
----- CONTEXT -----
    return decision;
  }

  function applySoftMultiplierRulesToDecision(playbackState, context, decision, rules = []) {
    if (!playbackState || !context || !decision || !Array.isArray(rules)) return decision;

    for (const rule of rules) {
      const targets = getRuleTargets(rule);
      const multiplier = Number(rule.multiplier ?? rule.chanceMultiplier ?? rule.activationMultiplier ?? 1);

      if (!targets.length || !Number.isFinite(multiplier)) continue;

      const activeMatches = findActivePlaybackItemsForRuleTargets(

LINE 1549: context.startSeconds
----- CONTEXT -----

      const activeMatches = findActivePlaybackItemsForRuleTargets(
        playbackState,
        targets,
        context.startSeconds
      );

      if (activeMatches.length) {
        multiplyRuleDecisionChance(decision, multiplier, "soft_multiplier", {
          ruleId: rule.id || "",
          targets,
          activeMatches: activeMatches.map(item => ({
            id: item.id,

LINE 1553: multiplyRuleDecisionChance(decision, multiplier, "soft_multiplier", {
----- CONTEXT -----
        context.startSeconds
      );

      if (activeMatches.length) {
        multiplyRuleDecisionChance(decision, multiplier, "soft_multiplier", {
          ruleId: rule.id || "",
          targets,
          activeMatches: activeMatches.map(item => ({
            id: item.id,
            key: item.key,
            family: item.family
          }))
        });

LINE 1565: return decision;
----- CONTEXT -----
        });
      }
    }

    return decision;
  }


  function getGroupedActivationDecisionId(rule, context) {
    const groupId = String(
      rule.groupId ||
      rule.group ||
      rule.id ||

LINE 1569: function getGroupedActivationDecisionId(rule, context) {
----- CONTEXT -----
    return decision;
  }


  function getGroupedActivationDecisionId(rule, context) {
    const groupId = String(
      rule.groupId ||
      rule.group ||
      rule.id ||
      rule.name ||
      ""
    );


LINE 1580: const scope = normalizeRuleDecisionToken(rule.scope || "opportunity");
----- CONTEXT -----
    );

    if (!groupId || !context) return "";

    const scope = normalizeRuleDecisionToken(rule.scope || "opportunity");
    const sectionId = String(context.sectionId || "");
    const localBarIndex = context.localBarIndex === null || context.localBarIndex === undefined
      ? "none"
      : String(context.localBarIndex);
    const startSeconds = Number.isFinite(Number(context.startSeconds))
      ? Number(context.startSeconds).toFixed(4)
      : "none";


LINE 1585: const startSeconds = Number.isFinite(Number(context.startSeconds))
----- CONTEXT -----
    const sectionId = String(context.sectionId || "");
    const localBarIndex = context.localBarIndex === null || context.localBarIndex === undefined
      ? "none"
      : String(context.localBarIndex);
    const startSeconds = Number.isFinite(Number(context.startSeconds))
      ? Number(context.startSeconds).toFixed(4)
      : "none";

    if (scope === "section") {
      return `${groupId}:section:${sectionId}`;
    }

    if (scope === "bar" || scope === "opportunity") {

LINE 1586: ? Number(context.startSeconds).toFixed(4)
----- CONTEXT -----
    const localBarIndex = context.localBarIndex === null || context.localBarIndex === undefined
      ? "none"
      : String(context.localBarIndex);
    const startSeconds = Number.isFinite(Number(context.startSeconds))
      ? Number(context.startSeconds).toFixed(4)
      : "none";

    if (scope === "section") {
      return `${groupId}:section:${sectionId}`;
    }

    if (scope === "bar" || scope === "opportunity") {
      return `${groupId}:bar:${sectionId}:${localBarIndex}`;

LINE 1598: return `${groupId}:time:${startSeconds}`;
----- CONTEXT -----
      return `${groupId}:bar:${sectionId}:${localBarIndex}`;
    }

    if (scope === "time") {
      return `${groupId}:time:${startSeconds}`;
    }

    return `${groupId}:opportunity:${sectionId}:${localBarIndex}`;
  }

  function applyGroupedActivationRulesToDecision(playbackState, context, decision, rules = [], random = null) {
    if (!playbackState || !context || !decision || !Array.isArray(rules)) return decision;


LINE 1604: function applyGroupedActivationRulesToDecision(playbackState, context, decision, rules = [], random = null) {
----- CONTEXT -----

    return `${groupId}:opportunity:${sectionId}:${localBarIndex}`;
  }

  function applyGroupedActivationRulesToDecision(playbackState, context, decision, rules = [], random = null) {
    if (!playbackState || !context || !decision || !Array.isArray(rules)) return decision;

    if (!playbackState.groupedActivationDecisions) {
      playbackState.groupedActivationDecisions = new Map();
    }

    const safeRandom = typeof random === "function" ? random : (() => 1);


LINE 1605: if (!playbackState || !context || !decision || !Array.isArray(rules)) return decision;
----- CONTEXT -----
    return `${groupId}:opportunity:${sectionId}:${localBarIndex}`;
  }

  function applyGroupedActivationRulesToDecision(playbackState, context, decision, rules = [], random = null) {
    if (!playbackState || !context || !decision || !Array.isArray(rules)) return decision;

    if (!playbackState.groupedActivationDecisions) {
      playbackState.groupedActivationDecisions = new Map();
    }

    const safeRandom = typeof random === "function" ? random : (() => 1);

    for (const rule of rules) {

LINE 1607: if (!playbackState.groupedActivationDecisions) {
----- CONTEXT -----

  function applyGroupedActivationRulesToDecision(playbackState, context, decision, rules = [], random = null) {
    if (!playbackState || !context || !decision || !Array.isArray(rules)) return decision;

    if (!playbackState.groupedActivationDecisions) {
      playbackState.groupedActivationDecisions = new Map();
    }

    const safeRandom = typeof random === "function" ? random : (() => 1);

    for (const rule of rules) {
      const decisionId = getGroupedActivationDecisionId(rule, context);
      if (!decisionId) continue;

LINE 1608: playbackState.groupedActivationDecisions = new Map();
----- CONTEXT -----
  function applyGroupedActivationRulesToDecision(playbackState, context, decision, rules = [], random = null) {
    if (!playbackState || !context || !decision || !Array.isArray(rules)) return decision;

    if (!playbackState.groupedActivationDecisions) {
      playbackState.groupedActivationDecisions = new Map();
    }

    const safeRandom = typeof random === "function" ? random : (() => 1);

    for (const rule of rules) {
      const decisionId = getGroupedActivationDecisionId(rule, context);
      if (!decisionId) continue;


LINE 1614: const decisionId = getGroupedActivationDecisionId(rule, context);
----- CONTEXT -----

    const safeRandom = typeof random === "function" ? random : (() => 1);

    for (const rule of rules) {
      const decisionId = getGroupedActivationDecisionId(rule, context);
      if (!decisionId) continue;

      const existingDecision = playbackState.groupedActivationDecisions.get(decisionId);

      if (existingDecision) {
        addRuleDecisionReason(decision, "grouped_activation_reused", {
          ruleId: rule.id || rule.groupId || rule.group || "",
          decisionId,

LINE 1615: if (!decisionId) continue;
----- CONTEXT -----
    const safeRandom = typeof random === "function" ? random : (() => 1);

    for (const rule of rules) {
      const decisionId = getGroupedActivationDecisionId(rule, context);
      if (!decisionId) continue;

      const existingDecision = playbackState.groupedActivationDecisions.get(decisionId);

      if (existingDecision) {
        addRuleDecisionReason(decision, "grouped_activation_reused", {
          ruleId: rule.id || rule.groupId || rule.group || "",
          decisionId,
          groupedAllowed: existingDecision.allowed,

LINE 1617: const existingDecision = playbackState.groupedActivationDecisions.get(decisionId);
----- CONTEXT -----
    for (const rule of rules) {
      const decisionId = getGroupedActivationDecisionId(rule, context);
      if (!decisionId) continue;

      const existingDecision = playbackState.groupedActivationDecisions.get(decisionId);

      if (existingDecision) {
        addRuleDecisionReason(decision, "grouped_activation_reused", {
          ruleId: rule.id || rule.groupId || rule.group || "",
          decisionId,
          groupedAllowed: existingDecision.allowed,
          groupedRoll: existingDecision.roll,
          groupedChance: existingDecision.finalChance

LINE 1619: if (existingDecision) {
----- CONTEXT -----
      if (!decisionId) continue;

      const existingDecision = playbackState.groupedActivationDecisions.get(decisionId);

      if (existingDecision) {
        addRuleDecisionReason(decision, "grouped_activation_reused", {
          ruleId: rule.id || rule.groupId || rule.group || "",
          decisionId,
          groupedAllowed: existingDecision.allowed,
          groupedRoll: existingDecision.roll,
          groupedChance: existingDecision.finalChance
        });


LINE 1620: addRuleDecisionReason(decision, "grouped_activation_reused", {
----- CONTEXT -----

      const existingDecision = playbackState.groupedActivationDecisions.get(decisionId);

      if (existingDecision) {
        addRuleDecisionReason(decision, "grouped_activation_reused", {
          ruleId: rule.id || rule.groupId || rule.group || "",
          decisionId,
          groupedAllowed: existingDecision.allowed,
          groupedRoll: existingDecision.roll,
          groupedChance: existingDecision.finalChance
        });

        if (!existingDecision.allowed) {

LINE 1622: decisionId,
----- CONTEXT -----

      if (existingDecision) {
        addRuleDecisionReason(decision, "grouped_activation_reused", {
          ruleId: rule.id || rule.groupId || rule.group || "",
          decisionId,
          groupedAllowed: existingDecision.allowed,
          groupedRoll: existingDecision.roll,
          groupedChance: existingDecision.finalChance
        });

        if (!existingDecision.allowed) {
          return blockRuleDecision(decision, "grouped_activation_blocked", {
            ruleId: rule.id || rule.groupId || rule.group || "",

LINE 1623: groupedAllowed: existingDecision.allowed,
----- CONTEXT -----
      if (existingDecision) {
        addRuleDecisionReason(decision, "grouped_activation_reused", {
          ruleId: rule.id || rule.groupId || rule.group || "",
          decisionId,
          groupedAllowed: existingDecision.allowed,
          groupedRoll: existingDecision.roll,
          groupedChance: existingDecision.finalChance
        });

        if (!existingDecision.allowed) {
          return blockRuleDecision(decision, "grouped_activation_blocked", {
            ruleId: rule.id || rule.groupId || rule.group || "",
            decisionId,

LINE 1624: groupedRoll: existingDecision.roll,
----- CONTEXT -----
        addRuleDecisionReason(decision, "grouped_activation_reused", {
          ruleId: rule.id || rule.groupId || rule.group || "",
          decisionId,
          groupedAllowed: existingDecision.allowed,
          groupedRoll: existingDecision.roll,
          groupedChance: existingDecision.finalChance
        });

        if (!existingDecision.allowed) {
          return blockRuleDecision(decision, "grouped_activation_blocked", {
            ruleId: rule.id || rule.groupId || rule.group || "",
            decisionId,
            groupedRoll: existingDecision.roll,

LINE 1625: groupedChance: existingDecision.finalChance
----- CONTEXT -----
          ruleId: rule.id || rule.groupId || rule.group || "",
          decisionId,
          groupedAllowed: existingDecision.allowed,
          groupedRoll: existingDecision.roll,
          groupedChance: existingDecision.finalChance
        });

        if (!existingDecision.allowed) {
          return blockRuleDecision(decision, "grouped_activation_blocked", {
            ruleId: rule.id || rule.groupId || rule.group || "",
            decisionId,
            groupedRoll: existingDecision.roll,
            groupedChance: existingDecision.finalChance

LINE 1628: if (!existingDecision.allowed) {
----- CONTEXT -----
          groupedRoll: existingDecision.roll,
          groupedChance: existingDecision.finalChance
        });

        if (!existingDecision.allowed) {
          return blockRuleDecision(decision, "grouped_activation_blocked", {
            ruleId: rule.id || rule.groupId || rule.group || "",
            decisionId,
            groupedRoll: existingDecision.roll,
            groupedChance: existingDecision.finalChance
          });
        }


LINE 1629: return blockRuleDecision(decision, "grouped_activation_blocked", {
----- CONTEXT -----
          groupedChance: existingDecision.finalChance
        });

        if (!existingDecision.allowed) {
          return blockRuleDecision(decision, "grouped_activation_blocked", {
            ruleId: rule.id || rule.groupId || rule.group || "",
            decisionId,
            groupedRoll: existingDecision.roll,
            groupedChance: existingDecision.finalChance
          });
        }

        decision.baseChance = 1;

LINE 1631: decisionId,
----- CONTEXT -----

        if (!existingDecision.allowed) {
          return blockRuleDecision(decision, "grouped_activation_blocked", {
            ruleId: rule.id || rule.groupId || rule.group || "",
            decisionId,
            groupedRoll: existingDecision.roll,
            groupedChance: existingDecision.finalChance
          });
        }

        decision.baseChance = 1;
        decision.chanceMultiplier = 1;
        decision.finalChance = 1;

LINE 1632: groupedRoll: existingDecision.roll,
----- CONTEXT -----
        if (!existingDecision.allowed) {
          return blockRuleDecision(decision, "grouped_activation_blocked", {
            ruleId: rule.id || rule.groupId || rule.group || "",
            decisionId,
            groupedRoll: existingDecision.roll,
            groupedChance: existingDecision.finalChance
          });
        }

        decision.baseChance = 1;
        decision.chanceMultiplier = 1;
        decision.finalChance = 1;
        continue;

LINE 1633: groupedChance: existingDecision.finalChance
----- CONTEXT -----
          return blockRuleDecision(decision, "grouped_activation_blocked", {
            ruleId: rule.id || rule.groupId || rule.group || "",
            decisionId,
            groupedRoll: existingDecision.roll,
            groupedChance: existingDecision.finalChance
          });
        }

        decision.baseChance = 1;
        decision.chanceMultiplier = 1;
        decision.finalChance = 1;
        continue;
      }

LINE 1637: decision.baseChance = 1;
----- CONTEXT -----
            groupedChance: existingDecision.finalChance
          });
        }

        decision.baseChance = 1;
        decision.chanceMultiplier = 1;
        decision.finalChance = 1;
        continue;
      }

      const explicitChance = rule.chance ?? rule.activationChance ?? rule.groupChance;
      const groupedChance = explicitChance === undefined
        ? clampProbability(decision.finalChance, 1)

LINE 1638: decision.chanceMultiplier = 1;
----- CONTEXT -----
          });
        }

        decision.baseChance = 1;
        decision.chanceMultiplier = 1;
        decision.finalChance = 1;
        continue;
      }

      const explicitChance = rule.chance ?? rule.activationChance ?? rule.groupChance;
      const groupedChance = explicitChance === undefined
        ? clampProbability(decision.finalChance, 1)
        : clampProbability(explicitChance, decision.finalChance);

LINE 1639: decision.finalChance = 1;
----- CONTEXT -----
        }

        decision.baseChance = 1;
        decision.chanceMultiplier = 1;
        decision.finalChance = 1;
        continue;
      }

      const explicitChance = rule.chance ?? rule.activationChance ?? rule.groupChance;
      const groupedChance = explicitChance === undefined
        ? clampProbability(decision.finalChance, 1)
        : clampProbability(explicitChance, decision.finalChance);
      const roll = safeRandom();

LINE 1645: ? clampProbability(decision.finalChance, 1)
----- CONTEXT -----
      }

      const explicitChance = rule.chance ?? rule.activationChance ?? rule.groupChance;
      const groupedChance = explicitChance === undefined
        ? clampProbability(decision.finalChance, 1)
        : clampProbability(explicitChance, decision.finalChance);
      const roll = safeRandom();
      const allowed = roll <= groupedChance;

      playbackState.groupedActivationDecisions.set(decisionId, {
        allowed,
        roll,
        finalChance: groupedChance

LINE 1646: : clampProbability(explicitChance, decision.finalChance);
----- CONTEXT -----

      const explicitChance = rule.chance ?? rule.activationChance ?? rule.groupChance;
      const groupedChance = explicitChance === undefined
        ? clampProbability(decision.finalChance, 1)
        : clampProbability(explicitChance, decision.finalChance);
      const roll = safeRandom();
      const allowed = roll <= groupedChance;

      playbackState.groupedActivationDecisions.set(decisionId, {
        allowed,
        roll,
        finalChance: groupedChance
      });

LINE 1648: const allowed = roll <= groupedChance;
----- CONTEXT -----
      const groupedChance = explicitChance === undefined
        ? clampProbability(decision.finalChance, 1)
        : clampProbability(explicitChance, decision.finalChance);
      const roll = safeRandom();
      const allowed = roll <= groupedChance;

      playbackState.groupedActivationDecisions.set(decisionId, {
        allowed,
        roll,
        finalChance: groupedChance
      });

      addRuleDecisionReason(decision, "grouped_activation_created", {

LINE 1650: playbackState.groupedActivationDecisions.set(decisionId, {
----- CONTEXT -----
        : clampProbability(explicitChance, decision.finalChance);
      const roll = safeRandom();
      const allowed = roll <= groupedChance;

      playbackState.groupedActivationDecisions.set(decisionId, {
        allowed,
        roll,
        finalChance: groupedChance
      });

      addRuleDecisionReason(decision, "grouped_activation_created", {
        ruleId: rule.id || rule.groupId || rule.group || "",
        decisionId,

LINE 1651: allowed,
----- CONTEXT -----
      const roll = safeRandom();
      const allowed = roll <= groupedChance;

      playbackState.groupedActivationDecisions.set(decisionId, {
        allowed,
        roll,
        finalChance: groupedChance
      });

      addRuleDecisionReason(decision, "grouped_activation_created", {
        ruleId: rule.id || rule.groupId || rule.group || "",
        decisionId,
        groupedRoll: roll,

LINE 1653: finalChance: groupedChance
----- CONTEXT -----

      playbackState.groupedActivationDecisions.set(decisionId, {
        allowed,
        roll,
        finalChance: groupedChance
      });

      addRuleDecisionReason(decision, "grouped_activation_created", {
        ruleId: rule.id || rule.groupId || rule.group || "",
        decisionId,
        groupedRoll: roll,
        groupedChance
      });

LINE 1656: addRuleDecisionReason(decision, "grouped_activation_created", {
----- CONTEXT -----
        roll,
        finalChance: groupedChance
      });

      addRuleDecisionReason(decision, "grouped_activation_created", {
        ruleId: rule.id || rule.groupId || rule.group || "",
        decisionId,
        groupedRoll: roll,
        groupedChance
      });

      if (!allowed) {
        return blockRuleDecision(decision, "grouped_activation_blocked", {

LINE 1658: decisionId,
----- CONTEXT -----
      });

      addRuleDecisionReason(decision, "grouped_activation_created", {
        ruleId: rule.id || rule.groupId || rule.group || "",
        decisionId,
        groupedRoll: roll,
        groupedChance
      });

      if (!allowed) {
        return blockRuleDecision(decision, "grouped_activation_blocked", {
          ruleId: rule.id || rule.groupId || rule.group || "",
          decisionId,

LINE 1663: if (!allowed) {
----- CONTEXT -----
        groupedRoll: roll,
        groupedChance
      });

      if (!allowed) {
        return blockRuleDecision(decision, "grouped_activation_blocked", {
          ruleId: rule.id || rule.groupId || rule.group || "",
          decisionId,
          groupedRoll: roll,
          groupedChance
        });
      }


LINE 1664: return blockRuleDecision(decision, "grouped_activation_blocked", {
----- CONTEXT -----
        groupedChance
      });

      if (!allowed) {
        return blockRuleDecision(decision, "grouped_activation_blocked", {
          ruleId: rule.id || rule.groupId || rule.group || "",
          decisionId,
          groupedRoll: roll,
          groupedChance
        });
      }

      decision.baseChance = 1;

LINE 1666: decisionId,
----- CONTEXT -----

      if (!allowed) {
        return blockRuleDecision(decision, "grouped_activation_blocked", {
          ruleId: rule.id || rule.groupId || rule.group || "",
          decisionId,
          groupedRoll: roll,
          groupedChance
        });
      }

      decision.baseChance = 1;
      decision.chanceMultiplier = 1;
      decision.finalChance = 1;

LINE 1672: decision.baseChance = 1;
----- CONTEXT -----
          groupedChance
        });
      }

      decision.baseChance = 1;
      decision.chanceMultiplier = 1;
      decision.finalChance = 1;
    }

    return decision;
  }

  function applyDependencyRulesToDecision(playbackState, context, decision, rules = []) {

LINE 1673: decision.chanceMultiplier = 1;
----- CONTEXT -----
        });
      }

      decision.baseChance = 1;
      decision.chanceMultiplier = 1;
      decision.finalChance = 1;
    }

    return decision;
  }

  function applyDependencyRulesToDecision(playbackState, context, decision, rules = []) {
    if (!playbackState || !context || !decision || !Array.isArray(rules)) return decision;

LINE 1674: decision.finalChance = 1;
----- CONTEXT -----
      }

      decision.baseChance = 1;
      decision.chanceMultiplier = 1;
      decision.finalChance = 1;
    }

    return decision;
  }

  function applyDependencyRulesToDecision(playbackState, context, decision, rules = []) {
    if (!playbackState || !context || !decision || !Array.isArray(rules)) return decision;


LINE 1677: return decision;
----- CONTEXT -----
      decision.chanceMultiplier = 1;
      decision.finalChance = 1;
    }

    return decision;
  }

  function applyDependencyRulesToDecision(playbackState, context, decision, rules = []) {
    if (!playbackState || !context || !decision || !Array.isArray(rules)) return decision;

    for (const rule of rules) {
      const targets = getRuleTargets(rule);
      if (!targets.length) continue;

LINE 1680: function applyDependencyRulesToDecision(playbackState, context, decision, rules = []) {
----- CONTEXT -----

    return decision;
  }

  function applyDependencyRulesToDecision(playbackState, context, decision, rules = []) {
    if (!playbackState || !context || !decision || !Array.isArray(rules)) return decision;

    for (const rule of rules) {
      const targets = getRuleTargets(rule);
      if (!targets.length) continue;

      const activeMatches = findActivePlaybackItemsForRuleTargets(
        playbackState,

LINE 1681: if (!playbackState || !context || !decision || !Array.isArray(rules)) return decision;
----- CONTEXT -----
    return decision;
  }

  function applyDependencyRulesToDecision(playbackState, context, decision, rules = []) {
    if (!playbackState || !context || !decision || !Array.isArray(rules)) return decision;

    for (const rule of rules) {
      const targets = getRuleTargets(rule);
      if (!targets.length) continue;

      const activeMatches = findActivePlaybackItemsForRuleTargets(
        playbackState,
        targets,

LINE 1690: context.startSeconds
----- CONTEXT -----

      const activeMatches = findActivePlaybackItemsForRuleTargets(
        playbackState,
        targets,
        context.startSeconds
      );

      if (!activeMatches.length) {
        return blockRuleDecision(decision, "dependency_missing", {
          ruleId: rule.id || "",
          targets
        });
      }

LINE 1694: return blockRuleDecision(decision, "dependency_missing", {
----- CONTEXT -----
        context.startSeconds
      );

      if (!activeMatches.length) {
        return blockRuleDecision(decision, "dependency_missing", {
          ruleId: rule.id || "",
          targets
        });
      }
    }

    return decision;
  }

LINE 1701: return decision;
----- CONTEXT -----
        });
      }
    }

    return decision;
  }

  function applyTimedBlockRulesToDecision(playbackState, context, decision, rules = [], random = null) {
    if (!playbackState || !context || !decision || !Array.isArray(rules)) return decision;

    for (const rule of rules) {
      const targets = getRuleTargets(rule);
      const durationSeconds = Number(rule.durationSeconds ?? rule.seconds ?? 0);

LINE 1704: function applyTimedBlockRulesToDecision(playbackState, context, decision, rules = [], random = null) {
----- CONTEXT -----

    return decision;
  }

  function applyTimedBlockRulesToDecision(playbackState, context, decision, rules = [], random = null) {
    if (!playbackState || !context || !decision || !Array.isArray(rules)) return decision;

    for (const rule of rules) {
      const targets = getRuleTargets(rule);
      const durationSeconds = Number(rule.durationSeconds ?? rule.seconds ?? 0);

      if (!targets.length || !Number.isFinite(durationSeconds) || durationSeconds <= 0) continue;


LINE 1705: if (!playbackState || !context || !decision || !Array.isArray(rules)) return decision;
----- CONTEXT -----
    return decision;
  }

  function applyTimedBlockRulesToDecision(playbackState, context, decision, rules = [], random = null) {
    if (!playbackState || !context || !decision || !Array.isArray(rules)) return decision;

    for (const rule of rules) {
      const targets = getRuleTargets(rule);
      const durationSeconds = Number(rule.durationSeconds ?? rule.seconds ?? 0);

      if (!targets.length || !Number.isFinite(durationSeconds) || durationSeconds <= 0) continue;

      const activeMatches = findActivePlaybackItemsForRuleTargets(

LINE 1716: context.startSeconds
----- CONTEXT -----

      const activeMatches = findActivePlaybackItemsForRuleTargets(
        playbackState,
        targets,
        context.startSeconds
      );

      const ruleChance = clampProbability(rule.chance ?? 1, 1);
      const ruleRoll = typeof random === "function"
        ? random()
        : (Number.isFinite(Number(rule.roll)) ? Number(rule.roll) : 0);

      if (activeMatches.length && ruleRoll < ruleChance) {

LINE 1725: const endSeconds = Number(context.startSeconds || 0) + durationSeconds;
----- CONTEXT -----
        ? random()
        : (Number.isFinite(Number(rule.roll)) ? Number(rule.roll) : 0);

      if (activeMatches.length && ruleRoll < ruleChance) {
        const endSeconds = Number(context.startSeconds || 0) + durationSeconds;

        addActivationBlockWindow(playbackState, {
          startsAtSeconds: context.startSeconds,
          endsAtSeconds: endSeconds,
          kind: context.kind,
          key: context.itemKey,
          reason: rule.id || "timed_block_rule"
        });

LINE 1728: startsAtSeconds: context.startSeconds,
----- CONTEXT -----
      if (activeMatches.length && ruleRoll < ruleChance) {
        const endSeconds = Number(context.startSeconds || 0) + durationSeconds;

        addActivationBlockWindow(playbackState, {
          startsAtSeconds: context.startSeconds,
          endsAtSeconds: endSeconds,
          kind: context.kind,
          key: context.itemKey,
          reason: rule.id || "timed_block_rule"
        });

        return blockRuleDecision(decision, "timed_block_rule", {
          ruleId: rule.id || "",

LINE 1735: return blockRuleDecision(decision, "timed_block_rule", {
----- CONTEXT -----
          key: context.itemKey,
          reason: rule.id || "timed_block_rule"
        });

        return blockRuleDecision(decision, "timed_block_rule", {
          ruleId: rule.id || "",
          untilSeconds: endSeconds
        });
      }
    }

    return decision;
  }

LINE 1742: return decision;
----- CONTEXT -----
        });
      }
    }

    return decision;
  }

  function applyFamilyLockRulesToDecision(playbackState, context, decision, rules = []) {
    if (!playbackState || !context || !decision || !Array.isArray(rules)) return decision;

    for (const rule of rules) {
      const family = rule.family || rule.group || rule.id || "";


LINE 1745: function applyFamilyLockRulesToDecision(playbackState, context, decision, rules = []) {
----- CONTEXT -----

    return decision;
  }

  function applyFamilyLockRulesToDecision(playbackState, context, decision, rules = []) {
    if (!playbackState || !context || !decision || !Array.isArray(rules)) return decision;

    for (const rule of rules) {
      const family = rule.family || rule.group || rule.id || "";

      if (!family) continue;

      applyFamilyLock(playbackState, context, decision, family);

LINE 1746: if (!playbackState || !context || !decision || !Array.isArray(rules)) return decision;
----- CONTEXT -----
    return decision;
  }

  function applyFamilyLockRulesToDecision(playbackState, context, decision, rules = []) {
    if (!playbackState || !context || !decision || !Array.isArray(rules)) return decision;

    for (const rule of rules) {
      const family = rule.family || rule.group || rule.id || "";

      if (!family) continue;

      applyFamilyLock(playbackState, context, decision, family);


LINE 1753: applyFamilyLock(playbackState, context, decision, family);
----- CONTEXT -----
      const family = rule.family || rule.group || rule.id || "";

      if (!family) continue;

      applyFamilyLock(playbackState, context, decision, family);

      if (decision.blocked) {
        addRuleDecisionReason(decision, "family_lock_rule", {
          ruleId: rule.id || "",
          family: normalizeRuleDecisionToken(family)
        });
        return decision;
      }

LINE 1755: if (decision.blocked) {
----- CONTEXT -----
      if (!family) continue;

      applyFamilyLock(playbackState, context, decision, family);

      if (decision.blocked) {
        addRuleDecisionReason(decision, "family_lock_rule", {
          ruleId: rule.id || "",
          family: normalizeRuleDecisionToken(family)
        });
        return decision;
      }
    }


LINE 1756: addRuleDecisionReason(decision, "family_lock_rule", {
----- CONTEXT -----

      applyFamilyLock(playbackState, context, decision, family);

      if (decision.blocked) {
        addRuleDecisionReason(decision, "family_lock_rule", {
          ruleId: rule.id || "",
          family: normalizeRuleDecisionToken(family)
        });
        return decision;
      }
    }

    return decision;

LINE 1758: family: normalizeRuleDecisionToken(family)
----- CONTEXT -----

      if (decision.blocked) {
        addRuleDecisionReason(decision, "family_lock_rule", {
          ruleId: rule.id || "",
          family: normalizeRuleDecisionToken(family)
        });
        return decision;
      }
    }

    return decision;
  }


LINE 1760: return decision;
----- CONTEXT -----
        addRuleDecisionReason(decision, "family_lock_rule", {
          ruleId: rule.id || "",
          family: normalizeRuleDecisionToken(family)
        });
        return decision;
      }
    }

    return decision;
  }

  function applyCutoffRulesForAllowedDecision(playbackState, context, profile = {}) {
    if (!playbackState || !context || !profile) return [];

LINE 1764: return decision;
----- CONTEXT -----
        return decision;
      }
    }

    return decision;
  }

  function applyCutoffRulesForAllowedDecision(playbackState, context, profile = {}) {
    if (!playbackState || !context || !profile) return [];

    const events = [];

    for (const rule of getCutoffRules(profile)) {

LINE 1767: function applyCutoffRulesForAllowedDecision(playbackState, context, profile = {}) {
----- CONTEXT -----

    return decision;
  }

  function applyCutoffRulesForAllowedDecision(playbackState, context, profile = {}) {
    if (!playbackState || !context || !profile) return [];

    const events = [];

    for (const rule of getCutoffRules(profile)) {
      const targets = getRuleTargets(rule);
      if (!targets.length) continue;


LINE 1782: cutTimeSeconds: context.startSeconds,
----- CONTEXT -----

      for (const target of targets) {
        const filter = getPlaybackFilterFromRuleTarget(target);
        const result = applyGenericCutoffAction(playbackState, {
          cutTimeSeconds: context.startSeconds,
          filter,
          includeFutureScheduled,
          fadeSeconds,
          reason: rule.id || "rule_profile_cutoff"
        });

        if (result.activeCutCount || result.futureCutCount || result.blockWindow) {
          events.push({

LINE 1803: function applyRuleProfileToDecision(playbackState, context, decision, profile = {}, random = null) {
----- CONTEXT -----
    }

    return events;
  }
  function applyRuleProfileToDecision(playbackState, context, decision, profile = {}, random = null) {
    applyActivationBlockWindows(playbackState, context, decision);
    if (decision.blocked) return decision;

    applyTimedBlockRulesToDecision(playbackState, context, decision, getTimedBlockRules(profile), random);
    if (decision.blocked) return decision;

    applyFamilyLockRulesToDecision(playbackState, context, decision, getFamilyLockRules(profile));
    if (decision.blocked) return decision;

LINE 1804: applyActivationBlockWindows(playbackState, context, decision);
----- CONTEXT -----

    return events;
  }
  function applyRuleProfileToDecision(playbackState, context, decision, profile = {}, random = null) {
    applyActivationBlockWindows(playbackState, context, decision);
    if (decision.blocked) return decision;

    applyTimedBlockRulesToDecision(playbackState, context, decision, getTimedBlockRules(profile), random);
    if (decision.blocked) return decision;

    applyFamilyLockRulesToDecision(playbackState, context, decision, getFamilyLockRules(profile));
    if (decision.blocked) return decision;


LINE 1805: if (decision.blocked) return decision;
----- CONTEXT -----
    return events;
  }
  function applyRuleProfileToDecision(playbackState, context, decision, profile = {}, random = null) {
    applyActivationBlockWindows(playbackState, context, decision);
    if (decision.blocked) return decision;

    applyTimedBlockRulesToDecision(playbackState, context, decision, getTimedBlockRules(profile), random);
    if (decision.blocked) return decision;

    applyFamilyLockRulesToDecision(playbackState, context, decision, getFamilyLockRules(profile));
    if (decision.blocked) return decision;

    applyDependencyRulesToDecision(playbackState, context, decision, getDependencyRules(profile));

LINE 1807: applyTimedBlockRulesToDecision(playbackState, context, decision, getTimedBlockRules(profile), random);
----- CONTEXT -----
  function applyRuleProfileToDecision(playbackState, context, decision, profile = {}, random = null) {
    applyActivationBlockWindows(playbackState, context, decision);
    if (decision.blocked) return decision;

    applyTimedBlockRulesToDecision(playbackState, context, decision, getTimedBlockRules(profile), random);
    if (decision.blocked) return decision;

    applyFamilyLockRulesToDecision(playbackState, context, decision, getFamilyLockRules(profile));
    if (decision.blocked) return decision;

    applyDependencyRulesToDecision(playbackState, context, decision, getDependencyRules(profile));
    if (decision.blocked) return decision;


LINE 1808: if (decision.blocked) return decision;
----- CONTEXT -----
    applyActivationBlockWindows(playbackState, context, decision);
    if (decision.blocked) return decision;

    applyTimedBlockRulesToDecision(playbackState, context, decision, getTimedBlockRules(profile), random);
    if (decision.blocked) return decision;

    applyFamilyLockRulesToDecision(playbackState, context, decision, getFamilyLockRules(profile));
    if (decision.blocked) return decision;

    applyDependencyRulesToDecision(playbackState, context, decision, getDependencyRules(profile));
    if (decision.blocked) return decision;

    applyHardClashRulesToDecision(playbackState, context, decision, getHardClashRules(profile));

LINE 1810: applyFamilyLockRulesToDecision(playbackState, context, decision, getFamilyLockRules(profile));
----- CONTEXT -----

    applyTimedBlockRulesToDecision(playbackState, context, decision, getTimedBlockRules(profile), random);
    if (decision.blocked) return decision;

    applyFamilyLockRulesToDecision(playbackState, context, decision, getFamilyLockRules(profile));
    if (decision.blocked) return decision;

    applyDependencyRulesToDecision(playbackState, context, decision, getDependencyRules(profile));
    if (decision.blocked) return decision;

    applyHardClashRulesToDecision(playbackState, context, decision, getHardClashRules(profile));
    if (decision.blocked) return decision;


LINE 1811: if (decision.blocked) return decision;
----- CONTEXT -----
    applyTimedBlockRulesToDecision(playbackState, context, decision, getTimedBlockRules(profile), random);
    if (decision.blocked) return decision;

    applyFamilyLockRulesToDecision(playbackState, context, decision, getFamilyLockRules(profile));
    if (decision.blocked) return decision;

    applyDependencyRulesToDecision(playbackState, context, decision, getDependencyRules(profile));
    if (decision.blocked) return decision;

    applyHardClashRulesToDecision(playbackState, context, decision, getHardClashRules(profile));
    if (decision.blocked) return decision;

    applySoftMultiplierRulesToDecision(playbackState, context, decision, getSoftMultiplierRules(profile));

LINE 1813: applyDependencyRulesToDecision(playbackState, context, decision, getDependencyRules(profile));
----- CONTEXT -----

    applyFamilyLockRulesToDecision(playbackState, context, decision, getFamilyLockRules(profile));
    if (decision.blocked) return decision;

    applyDependencyRulesToDecision(playbackState, context, decision, getDependencyRules(profile));
    if (decision.blocked) return decision;

    applyHardClashRulesToDecision(playbackState, context, decision, getHardClashRules(profile));
    if (decision.blocked) return decision;

    applySoftMultiplierRulesToDecision(playbackState, context, decision, getSoftMultiplierRules(profile));

    applyGroupedActivationRulesToDecision(

LINE 1814: if (decision.blocked) return decision;
----- CONTEXT -----
    applyFamilyLockRulesToDecision(playbackState, context, decision, getFamilyLockRules(profile));
    if (decision.blocked) return decision;

    applyDependencyRulesToDecision(playbackState, context, decision, getDependencyRules(profile));
    if (decision.blocked) return decision;

    applyHardClashRulesToDecision(playbackState, context, decision, getHardClashRules(profile));
    if (decision.blocked) return decision;

    applySoftMultiplierRulesToDecision(playbackState, context, decision, getSoftMultiplierRules(profile));

    applyGroupedActivationRulesToDecision(
      playbackState,

LINE 1816: applyHardClashRulesToDecision(playbackState, context, decision, getHardClashRules(profile));
----- CONTEXT -----

    applyDependencyRulesToDecision(playbackState, context, decision, getDependencyRules(profile));
    if (decision.blocked) return decision;

    applyHardClashRulesToDecision(playbackState, context, decision, getHardClashRules(profile));
    if (decision.blocked) return decision;

    applySoftMultiplierRulesToDecision(playbackState, context, decision, getSoftMultiplierRules(profile));

    applyGroupedActivationRulesToDecision(
      playbackState,
      context,
      decision,

LINE 1817: if (decision.blocked) return decision;
----- CONTEXT -----
    applyDependencyRulesToDecision(playbackState, context, decision, getDependencyRules(profile));
    if (decision.blocked) return decision;

    applyHardClashRulesToDecision(playbackState, context, decision, getHardClashRules(profile));
    if (decision.blocked) return decision;

    applySoftMultiplierRulesToDecision(playbackState, context, decision, getSoftMultiplierRules(profile));

    applyGroupedActivationRulesToDecision(
      playbackState,
      context,
      decision,
      getGroupedActivationRules(profile),

LINE 1819: applySoftMultiplierRulesToDecision(playbackState, context, decision, getSoftMultiplierRules(profile));
----- CONTEXT -----

    applyHardClashRulesToDecision(playbackState, context, decision, getHardClashRules(profile));
    if (decision.blocked) return decision;

    applySoftMultiplierRulesToDecision(playbackState, context, decision, getSoftMultiplierRules(profile));

    applyGroupedActivationRulesToDecision(
      playbackState,
      context,
      decision,
      getGroupedActivationRules(profile),
      random
    );

LINE 1821: applyGroupedActivationRulesToDecision(
----- CONTEXT -----
    if (decision.blocked) return decision;

    applySoftMultiplierRulesToDecision(playbackState, context, decision, getSoftMultiplierRules(profile));

    applyGroupedActivationRulesToDecision(
      playbackState,
      context,
      decision,
      getGroupedActivationRules(profile),
      random
    );
    if (decision.blocked) return decision;


LINE 1824: decision,
----- CONTEXT -----

    applyGroupedActivationRulesToDecision(
      playbackState,
      context,
      decision,
      getGroupedActivationRules(profile),
      random
    );
    if (decision.blocked) return decision;

    applyDensityRulesToDecision(context, decision, profile);
    applyTensionRulesToDecision(context, decision, profile);
    applyCrescendoRulesToDecision(context, decision, profile);

LINE 1828: if (decision.blocked) return decision;
----- CONTEXT -----
      decision,
      getGroupedActivationRules(profile),
      random
    );
    if (decision.blocked) return decision;

    applyDensityRulesToDecision(context, decision, profile);
    applyTensionRulesToDecision(context, decision, profile);
    applyCrescendoRulesToDecision(context, decision, profile);

    return decision;
  }
  function resolveRuleProfileDecision({

LINE 1830: applyDensityRulesToDecision(context, decision, profile);
----- CONTEXT -----
      random
    );
    if (decision.blocked) return decision;

    applyDensityRulesToDecision(context, decision, profile);
    applyTensionRulesToDecision(context, decision, profile);
    applyCrescendoRulesToDecision(context, decision, profile);

    return decision;
  }
  function resolveRuleProfileDecision({
    random,
    plan = null,

LINE 1831: applyTensionRulesToDecision(context, decision, profile);
----- CONTEXT -----
    );
    if (decision.blocked) return decision;

    applyDensityRulesToDecision(context, decision, profile);
    applyTensionRulesToDecision(context, decision, profile);
    applyCrescendoRulesToDecision(context, decision, profile);

    return decision;
  }
  function resolveRuleProfileDecision({
    random,
    plan = null,
    playbackState = null,

LINE 1832: applyCrescendoRulesToDecision(context, decision, profile);
----- CONTEXT -----
    if (decision.blocked) return decision;

    applyDensityRulesToDecision(context, decision, profile);
    applyTensionRulesToDecision(context, decision, profile);
    applyCrescendoRulesToDecision(context, decision, profile);

    return decision;
  }
  function resolveRuleProfileDecision({
    random,
    plan = null,
    playbackState = null,
    kind = "audio",

LINE 1834: return decision;
----- CONTEXT -----
    applyDensityRulesToDecision(context, decision, profile);
    applyTensionRulesToDecision(context, decision, profile);
    applyCrescendoRulesToDecision(context, decision, profile);

    return decision;
  }
  function resolveRuleProfileDecision({
    random,
    plan = null,
    playbackState = null,
    kind = "audio",
    key = "",
    entry = null,

LINE 1836: function resolveRuleProfileDecision({
----- CONTEXT -----
    applyCrescendoRulesToDecision(context, decision, profile);

    return decision;
  }
  function resolveRuleProfileDecision({
    random,
    plan = null,
    playbackState = null,
    kind = "audio",
    key = "",
    entry = null,
    pattern = null,
    section = null,

LINE 1847: startSeconds = null,
----- CONTEXT -----
    pattern = null,
    section = null,
    lifecycleStates = null,
    localBarIndex = null,
    startSeconds = null,
    baseChance = 1,
    profile = null
  } = {}) {
    const safeRandom = typeof random === "function" ? random : (() => 1);

    const activeProfile = profile || (
      kind === "midi"
        ? getRuleProfileForMidiPattern(pattern)

LINE 1859: const context = createRuleDecisionContext({
----- CONTEXT -----
        ? getRuleProfileForMidiPattern(pattern)
        : getRuleProfileForEntry(entry)
    );

    const context = createRuleDecisionContext({
      kind,
      key,
      entry,
      pattern,
      section,
      lifecycleStates,
      localBarIndex,
      startSeconds,

LINE 1867: startSeconds,
----- CONTEXT -----
      pattern,
      section,
      lifecycleStates,
      localBarIndex,
      startSeconds,
      baseChance
    });

    const decision = createRuleDecisionResult(context, {
      baseChance
    });

    applyLifecycleDropoutToDecision(safeRandom, lifecycleStates, context, decision, activeProfile);

LINE 1871: const decision = createRuleDecisionResult(context, {
----- CONTEXT -----
      startSeconds,
      baseChance
    });

    const decision = createRuleDecisionResult(context, {
      baseChance
    });

    applyLifecycleDropoutToDecision(safeRandom, lifecycleStates, context, decision, activeProfile);
    if (decision.droppedOut) {
      recordRuleDecisionDebug(plan, decision);

      return {

LINE 1875: applyLifecycleDropoutToDecision(safeRandom, lifecycleStates, context, decision, activeProfile);
----- CONTEXT -----
    const decision = createRuleDecisionResult(context, {
      baseChance
    });

    applyLifecycleDropoutToDecision(safeRandom, lifecycleStates, context, decision, activeProfile);
    if (decision.droppedOut) {
      recordRuleDecisionDebug(plan, decision);

      return {
        allowed: false,
        context,
        decision,
        profile: activeProfile

LINE 1876: if (decision.droppedOut) {
----- CONTEXT -----
      baseChance
    });

    applyLifecycleDropoutToDecision(safeRandom, lifecycleStates, context, decision, activeProfile);
    if (decision.droppedOut) {
      recordRuleDecisionDebug(plan, decision);

      return {
        allowed: false,
        context,
        decision,
        profile: activeProfile
      };

LINE 1877: recordRuleDecisionDebug(plan, decision);
----- CONTEXT -----
    });

    applyLifecycleDropoutToDecision(safeRandom, lifecycleStates, context, decision, activeProfile);
    if (decision.droppedOut) {
      recordRuleDecisionDebug(plan, decision);

      return {
        allowed: false,
        context,
        decision,
        profile: activeProfile
      };
    }

LINE 1880: allowed: false,
----- CONTEXT -----
    if (decision.droppedOut) {
      recordRuleDecisionDebug(plan, decision);

      return {
        allowed: false,
        context,
        decision,
        profile: activeProfile
      };
    }

    applyRuleProfileToDecision(playbackState, context, decision, activeProfile, safeRandom);


LINE 1882: decision,
----- CONTEXT -----

      return {
        allowed: false,
        context,
        decision,
        profile: activeProfile
      };
    }

    applyRuleProfileToDecision(playbackState, context, decision, activeProfile, safeRandom);

    const allowed = finalizeRuleDecision(safeRandom, decision);
    recordRuleDecisionDebug(plan, decision);

LINE 1887: applyRuleProfileToDecision(playbackState, context, decision, activeProfile, safeRandom);
----- CONTEXT -----
        profile: activeProfile
      };
    }

    applyRuleProfileToDecision(playbackState, context, decision, activeProfile, safeRandom);

    const allowed = finalizeRuleDecision(safeRandom, decision);
    recordRuleDecisionDebug(plan, decision);

    return {
      allowed,
      context,
      decision,

LINE 1889: const allowed = finalizeRuleDecision(safeRandom, decision);
----- CONTEXT -----
    }

    applyRuleProfileToDecision(playbackState, context, decision, activeProfile, safeRandom);

    const allowed = finalizeRuleDecision(safeRandom, decision);
    recordRuleDecisionDebug(plan, decision);

    return {
      allowed,
      context,
      decision,
      profile: activeProfile
    };

LINE 1890: recordRuleDecisionDebug(plan, decision);
----- CONTEXT -----

    applyRuleProfileToDecision(playbackState, context, decision, activeProfile, safeRandom);

    const allowed = finalizeRuleDecision(safeRandom, decision);
    recordRuleDecisionDebug(plan, decision);

    return {
      allowed,
      context,
      decision,
      profile: activeProfile
    };
  }

LINE 1893: allowed,
----- CONTEXT -----
    const allowed = finalizeRuleDecision(safeRandom, decision);
    recordRuleDecisionDebug(plan, decision);

    return {
      allowed,
      context,
      decision,
      profile: activeProfile
    };
  }
  function createGlobalInclusionState() {
    return {
      decisions: new Map(),

LINE 1895: decision,
----- CONTEXT -----

    return {
      allowed,
      context,
      decision,
      profile: activeProfile
    };
  }
  function createGlobalInclusionState() {
    return {
      decisions: new Map(),
      debug: []
    };

LINE 1901: decisions: new Map(),
----- CONTEXT -----
    };
  }
  function createGlobalInclusionState() {
    return {
      decisions: new Map(),
      debug: []
    };
  }

  function getGlobalInclusionId(kind, key) {
    return `${normalizeRuleDecisionToken(kind)}:${String(key || "")}`;
  }


LINE 1907: return `${normalizeRuleDecisionToken(kind)}:${String(key || "")}`;
----- CONTEXT -----
    };
  }

  function getGlobalInclusionId(kind, key) {
    return `${normalizeRuleDecisionToken(kind)}:${String(key || "")}`;
  }

  function hasGlobalInclusionDecision(globalInclusionState, kind, key) {
    if (!globalInclusionState) return false;
    return globalInclusionState.decisions.has(getGlobalInclusionId(kind, key));
  }

  function getGlobalInclusionDecision(globalInclusionState, kind, key) {

LINE 1910: function hasGlobalInclusionDecision(globalInclusionState, kind, key) {
----- CONTEXT -----
  function getGlobalInclusionId(kind, key) {
    return `${normalizeRuleDecisionToken(kind)}:${String(key || "")}`;
  }

  function hasGlobalInclusionDecision(globalInclusionState, kind, key) {
    if (!globalInclusionState) return false;
    return globalInclusionState.decisions.has(getGlobalInclusionId(kind, key));
  }

  function getGlobalInclusionDecision(globalInclusionState, kind, key) {
    if (!globalInclusionState) return null;
    return globalInclusionState.decisions.get(getGlobalInclusionId(kind, key)) || null;
  }

LINE 1912: return globalInclusionState.decisions.has(getGlobalInclusionId(kind, key));
----- CONTEXT -----
  }

  function hasGlobalInclusionDecision(globalInclusionState, kind, key) {
    if (!globalInclusionState) return false;
    return globalInclusionState.decisions.has(getGlobalInclusionId(kind, key));
  }

  function getGlobalInclusionDecision(globalInclusionState, kind, key) {
    if (!globalInclusionState) return null;
    return globalInclusionState.decisions.get(getGlobalInclusionId(kind, key)) || null;
  }

  function setGlobalInclusionDecision(globalInclusionState, decision) {

LINE 1915: function getGlobalInclusionDecision(globalInclusionState, kind, key) {
----- CONTEXT -----
    if (!globalInclusionState) return false;
    return globalInclusionState.decisions.has(getGlobalInclusionId(kind, key));
  }

  function getGlobalInclusionDecision(globalInclusionState, kind, key) {
    if (!globalInclusionState) return null;
    return globalInclusionState.decisions.get(getGlobalInclusionId(kind, key)) || null;
  }

  function setGlobalInclusionDecision(globalInclusionState, decision) {
    if (!globalInclusionState || !decision) return decision;

    const id = decision.id || getGlobalInclusionId(decision.kind, decision.key);

LINE 1917: return globalInclusionState.decisions.get(getGlobalInclusionId(kind, key)) || null;
----- CONTEXT -----
  }

  function getGlobalInclusionDecision(globalInclusionState, kind, key) {
    if (!globalInclusionState) return null;
    return globalInclusionState.decisions.get(getGlobalInclusionId(kind, key)) || null;
  }

  function setGlobalInclusionDecision(globalInclusionState, decision) {
    if (!globalInclusionState || !decision) return decision;

    const id = decision.id || getGlobalInclusionId(decision.kind, decision.key);
    const storedDecision = {
      ...decision,

LINE 1920: function setGlobalInclusionDecision(globalInclusionState, decision) {
----- CONTEXT -----
    if (!globalInclusionState) return null;
    return globalInclusionState.decisions.get(getGlobalInclusionId(kind, key)) || null;
  }

  function setGlobalInclusionDecision(globalInclusionState, decision) {
    if (!globalInclusionState || !decision) return decision;

    const id = decision.id || getGlobalInclusionId(decision.kind, decision.key);
    const storedDecision = {
      ...decision,
      id
    };


LINE 1921: if (!globalInclusionState || !decision) return decision;
----- CONTEXT -----
    return globalInclusionState.decisions.get(getGlobalInclusionId(kind, key)) || null;
  }

  function setGlobalInclusionDecision(globalInclusionState, decision) {
    if (!globalInclusionState || !decision) return decision;

    const id = decision.id || getGlobalInclusionId(decision.kind, decision.key);
    const storedDecision = {
      ...decision,
      id
    };

    globalInclusionState.decisions.set(id, storedDecision);

LINE 1923: const id = decision.id || getGlobalInclusionId(decision.kind, decision.key);
----- CONTEXT -----

  function setGlobalInclusionDecision(globalInclusionState, decision) {
    if (!globalInclusionState || !decision) return decision;

    const id = decision.id || getGlobalInclusionId(decision.kind, decision.key);
    const storedDecision = {
      ...decision,
      id
    };

    globalInclusionState.decisions.set(id, storedDecision);
    globalInclusionState.debug.push({ ...storedDecision });


LINE 1924: const storedDecision = {
----- CONTEXT -----
  function setGlobalInclusionDecision(globalInclusionState, decision) {
    if (!globalInclusionState || !decision) return decision;

    const id = decision.id || getGlobalInclusionId(decision.kind, decision.key);
    const storedDecision = {
      ...decision,
      id
    };

    globalInclusionState.decisions.set(id, storedDecision);
    globalInclusionState.debug.push({ ...storedDecision });

    return storedDecision;

LINE 1925: ...decision,
----- CONTEXT -----
    if (!globalInclusionState || !decision) return decision;

    const id = decision.id || getGlobalInclusionId(decision.kind, decision.key);
    const storedDecision = {
      ...decision,
      id
    };

    globalInclusionState.decisions.set(id, storedDecision);
    globalInclusionState.debug.push({ ...storedDecision });

    return storedDecision;
  }

LINE 1929: globalInclusionState.decisions.set(id, storedDecision);
----- CONTEXT -----
      ...decision,
      id
    };

    globalInclusionState.decisions.set(id, storedDecision);
    globalInclusionState.debug.push({ ...storedDecision });

    return storedDecision;
  }

  function resolveGlobalInclusionDecision({
    random,
    globalInclusionState,

LINE 1930: globalInclusionState.debug.push({ ...storedDecision });
----- CONTEXT -----
      id
    };

    globalInclusionState.decisions.set(id, storedDecision);
    globalInclusionState.debug.push({ ...storedDecision });

    return storedDecision;
  }

  function resolveGlobalInclusionDecision({
    random,
    globalInclusionState,
    kind = "audio",

LINE 1932: return storedDecision;
----- CONTEXT -----

    globalInclusionState.decisions.set(id, storedDecision);
    globalInclusionState.debug.push({ ...storedDecision });

    return storedDecision;
  }

  function resolveGlobalInclusionDecision({
    random,
    globalInclusionState,
    kind = "audio",
    key = "",
    entry = null,

LINE 1935: function resolveGlobalInclusionDecision({
----- CONTEXT -----

    return storedDecision;
  }

  function resolveGlobalInclusionDecision({
    random,
    globalInclusionState,
    kind = "audio",
    key = "",
    entry = null,
    pattern = null,
    profile = null,
    fallbackChance = 0

LINE 1945: const itemKey = getRuleDecisionItemKey({ key, entry, pattern });
----- CONTEXT -----
    pattern = null,
    profile = null,
    fallbackChance = 0
  } = {}) {
    const itemKey = getRuleDecisionItemKey({ key, entry, pattern });
    const id = getGlobalInclusionId(kind, itemKey);

    if (globalInclusionState?.decisions?.has(id)) {
      return globalInclusionState.decisions.get(id);
    }

    const safeRandom = typeof random === "function" ? random : (() => 1);
    const activeProfile = profile || (

LINE 1948: if (globalInclusionState?.decisions?.has(id)) {
----- CONTEXT -----
  } = {}) {
    const itemKey = getRuleDecisionItemKey({ key, entry, pattern });
    const id = getGlobalInclusionId(kind, itemKey);

    if (globalInclusionState?.decisions?.has(id)) {
      return globalInclusionState.decisions.get(id);
    }

    const safeRandom = typeof random === "function" ? random : (() => 1);
    const activeProfile = profile || (
      kind === "midi"
        ? getRuleProfileForMidiPattern(pattern)
        : getRuleProfileForEntry(entry)

LINE 1949: return globalInclusionState.decisions.get(id);
----- CONTEXT -----
    const itemKey = getRuleDecisionItemKey({ key, entry, pattern });
    const id = getGlobalInclusionId(kind, itemKey);

    if (globalInclusionState?.decisions?.has(id)) {
      return globalInclusionState.decisions.get(id);
    }

    const safeRandom = typeof random === "function" ? random : (() => 1);
    const activeProfile = profile || (
      kind === "midi"
        ? getRuleProfileForMidiPattern(pattern)
        : getRuleProfileForEntry(entry)
    );

LINE 1963: return setGlobalInclusionDecision(globalInclusionState, {
----- CONTEXT -----
    const globalChance = getGlobalInclusionChance(activeProfile, fallbackChance);
    const roll = safeRandom();
    const included = roll < globalChance;

    return setGlobalInclusionDecision(globalInclusionState, {
      id,
      kind,
      key: itemKey,
      included,
      globalChance,
      roll,
      profileSummary: {
        family: entry?.family || pattern?.id || "",

LINE 1992: return `${normalizeRuleDecisionToken(kind)}:${String(key || "")}`;
----- CONTEXT -----
    };
  }

  function getRequiredActivationId(kind, key) {
    return `${normalizeRuleDecisionToken(kind)}:${String(key || "")}`;
  }

  function addRequiredActivationObligation(requiredActivationState, {
    kind = "audio",
    key = "",
    family = "",
    tag = "",
    reason = "",

LINE 2003: allowedSectionTypes = [],
----- CONTEXT -----
    tag = "",
    reason = "",
    minActivations = 1,
    maxActivations = null,
    allowedSectionTypes = [],
    forbiddenSectionTypes = []
  } = {}) {
    if (!requiredActivationState) return null;

    const id = getRequiredActivationId(kind, key || family || tag);

    const obligation = {
      id,

LINE 2012: kind: normalizeRuleDecisionToken(kind),
----- CONTEXT -----
    const id = getRequiredActivationId(kind, key || family || tag);

    const obligation = {
      id,
      kind: normalizeRuleDecisionToken(kind),
      key: String(key || ""),
      family: normalizeRuleDecisionToken(family),
      tag: normalizeRuleDecisionToken(tag),
      reason: String(reason || "required_activation"),
      minActivations: Math.max(0, Number(minActivations) || 0),
      maxActivations: Number.isFinite(Number(maxActivations)) ? Math.max(0, Number(maxActivations)) : null,
      activationCount: 0,
      allowedSectionTypes: Array.isArray(allowedSectionTypes)

LINE 2014: family: normalizeRuleDecisionToken(family),
----- CONTEXT -----
    const obligation = {
      id,
      kind: normalizeRuleDecisionToken(kind),
      key: String(key || ""),
      family: normalizeRuleDecisionToken(family),
      tag: normalizeRuleDecisionToken(tag),
      reason: String(reason || "required_activation"),
      minActivations: Math.max(0, Number(minActivations) || 0),
      maxActivations: Number.isFinite(Number(maxActivations)) ? Math.max(0, Number(maxActivations)) : null,
      activationCount: 0,
      allowedSectionTypes: Array.isArray(allowedSectionTypes)
        ? allowedSectionTypes.map(type => normalizeRuleDecisionToken(type)).filter(Boolean)
        : [],

LINE 2015: tag: normalizeRuleDecisionToken(tag),
----- CONTEXT -----
      id,
      kind: normalizeRuleDecisionToken(kind),
      key: String(key || ""),
      family: normalizeRuleDecisionToken(family),
      tag: normalizeRuleDecisionToken(tag),
      reason: String(reason || "required_activation"),
      minActivations: Math.max(0, Number(minActivations) || 0),
      maxActivations: Number.isFinite(Number(maxActivations)) ? Math.max(0, Number(maxActivations)) : null,
      activationCount: 0,
      allowedSectionTypes: Array.isArray(allowedSectionTypes)
        ? allowedSectionTypes.map(type => normalizeRuleDecisionToken(type)).filter(Boolean)
        : [],
      forbiddenSectionTypes: Array.isArray(forbiddenSectionTypes)

LINE 2020: allowedSectionTypes: Array.isArray(allowedSectionTypes)
----- CONTEXT -----
      reason: String(reason || "required_activation"),
      minActivations: Math.max(0, Number(minActivations) || 0),
      maxActivations: Number.isFinite(Number(maxActivations)) ? Math.max(0, Number(maxActivations)) : null,
      activationCount: 0,
      allowedSectionTypes: Array.isArray(allowedSectionTypes)
        ? allowedSectionTypes.map(type => normalizeRuleDecisionToken(type)).filter(Boolean)
        : [],
      forbiddenSectionTypes: Array.isArray(forbiddenSectionTypes)
        ? forbiddenSectionTypes.map(type => normalizeRuleDecisionToken(type)).filter(Boolean)
        : [],
      fulfilled: false
    };


LINE 2021: ? allowedSectionTypes.map(type => normalizeRuleDecisionToken(type)).filter(Boolean)
----- CONTEXT -----
      minActivations: Math.max(0, Number(minActivations) || 0),
      maxActivations: Number.isFinite(Number(maxActivations)) ? Math.max(0, Number(maxActivations)) : null,
      activationCount: 0,
      allowedSectionTypes: Array.isArray(allowedSectionTypes)
        ? allowedSectionTypes.map(type => normalizeRuleDecisionToken(type)).filter(Boolean)
        : [],
      forbiddenSectionTypes: Array.isArray(forbiddenSectionTypes)
        ? forbiddenSectionTypes.map(type => normalizeRuleDecisionToken(type)).filter(Boolean)
        : [],
      fulfilled: false
    };

    requiredActivationState.obligations.set(id, obligation);

LINE 2024: ? forbiddenSectionTypes.map(type => normalizeRuleDecisionToken(type)).filter(Boolean)
----- CONTEXT -----
      allowedSectionTypes: Array.isArray(allowedSectionTypes)
        ? allowedSectionTypes.map(type => normalizeRuleDecisionToken(type)).filter(Boolean)
        : [],
      forbiddenSectionTypes: Array.isArray(forbiddenSectionTypes)
        ? forbiddenSectionTypes.map(type => normalizeRuleDecisionToken(type)).filter(Boolean)
        : [],
      fulfilled: false
    };

    requiredActivationState.obligations.set(id, obligation);
    requiredActivationState.debug.push({
      action: "add",
      ...obligation

LINE 2041: const sectionType = normalizeRuleDecisionToken(context.sectionType);
----- CONTEXT -----

  function getMatchingRequiredActivationObligations(requiredActivationState, context) {
    if (!requiredActivationState || !context) return [];

    const sectionType = normalizeRuleDecisionToken(context.sectionType);
    const itemTags = context.itemTags || new Set();

    return [...requiredActivationState.obligations.values()].filter(obligation => {
      if (obligation.fulfilled) return false;
      if (obligation.kind && obligation.kind !== normalizeRuleDecisionToken(context.kind)) return false;
      if (obligation.key && obligation.key !== context.itemKey) return false;
      if (obligation.family) {
        const familyTag = `family:${obligation.family}`;

LINE 2046: if (obligation.kind && obligation.kind !== normalizeRuleDecisionToken(context.kind)) return false;
----- CONTEXT -----
    const itemTags = context.itemTags || new Set();

    return [...requiredActivationState.obligations.values()].filter(obligation => {
      if (obligation.fulfilled) return false;
      if (obligation.kind && obligation.kind !== normalizeRuleDecisionToken(context.kind)) return false;
      if (obligation.key && obligation.key !== context.itemKey) return false;
      if (obligation.family) {
        const familyTag = `family:${obligation.family}`;
        if (!itemTags.has(familyTag) && !itemTags.has(obligation.family)) return false;
      }
      if (obligation.tag && !itemTags.has(obligation.tag)) return false;
      if (obligation.allowedSectionTypes.length && !obligation.allowedSectionTypes.includes(sectionType)) return false;
      if (obligation.forbiddenSectionTypes.includes(sectionType)) return false;

LINE 2053: if (obligation.allowedSectionTypes.length && !obligation.allowedSectionTypes.includes(sectionType)) return false;
----- CONTEXT -----
        const familyTag = `family:${obligation.family}`;
        if (!itemTags.has(familyTag) && !itemTags.has(obligation.family)) return false;
      }
      if (obligation.tag && !itemTags.has(obligation.tag)) return false;
      if (obligation.allowedSectionTypes.length && !obligation.allowedSectionTypes.includes(sectionType)) return false;
      if (obligation.forbiddenSectionTypes.includes(sectionType)) return false;

      return true;
    });
  }

  function markRequiredActivationSatisfied(requiredActivationState, context) {
    const obligations = getMatchingRequiredActivationObligations(requiredActivationState, context);

LINE 2119: const itemKey = getRuleDecisionItemKey({ key, entry, pattern });
----- CONTEXT -----
    entry = null,
    pattern = null
  } = {}) {
    const target = normalizeRuleTargetFilter(rule?.target || rule || {});
    const itemKey = getRuleDecisionItemKey({ key, entry, pattern });

    return {
      kind: normalizeRuleDecisionToken(rule?.kind || target.kind || kind),
      key: String(rule?.key || rule?.file || target.key || itemKey || ""),
      family: normalizeRuleDecisionToken(rule?.family || rule?.group || target.family || entry?.family || pattern?.id || ""),
      tag: normalizeRuleDecisionToken(rule?.tag || target.tag || ""),
      reason: String(rule?.reason || rule?.id || "required_activation_rule"),
      minActivations: Math.max(1, Number(rule?.minActivations ?? rule?.minimum ?? 1) || 1),

LINE 2122: kind: normalizeRuleDecisionToken(rule?.kind || target.kind || kind),
----- CONTEXT -----
    const target = normalizeRuleTargetFilter(rule?.target || rule || {});
    const itemKey = getRuleDecisionItemKey({ key, entry, pattern });

    return {
      kind: normalizeRuleDecisionToken(rule?.kind || target.kind || kind),
      key: String(rule?.key || rule?.file || target.key || itemKey || ""),
      family: normalizeRuleDecisionToken(rule?.family || rule?.group || target.family || entry?.family || pattern?.id || ""),
      tag: normalizeRuleDecisionToken(rule?.tag || target.tag || ""),
      reason: String(rule?.reason || rule?.id || "required_activation_rule"),
      minActivations: Math.max(1, Number(rule?.minActivations ?? rule?.minimum ?? 1) || 1),
      maxActivations: Number.isFinite(Number(rule?.maxActivations ?? rule?.maximum))
        ? Math.max(0, Number(rule.maxActivations ?? rule.maximum))
        : null,

LINE 2124: family: normalizeRuleDecisionToken(rule?.family || rule?.group || target.family || entry?.family || pattern?.id || ""),
----- CONTEXT -----

    return {
      kind: normalizeRuleDecisionToken(rule?.kind || target.kind || kind),
      key: String(rule?.key || rule?.file || target.key || itemKey || ""),
      family: normalizeRuleDecisionToken(rule?.family || rule?.group || target.family || entry?.family || pattern?.id || ""),
      tag: normalizeRuleDecisionToken(rule?.tag || target.tag || ""),
      reason: String(rule?.reason || rule?.id || "required_activation_rule"),
      minActivations: Math.max(1, Number(rule?.minActivations ?? rule?.minimum ?? 1) || 1),
      maxActivations: Number.isFinite(Number(rule?.maxActivations ?? rule?.maximum))
        ? Math.max(0, Number(rule.maxActivations ?? rule.maximum))
        : null,
      allowedSectionTypes: Array.isArray(rule?.allowedSectionTypes || rule?.allowedSections)
        ? (rule.allowedSectionTypes || rule.allowedSections)

LINE 2125: tag: normalizeRuleDecisionToken(rule?.tag || target.tag || ""),
----- CONTEXT -----
    return {
      kind: normalizeRuleDecisionToken(rule?.kind || target.kind || kind),
      key: String(rule?.key || rule?.file || target.key || itemKey || ""),
      family: normalizeRuleDecisionToken(rule?.family || rule?.group || target.family || entry?.family || pattern?.id || ""),
      tag: normalizeRuleDecisionToken(rule?.tag || target.tag || ""),
      reason: String(rule?.reason || rule?.id || "required_activation_rule"),
      minActivations: Math.max(1, Number(rule?.minActivations ?? rule?.minimum ?? 1) || 1),
      maxActivations: Number.isFinite(Number(rule?.maxActivations ?? rule?.maximum))
        ? Math.max(0, Number(rule.maxActivations ?? rule.maximum))
        : null,
      allowedSectionTypes: Array.isArray(rule?.allowedSectionTypes || rule?.allowedSections)
        ? (rule.allowedSectionTypes || rule.allowedSections)
        : [],

LINE 2131: allowedSectionTypes: Array.isArray(rule?.allowedSectionTypes || rule?.allowedSections)
----- CONTEXT -----
      minActivations: Math.max(1, Number(rule?.minActivations ?? rule?.minimum ?? 1) || 1),
      maxActivations: Number.isFinite(Number(rule?.maxActivations ?? rule?.maximum))
        ? Math.max(0, Number(rule.maxActivations ?? rule.maximum))
        : null,
      allowedSectionTypes: Array.isArray(rule?.allowedSectionTypes || rule?.allowedSections)
        ? (rule.allowedSectionTypes || rule.allowedSections)
        : [],
      forbiddenSectionTypes: Array.isArray(rule?.forbiddenSectionTypes || rule?.forbiddenSections)
        ? (rule.forbiddenSectionTypes || rule.forbiddenSections)
        : []
    };
  }


LINE 2132: ? (rule.allowedSectionTypes || rule.allowedSections)
----- CONTEXT -----
      maxActivations: Number.isFinite(Number(rule?.maxActivations ?? rule?.maximum))
        ? Math.max(0, Number(rule.maxActivations ?? rule.maximum))
        : null,
      allowedSectionTypes: Array.isArray(rule?.allowedSectionTypes || rule?.allowedSections)
        ? (rule.allowedSectionTypes || rule.allowedSections)
        : [],
      forbiddenSectionTypes: Array.isArray(rule?.forbiddenSectionTypes || rule?.forbiddenSections)
        ? (rule.forbiddenSectionTypes || rule.forbiddenSections)
        : []
    };
  }

  function addRequiredActivationRulesFromProfile(requiredActivationState, {

LINE 2175: function applyLifecycleDropoutToDecision(random, lifecycleStates, context, decision, profile = {}) {
----- CONTEXT -----
    }

    return obligations;
  }
  function applyLifecycleDropoutToDecision(random, lifecycleStates, context, decision, profile = {}) {
    if (!lifecycleStates || !context || !decision || !context.lifecycleId) {
      return decision;
    }

    const dropoutConfig = getDropoutRuleConfig(profile);

    if (dropoutConfig.baseChance <= 0 && dropoutConfig.increasePerActivation <= 0) {
      return decision;

LINE 2176: if (!lifecycleStates || !context || !decision || !context.lifecycleId) {
----- CONTEXT -----

    return obligations;
  }
  function applyLifecycleDropoutToDecision(random, lifecycleStates, context, decision, profile = {}) {
    if (!lifecycleStates || !context || !decision || !context.lifecycleId) {
      return decision;
    }

    const dropoutConfig = getDropoutRuleConfig(profile);

    if (dropoutConfig.baseChance <= 0 && dropoutConfig.increasePerActivation <= 0) {
      return decision;
    }

LINE 2177: return decision;
----- CONTEXT -----
    return obligations;
  }
  function applyLifecycleDropoutToDecision(random, lifecycleStates, context, decision, profile = {}) {
    if (!lifecycleStates || !context || !decision || !context.lifecycleId) {
      return decision;
    }

    const dropoutConfig = getDropoutRuleConfig(profile);

    if (dropoutConfig.baseChance <= 0 && dropoutConfig.increasePerActivation <= 0) {
      return decision;
    }


LINE 2183: return decision;
----- CONTEXT -----

    const dropoutConfig = getDropoutRuleConfig(profile);

    if (dropoutConfig.baseChance <= 0 && dropoutConfig.increasePerActivation <= 0) {
      return decision;
    }

    const state = getLifecycleState(lifecycleStates, context.lifecycleId);

    if (!state.activated) {
      return decision;
    }


LINE 2189: return decision;
----- CONTEXT -----

    const state = getLifecycleState(lifecycleStates, context.lifecycleId);

    if (!state.activated) {
      return decision;
    }

    const dropoutChance = calculateLifecycleDropoutChance(state, dropoutConfig);

    if (chance(random, dropoutChance)) {
      dropoutLifecycleItem(lifecycleStates, context.lifecycleId);

      return markRuleDecisionDropout(decision, "lifecycle_dropout", {

LINE 2197: return markRuleDecisionDropout(decision, "lifecycle_dropout", {
----- CONTEXT -----

    if (chance(random, dropoutChance)) {
      dropoutLifecycleItem(lifecycleStates, context.lifecycleId);

      return markRuleDecisionDropout(decision, "lifecycle_dropout", {
        lifecycleId: context.lifecycleId,
        dropoutChance,
        activationCount: state.activationCount,
        dropoutCount: state.dropoutCount
      });
    }

    addRuleDecisionReason(decision, "lifecycle_dropout_survived", {

LINE 2205: addRuleDecisionReason(decision, "lifecycle_dropout_survived", {
----- CONTEXT -----
        dropoutCount: state.dropoutCount
      });
    }

    addRuleDecisionReason(decision, "lifecycle_dropout_survived", {
      lifecycleId: context.lifecycleId,
      dropoutChance,
      activationCount: state.activationCount,
      dropoutCount: state.dropoutCount
    });

    return decision;
  }

LINE 2212: return decision;
----- CONTEXT -----
      activationCount: state.activationCount,
      dropoutCount: state.dropoutCount
    });

    return decision;
  }
  function getSectionEnergyContext(section = {}) {
    const densityScore = Number(section.densityScore ?? section.density ?? 0);

    return {
      densityScore,
      densityBand: String(section.densityBand || section.densityLevel || getDensityBandFromScore(densityScore)),
      tensionValue: Number(section.tensionValue ?? section.tension ?? 0),

LINE 2271: function applyEnergyRulesToDecision(context, decision, rules = [], reasonCode = "energy_multiplier") {
----- CONTEXT -----
    const multiplier = Number(rule.multiplier ?? rule.chanceMultiplier ?? rule.activationMultiplier ?? 1);

    return Number.isFinite(multiplier) ? Math.max(0, multiplier) : 1;
  }
  function applyEnergyRulesToDecision(context, decision, rules = [], reasonCode = "energy_multiplier") {
    if (!context || !decision || !Array.isArray(rules)) return decision;

    const energyContext = getSectionEnergyContext(context.section);

    for (const rule of rules) {
      const multiplier = getEnergyRuleMultiplier(rule, energyContext);

      if (multiplier !== 1) {

LINE 2272: if (!context || !decision || !Array.isArray(rules)) return decision;
----- CONTEXT -----

    return Number.isFinite(multiplier) ? Math.max(0, multiplier) : 1;
  }
  function applyEnergyRulesToDecision(context, decision, rules = [], reasonCode = "energy_multiplier") {
    if (!context || !decision || !Array.isArray(rules)) return decision;

    const energyContext = getSectionEnergyContext(context.section);

    for (const rule of rules) {
      const multiplier = getEnergyRuleMultiplier(rule, energyContext);

      if (multiplier !== 1) {
        multiplyRuleDecisionChance(decision, multiplier, reasonCode, {

LINE 2280: multiplyRuleDecisionChance(decision, multiplier, reasonCode, {
----- CONTEXT -----
    for (const rule of rules) {
      const multiplier = getEnergyRuleMultiplier(rule, energyContext);

      if (multiplier !== 1) {
        multiplyRuleDecisionChance(decision, multiplier, reasonCode, {
          ruleId: rule.id || "",
          energyContext
        });
      }
    }

    return decision;
  }

LINE 2287: return decision;
----- CONTEXT -----
        });
      }
    }

    return decision;
  }

  function applyDensityRulesToDecision(context, decision, profile = {}) {
    return applyEnergyRulesToDecision(
      context,
      decision,
      getDensityRules(profile),
      "density_multiplier"

LINE 2290: function applyDensityRulesToDecision(context, decision, profile = {}) {
----- CONTEXT -----

    return decision;
  }

  function applyDensityRulesToDecision(context, decision, profile = {}) {
    return applyEnergyRulesToDecision(
      context,
      decision,
      getDensityRules(profile),
      "density_multiplier"
    );
  }


LINE 2291: return applyEnergyRulesToDecision(
----- CONTEXT -----
    return decision;
  }

  function applyDensityRulesToDecision(context, decision, profile = {}) {
    return applyEnergyRulesToDecision(
      context,
      decision,
      getDensityRules(profile),
      "density_multiplier"
    );
  }

  function applyTensionRulesToDecision(context, decision, profile = {}) {

LINE 2293: decision,
----- CONTEXT -----

  function applyDensityRulesToDecision(context, decision, profile = {}) {
    return applyEnergyRulesToDecision(
      context,
      decision,
      getDensityRules(profile),
      "density_multiplier"
    );
  }

  function applyTensionRulesToDecision(context, decision, profile = {}) {
    return applyEnergyRulesToDecision(
      context,

LINE 2299: function applyTensionRulesToDecision(context, decision, profile = {}) {
----- CONTEXT -----
      "density_multiplier"
    );
  }

  function applyTensionRulesToDecision(context, decision, profile = {}) {
    return applyEnergyRulesToDecision(
      context,
      decision,
      getTensionRules(profile),
      "tension_multiplier"
    );
  }


LINE 2300: return applyEnergyRulesToDecision(
----- CONTEXT -----
    );
  }

  function applyTensionRulesToDecision(context, decision, profile = {}) {
    return applyEnergyRulesToDecision(
      context,
      decision,
      getTensionRules(profile),
      "tension_multiplier"
    );
  }

  function applyCrescendoRulesToDecision(context, decision, profile = {}) {

LINE 2302: decision,
----- CONTEXT -----

  function applyTensionRulesToDecision(context, decision, profile = {}) {
    return applyEnergyRulesToDecision(
      context,
      decision,
      getTensionRules(profile),
      "tension_multiplier"
    );
  }

  function applyCrescendoRulesToDecision(context, decision, profile = {}) {
    return applyEnergyRulesToDecision(
      context,

LINE 2308: function applyCrescendoRulesToDecision(context, decision, profile = {}) {
----- CONTEXT -----
      "tension_multiplier"
    );
  }

  function applyCrescendoRulesToDecision(context, decision, profile = {}) {
    return applyEnergyRulesToDecision(
      context,
      decision,
      getCrescendoRules(profile),
      "crescendo_multiplier"
    );
  }
  function includeAudioByGlobalDecision({

LINE 2309: return applyEnergyRulesToDecision(
----- CONTEXT -----
    );
  }

  function applyCrescendoRulesToDecision(context, decision, profile = {}) {
    return applyEnergyRulesToDecision(
      context,
      decision,
      getCrescendoRules(profile),
      "crescendo_multiplier"
    );
  }
  function includeAudioByGlobalDecision({
    random,

LINE 2311: decision,
----- CONTEXT -----

  function applyCrescendoRulesToDecision(context, decision, profile = {}) {
    return applyEnergyRulesToDecision(
      context,
      decision,
      getCrescendoRules(profile),
      "crescendo_multiplier"
    );
  }
  function includeAudioByGlobalDecision({
    random,
    globalInclusionState = null,
    requiredActivationState = null,

LINE 2316: function includeAudioByGlobalDecision({
----- CONTEXT -----
      getCrescendoRules(profile),
      "crescendo_multiplier"
    );
  }
  function includeAudioByGlobalDecision({
    random,
    globalInclusionState = null,
    requiredActivationState = null,
    selectedAudio = null,
    key = "",
    entry = null,
    fallbackChance = 0,
    force = false,

LINE 2334: const decision = force
----- CONTEXT -----
    if (!activeEntry?.key) return false;

    const profile = getRuleProfileForEntry(activeEntry);

    const decision = force
      ? setGlobalInclusionDecision(globalInclusionState, {
          kind: "audio",
          key: activeEntry.key,
          included: true,
          globalChance: 1,
          roll: 0,
          forced: true,
          reason: reason || "forced_global_selection",

LINE 2335: ? setGlobalInclusionDecision(globalInclusionState, {
----- CONTEXT -----

    const profile = getRuleProfileForEntry(activeEntry);

    const decision = force
      ? setGlobalInclusionDecision(globalInclusionState, {
          kind: "audio",
          key: activeEntry.key,
          included: true,
          globalChance: 1,
          roll: 0,
          forced: true,
          reason: reason || "forced_global_selection",
          profileSummary: {

LINE 2348: : resolveGlobalInclusionDecision({
----- CONTEXT -----
            family: activeEntry.family || "",
            tags: activeEntry.tags || []
          }
        })
      : resolveGlobalInclusionDecision({
          random,
          globalInclusionState,
          kind: "audio",
          key: activeEntry.key,
          entry: activeEntry,
          profile,
          fallbackChance
        });

LINE 2358: if (!decision?.included) return false;
----- CONTEXT -----
          profile,
          fallbackChance
        });

    if (!decision?.included) return false;

    selectedAudio.add(activeEntry.key);

    addRequiredActivationRulesFromProfile(requiredActivationState, {
      kind: "audio",
      key: activeEntry.key,
      entry: activeEntry,
      profile

LINE 2372: function includeMidiByGlobalDecision({
----- CONTEXT -----

    return true;
  }

  function includeMidiByGlobalDecision({
    random,
    globalInclusionState = null,
    requiredActivationState = null,
    selectedMidi = null,
    pattern = null,
    fallbackChance = 0,
    force = false,
    reason = ""

LINE 2386: const decision = force
----- CONTEXT -----
    if (!selectedMidi || !pattern?.file) return false;

    const profile = getRuleProfileForMidiPattern(pattern);

    const decision = force
      ? setGlobalInclusionDecision(globalInclusionState, {
          kind: "midi",
          key: pattern.file,
          included: true,
          globalChance: 1,
          roll: 0,
          forced: true,
          reason: reason || "forced_global_selection",

LINE 2387: ? setGlobalInclusionDecision(globalInclusionState, {
----- CONTEXT -----

    const profile = getRuleProfileForMidiPattern(pattern);

    const decision = force
      ? setGlobalInclusionDecision(globalInclusionState, {
          kind: "midi",
          key: pattern.file,
          included: true,
          globalChance: 1,
          roll: 0,
          forced: true,
          reason: reason || "forced_global_selection",
          profileSummary: {

LINE 2400: : resolveGlobalInclusionDecision({
----- CONTEXT -----
            family: pattern.id || "",
            tags: ["midi"]
          }
        })
      : resolveGlobalInclusionDecision({
          random,
          globalInclusionState,
          kind: "midi",
          key: pattern.file,
          pattern,
          profile,
          fallbackChance
        });

LINE 2410: if (!decision?.included) return false;
----- CONTEXT -----
          profile,
          fallbackChance
        });

    if (!decision?.included) return false;

    selectedMidi.add(pattern.file);

    addRequiredActivationRulesFromProfile(requiredActivationState, {
      kind: "midi",
      key: pattern.file,
      pattern,
      profile

LINE 2434: return includeAudioByGlobalDecision({
----- CONTEXT -----
  } = {}) {
    const entry = getCatalogEntry(key);
    if (!entry) return false;

    return includeAudioByGlobalDecision({
      random,
      globalInclusionState,
      requiredActivationState,
      selectedAudio,
      key,
      entry,
      fallbackChance: 1,
      force: true,

LINE 2548: return normalizeRuleDecisionToken(entry?.family || pattern?.id || pattern?.file || "");
----- CONTEXT -----
  }

  function getMidiPatternRuleFamily(pattern) {
    const entry = getMidiPatternRuleEntry(pattern);
    return normalizeRuleDecisionToken(entry?.family || pattern?.id || pattern?.file || "");
  }

  function getMidiPatternRuleTags(pattern) {
    const entry = getMidiPatternRuleEntry(pattern);
    return new Set((entry?.tags || []).map(tag => normalizeRuleDecisionToken(tag)));
  }

  function getMidiPatternBaseId(pattern) {

LINE 2553: return new Set((entry?.tags || []).map(tag => normalizeRuleDecisionToken(tag)));
----- CONTEXT -----
  }

  function getMidiPatternRuleTags(pattern) {
    const entry = getMidiPatternRuleEntry(pattern);
    return new Set((entry?.tags || []).map(tag => normalizeRuleDecisionToken(tag)));
  }

  function getMidiPatternBaseId(pattern) {
    let base = String(pattern?.id || pattern?.file || "").replace(/\.mid$/i, "");

    const sampleSuffixes = [
      "ridehard",
      "ride04",

LINE 2617: const tags = Array.isArray(item?.tags) ? item.tags.map(tag => normalizeRuleDecisionToken(tag)) : [];
----- CONTEXT -----
    }

    return scheduledAudio.some(item => {
      const key = String(item?.key || "").toLowerCase();
      const tags = Array.isArray(item?.tags) ? item.tags.map(tag => normalizeRuleDecisionToken(tag)) : [];

      return key.includes("jazz_crash") || tags.includes("jazz_crash");
    });
  }

  function chooseJazzRideWithHatsVariantForSection(section, sectionMidi) {
    const defaultRide = sectionMidi.find(pattern => pattern.file === JAZZ_RIDE_WITH_HATS_FILE);
    const crashRide = sectionMidi.find(pattern => pattern.file === JAZZ_RIDE_WITH_HATS_AND_CRASH_FILE);

LINE 2632: const tags = getMidiPatternRuleTagList(pattern).map(tag => normalizeRuleDecisionToken(tag));
----- CONTEXT -----
  }

  function isJazzMidiHatPattern(pattern) {
    const family = getMidiPatternRuleFamily(pattern);
    const tags = getMidiPatternRuleTagList(pattern).map(tag => normalizeRuleDecisionToken(tag));
    const key = String(pattern?.file || pattern?.id || "").toLowerCase();

    return (
      family === "jazz_hats" ||
      family === "jazz_rides" ||
      family === "jazz_ghost_rides" ||
      tags.includes("jazz_hats") ||
      tags.includes("jazz_rides") ||

LINE 2647: const tags = getMidiPatternRuleTagList(pattern).map(tag => normalizeRuleDecisionToken(tag));
----- CONTEXT -----
    );
  }

  function isNormalMidiHatPattern(pattern) {
    const tags = getMidiPatternRuleTagList(pattern).map(tag => normalizeRuleDecisionToken(tag));
    const key = String(pattern?.file || pattern?.id || "").toLowerCase();

    return tags.includes("hats") && key.includes("hats") && !isJazzMidiHatPattern(pattern);
  }

  function getNormalMidiHatCompanionGroupId(pattern) {
    if (!isNormalMidiHatPattern(pattern)) return "";
    return normalizeRuleDecisionToken(getMidiPatternBaseId(pattern));

LINE 2655: return normalizeRuleDecisionToken(getMidiPatternBaseId(pattern));
----- CONTEXT -----
  }

  function getNormalMidiHatCompanionGroupId(pattern) {
    if (!isNormalMidiHatPattern(pattern)) return "";
    return normalizeRuleDecisionToken(getMidiPatternBaseId(pattern));
  }

  function getNormalHatPatternDropoutChance(choiceGroupId) {
  if (
    choiceGroupId === "trap_hats" ||
    choiceGroupId === "holdit_hats" ||
    choiceGroupId === "messy_hats_fast" ||
    choiceGroupId === "messy_hats_fast_ends_in_main_hats"

LINE 2701: const baseId = normalizeRuleDecisionToken(getMidiPatternBaseId(pattern));
----- CONTEXT -----
}
function isRimMidiPattern(pattern) {
  const entry = getMidiPatternRuleEntry(pattern);
  const tags = getMidiPatternRuleTagList(pattern);
  const baseId = normalizeRuleDecisionToken(getMidiPatternBaseId(pattern));
  const key = normalizeRuleDecisionToken(entry?.key || pattern?.file || "");
  const hasRimTag = tags.some(tag => normalizeRuleDecisionToken(tag) === "rim");

  return hasRimTag || baseId.includes("rim") || key.includes("rim");
}

function isExtraRimMidiPattern(pattern) {
  const baseId = normalizeRuleDecisionToken(getMidiPatternBaseId(pattern));

LINE 2702: const key = normalizeRuleDecisionToken(entry?.key || pattern?.file || "");
----- CONTEXT -----
function isRimMidiPattern(pattern) {
  const entry = getMidiPatternRuleEntry(pattern);
  const tags = getMidiPatternRuleTagList(pattern);
  const baseId = normalizeRuleDecisionToken(getMidiPatternBaseId(pattern));
  const key = normalizeRuleDecisionToken(entry?.key || pattern?.file || "");
  const hasRimTag = tags.some(tag => normalizeRuleDecisionToken(tag) === "rim");

  return hasRimTag || baseId.includes("rim") || key.includes("rim");
}

function isExtraRimMidiPattern(pattern) {
  const baseId = normalizeRuleDecisionToken(getMidiPatternBaseId(pattern));
  const key = normalizeRuleDecisionToken(pattern?.file || "");

LINE 2703: const hasRimTag = tags.some(tag => normalizeRuleDecisionToken(tag) === "rim");
----- CONTEXT -----
  const entry = getMidiPatternRuleEntry(pattern);
  const tags = getMidiPatternRuleTagList(pattern);
  const baseId = normalizeRuleDecisionToken(getMidiPatternBaseId(pattern));
  const key = normalizeRuleDecisionToken(entry?.key || pattern?.file || "");
  const hasRimTag = tags.some(tag => normalizeRuleDecisionToken(tag) === "rim");

  return hasRimTag || baseId.includes("rim") || key.includes("rim");
}

function isExtraRimMidiPattern(pattern) {
  const baseId = normalizeRuleDecisionToken(getMidiPatternBaseId(pattern));
  const key = normalizeRuleDecisionToken(pattern?.file || "");


LINE 2709: const baseId = normalizeRuleDecisionToken(getMidiPatternBaseId(pattern));
----- CONTEXT -----
  return hasRimTag || baseId.includes("rim") || key.includes("rim");
}

function isExtraRimMidiPattern(pattern) {
  const baseId = normalizeRuleDecisionToken(getMidiPatternBaseId(pattern));
  const key = normalizeRuleDecisionToken(pattern?.file || "");

  return baseId.startsWith("rims_xtra") || key.includes("rims_xtra");
}

function isRimsDrumsRimMidiPattern(pattern) {
  const baseId = normalizeRuleDecisionToken(getMidiPatternBaseId(pattern));
  const key = normalizeRuleDecisionToken(pattern?.file || "");

LINE 2710: const key = normalizeRuleDecisionToken(pattern?.file || "");
----- CONTEXT -----
}

function isExtraRimMidiPattern(pattern) {
  const baseId = normalizeRuleDecisionToken(getMidiPatternBaseId(pattern));
  const key = normalizeRuleDecisionToken(pattern?.file || "");

  return baseId.startsWith("rims_xtra") || key.includes("rims_xtra");
}

function isRimsDrumsRimMidiPattern(pattern) {
  const baseId = normalizeRuleDecisionToken(getMidiPatternBaseId(pattern));
  const key = normalizeRuleDecisionToken(pattern?.file || "");


LINE 2716: const baseId = normalizeRuleDecisionToken(getMidiPatternBaseId(pattern));
----- CONTEXT -----
  return baseId.startsWith("rims_xtra") || key.includes("rims_xtra");
}

function isRimsDrumsRimMidiPattern(pattern) {
  const baseId = normalizeRuleDecisionToken(getMidiPatternBaseId(pattern));
  const key = normalizeRuleDecisionToken(pattern?.file || "");

  return baseId === "rims_drums_rim" || key.includes("rims_drums_rim");
}

function midiFileTriggersRimsDrumsRimFirstNoteMute(file) {
  const rawKey = String(file || "").toLowerCase();
  const key = normalizeRuleDecisionToken(file || "");

LINE 2717: const key = normalizeRuleDecisionToken(pattern?.file || "");
----- CONTEXT -----
}

function isRimsDrumsRimMidiPattern(pattern) {
  const baseId = normalizeRuleDecisionToken(getMidiPatternBaseId(pattern));
  const key = normalizeRuleDecisionToken(pattern?.file || "");

  return baseId === "rims_drums_rim" || key.includes("rims_drums_rim");
}

function midiFileTriggersRimsDrumsRimFirstNoteMute(file) {
  const rawKey = String(file || "").toLowerCase();
  const key = normalizeRuleDecisionToken(file || "");


LINE 2724: const key = normalizeRuleDecisionToken(file || "");
----- CONTEXT -----
}

function midiFileTriggersRimsDrumsRimFirstNoteMute(file) {
  const rawKey = String(file || "").toLowerCase();
  const key = normalizeRuleDecisionToken(file || "");

  return (
    key.includes("pre_snare") ||
    key.includes("presnare") ||
    rawKey.includes("pre-snare") ||
    key.includes("snare_xtra") ||
    key.includes("jazz_crash") ||
    key.includes("crash")

LINE 2772: const baseId = normalizeRuleDecisionToken(getMidiPatternBaseId(pattern));
----- CONTEXT -----
}
function isBeepipes1MidiPattern(pattern) {
  const entry = getMidiPatternRuleEntry(pattern);
  const tags = getMidiPatternRuleTagList(pattern);
  const baseId = normalizeRuleDecisionToken(getMidiPatternBaseId(pattern));
  const key = normalizeRuleDecisionToken(entry?.key || pattern?.file || "");
  const hasBeepipes1Tag = tags.some(tag =>
    normalizeRuleDecisionToken(tag).includes("beepipes_1")
  );

  return hasBeepipes1Tag || baseId.includes("beepipes_1") || key.includes("beepipes_1");
}


LINE 2773: const key = normalizeRuleDecisionToken(entry?.key || pattern?.file || "");
----- CONTEXT -----
function isBeepipes1MidiPattern(pattern) {
  const entry = getMidiPatternRuleEntry(pattern);
  const tags = getMidiPatternRuleTagList(pattern);
  const baseId = normalizeRuleDecisionToken(getMidiPatternBaseId(pattern));
  const key = normalizeRuleDecisionToken(entry?.key || pattern?.file || "");
  const hasBeepipes1Tag = tags.some(tag =>
    normalizeRuleDecisionToken(tag).includes("beepipes_1")
  );

  return hasBeepipes1Tag || baseId.includes("beepipes_1") || key.includes("beepipes_1");
}

function getBeepipes1DensityChanceMultiplier(section = {}) {

LINE 2775: normalizeRuleDecisionToken(tag).includes("beepipes_1")
----- CONTEXT -----
  const tags = getMidiPatternRuleTagList(pattern);
  const baseId = normalizeRuleDecisionToken(getMidiPatternBaseId(pattern));
  const key = normalizeRuleDecisionToken(entry?.key || pattern?.file || "");
  const hasBeepipes1Tag = tags.some(tag =>
    normalizeRuleDecisionToken(tag).includes("beepipes_1")
  );

  return hasBeepipes1Tag || baseId.includes("beepipes_1") || key.includes("beepipes_1");
}

function getBeepipes1DensityChanceMultiplier(section = {}) {
  const densityScore = Number(section.densityScore ?? section.density ?? 0);


LINE 2797: const baseId = normalizeRuleDecisionToken(getMidiPatternBaseId(pattern));
----- CONTEXT -----
}
function isBeepipesMidiPattern(pattern) {
  const entry = getMidiPatternRuleEntry(pattern);
  const tags = getMidiPatternRuleTagList(pattern);
  const baseId = normalizeRuleDecisionToken(getMidiPatternBaseId(pattern));
  const key = normalizeRuleDecisionToken(entry?.key || pattern?.file || "");
  const hasBeepipesTag = tags.some(tag =>
    normalizeRuleDecisionToken(tag).includes("beepipes")
  );

  return hasBeepipesTag || baseId.includes("beepipes") || key.includes("beepipes");
}


LINE 2798: const key = normalizeRuleDecisionToken(entry?.key || pattern?.file || "");
----- CONTEXT -----
function isBeepipesMidiPattern(pattern) {
  const entry = getMidiPatternRuleEntry(pattern);
  const tags = getMidiPatternRuleTagList(pattern);
  const baseId = normalizeRuleDecisionToken(getMidiPatternBaseId(pattern));
  const key = normalizeRuleDecisionToken(entry?.key || pattern?.file || "");
  const hasBeepipesTag = tags.some(tag =>
    normalizeRuleDecisionToken(tag).includes("beepipes")
  );

  return hasBeepipesTag || baseId.includes("beepipes") || key.includes("beepipes");
}

function isSnarePatternMidiPattern(pattern) {

LINE 2800: normalizeRuleDecisionToken(tag).includes("beepipes")
----- CONTEXT -----
  const tags = getMidiPatternRuleTagList(pattern);
  const baseId = normalizeRuleDecisionToken(getMidiPatternBaseId(pattern));
  const key = normalizeRuleDecisionToken(entry?.key || pattern?.file || "");
  const hasBeepipesTag = tags.some(tag =>
    normalizeRuleDecisionToken(tag).includes("beepipes")
  );

  return hasBeepipesTag || baseId.includes("beepipes") || key.includes("beepipes");
}

function isSnarePatternMidiPattern(pattern) {
  const baseId = normalizeRuleDecisionToken(getMidiPatternBaseId(pattern));
  const key = normalizeRuleDecisionToken(pattern?.file || "");

LINE 2807: const baseId = normalizeRuleDecisionToken(getMidiPatternBaseId(pattern));
----- CONTEXT -----
  return hasBeepipesTag || baseId.includes("beepipes") || key.includes("beepipes");
}

function isSnarePatternMidiPattern(pattern) {
  const baseId = normalizeRuleDecisionToken(getMidiPatternBaseId(pattern));
  const key = normalizeRuleDecisionToken(pattern?.file || "");

  return baseId.startsWith("snare_pattern") || key.includes("snare_pattern");
}

function midiPatternClashesWithSelectedSection(pattern, selectedMidiFiles, midiPatternPool) {
  const selectedPatterns = [...selectedMidiFiles]
    .map(file => midiPatternPool.find(item => item.file === file))

LINE 2808: const key = normalizeRuleDecisionToken(pattern?.file || "");
----- CONTEXT -----
}

function isSnarePatternMidiPattern(pattern) {
  const baseId = normalizeRuleDecisionToken(getMidiPatternBaseId(pattern));
  const key = normalizeRuleDecisionToken(pattern?.file || "");

  return baseId.startsWith("snare_pattern") || key.includes("snare_pattern");
}

function midiPatternClashesWithSelectedSection(pattern, selectedMidiFiles, midiPatternPool) {
  const selectedPatterns = [...selectedMidiFiles]
    .map(file => midiPatternPool.find(item => item.file === file))
    .filter(Boolean);

LINE 2883: const baseId = normalizeRuleDecisionToken(getMidiPatternBaseId(pattern));
----- CONTEXT -----
}
function isBeepipesGhostsMidiPattern(pattern) {
  const entry = getMidiPatternRuleEntry(pattern);
  const tags = getMidiPatternRuleTagList(pattern);
  const baseId = normalizeRuleDecisionToken(getMidiPatternBaseId(pattern));
  const key = normalizeRuleDecisionToken(entry?.key || pattern?.file || "");
  const hasGhostTag = tags.some(tag => {
    const normalisedTag = normalizeRuleDecisionToken(tag);
    return normalisedTag.includes("beepipes_ghosts") || normalisedTag === "ghosts";
  });

  return hasGhostTag || baseId.includes("beepipes_ghosts") || key.includes("beepipes_ghosts");
}

LINE 2884: const key = normalizeRuleDecisionToken(entry?.key || pattern?.file || "");
----- CONTEXT -----
function isBeepipesGhostsMidiPattern(pattern) {
  const entry = getMidiPatternRuleEntry(pattern);
  const tags = getMidiPatternRuleTagList(pattern);
  const baseId = normalizeRuleDecisionToken(getMidiPatternBaseId(pattern));
  const key = normalizeRuleDecisionToken(entry?.key || pattern?.file || "");
  const hasGhostTag = tags.some(tag => {
    const normalisedTag = normalizeRuleDecisionToken(tag);
    return normalisedTag.includes("beepipes_ghosts") || normalisedTag === "ghosts";
  });

  return hasGhostTag || baseId.includes("beepipes_ghosts") || key.includes("beepipes_ghosts");
}
function isBeepipes2MidiPattern(pattern) {

LINE 2886: const normalisedTag = normalizeRuleDecisionToken(tag);
----- CONTEXT -----
  const tags = getMidiPatternRuleTagList(pattern);
  const baseId = normalizeRuleDecisionToken(getMidiPatternBaseId(pattern));
  const key = normalizeRuleDecisionToken(entry?.key || pattern?.file || "");
  const hasGhostTag = tags.some(tag => {
    const normalisedTag = normalizeRuleDecisionToken(tag);
    return normalisedTag.includes("beepipes_ghosts") || normalisedTag === "ghosts";
  });

  return hasGhostTag || baseId.includes("beepipes_ghosts") || key.includes("beepipes_ghosts");
}
function isBeepipes2MidiPattern(pattern) {
  const entry = getMidiPatternRuleEntry(pattern);
  const tags = getMidiPatternRuleTagList(pattern);

LINE 2895: const baseId = normalizeRuleDecisionToken(getMidiPatternBaseId(pattern));
----- CONTEXT -----
}
function isBeepipes2MidiPattern(pattern) {
  const entry = getMidiPatternRuleEntry(pattern);
  const tags = getMidiPatternRuleTagList(pattern);
  const baseId = normalizeRuleDecisionToken(getMidiPatternBaseId(pattern));
  const key = normalizeRuleDecisionToken(entry?.key || pattern?.file || "");
  const hasBeepipes2Tag = tags.some(tag =>
    normalizeRuleDecisionToken(tag).includes("beepipes_2")
  );

  return hasBeepipes2Tag || baseId.includes("beepipes_2") || key.includes("beepipes_2");
}
function normalHatChoiceClashesWithMidiPattern(choiceGroupId, pattern) {

LINE 2896: const key = normalizeRuleDecisionToken(entry?.key || pattern?.file || "");
----- CONTEXT -----
function isBeepipes2MidiPattern(pattern) {
  const entry = getMidiPatternRuleEntry(pattern);
  const tags = getMidiPatternRuleTagList(pattern);
  const baseId = normalizeRuleDecisionToken(getMidiPatternBaseId(pattern));
  const key = normalizeRuleDecisionToken(entry?.key || pattern?.file || "");
  const hasBeepipes2Tag = tags.some(tag =>
    normalizeRuleDecisionToken(tag).includes("beepipes_2")
  );

  return hasBeepipes2Tag || baseId.includes("beepipes_2") || key.includes("beepipes_2");
}
function normalHatChoiceClashesWithMidiPattern(choiceGroupId, pattern) {
  if (!choiceGroupId) return false;

LINE 2898: normalizeRuleDecisionToken(tag).includes("beepipes_2")
----- CONTEXT -----
  const tags = getMidiPatternRuleTagList(pattern);
  const baseId = normalizeRuleDecisionToken(getMidiPatternBaseId(pattern));
  const key = normalizeRuleDecisionToken(entry?.key || pattern?.file || "");
  const hasBeepipes2Tag = tags.some(tag =>
    normalizeRuleDecisionToken(tag).includes("beepipes_2")
  );

  return hasBeepipes2Tag || baseId.includes("beepipes_2") || key.includes("beepipes_2");
}
function normalHatChoiceClashesWithMidiPattern(choiceGroupId, pattern) {
  if (!choiceGroupId) return false;

  if (isBeepipes2MidiPattern(pattern) && choiceGroupId !== "hats_wiv_beepipes") {

LINE 2930: const baseId = normalizeRuleDecisionToken(getMidiPatternBaseId(pattern));
----- CONTEXT -----
    return true;
  }

  if (choiceGroupId === "speedy_hats") {
    const baseId = normalizeRuleDecisionToken(getMidiPatternBaseId(pattern));
    return baseId.startsWith("rims_xtra_1");
  }

  return false;
}
function shouldExcludeNormalMidiHatFromGenericPicker(pattern, choiceGroupId = null) {
  const baseId = normalizeRuleDecisionToken(getMidiPatternBaseId(pattern));
  const groupId = choiceGroupId || getNormalMidiHatChoiceGroupId(pattern);

LINE 2937: const baseId = normalizeRuleDecisionToken(getMidiPatternBaseId(pattern));
----- CONTEXT -----

  return false;
}
function shouldExcludeNormalMidiHatFromGenericPicker(pattern, choiceGroupId = null) {
  const baseId = normalizeRuleDecisionToken(getMidiPatternBaseId(pattern));
  const groupId = choiceGroupId || getNormalMidiHatChoiceGroupId(pattern);

  if (baseId.startsWith("messy_hats_addin")) {
    return true;
  }

  if (baseId.startsWith("speedy_hats_cont")) {
    return true;

LINE 2965: const base = normalizeRuleDecisionToken(getMidiPatternBaseId(pattern));
----- CONTEXT -----
}
function getNormalMidiHatChoiceGroupId(pattern) {
    if (!isNormalMidiHatPattern(pattern)) return "";

    const base = normalizeRuleDecisionToken(getMidiPatternBaseId(pattern));

    if (base.startsWith("main_hats")) return "main_hats";
    if (base.startsWith("hats_hook_metal")) return "hook_hats";
    if (base.startsWith("hats_wiv_beepipes")) return "hats_wiv_beepipes";
    if (base.startsWith("holdit_hats_forlyrix")) return "holdit_hats_forlyrix";
    if (base.startsWith("holdit_hats")) return "holdit_hats";
    if (base.startsWith("lego_hats")) return "lego_hats";
    if (base.startsWith("messy_hats_fast_ends_in_main_hats")) return "messy_hats_fast_ends_in_main_hats";

LINE 3094: const startSeconds = cursorSeconds;
----- CONTEXT -----
      name: "main_56"
    };

    function addSection(type, bars, options = {}) {
      const startSeconds = cursorSeconds;
      const barSeconds = currentGrid.barSeconds;
      const durationSeconds = bars * barSeconds;
      const endSeconds = startSeconds + durationSeconds;

      const section = {
        id: `${sectionTimeline.length + 1}_${type}`,
        type,
        startSeconds,

LINE 3097: const endSeconds = startSeconds + durationSeconds;
----- CONTEXT -----
    function addSection(type, bars, options = {}) {
      const startSeconds = cursorSeconds;
      const barSeconds = currentGrid.barSeconds;
      const durationSeconds = bars * barSeconds;
      const endSeconds = startSeconds + durationSeconds;

      const section = {
        id: `${sectionTimeline.length + 1}_${type}`,
        type,
        startSeconds,
        endSeconds,
        durationSeconds,
        bars,

LINE 3102: startSeconds,
----- CONTEXT -----

      const section = {
        id: `${sectionTimeline.length + 1}_${type}`,
        type,
        startSeconds,
        endSeconds,
        durationSeconds,
        bars,
        trackStartBar: cursorBars + 1,
        trackEndBar: cursorBars + bars,
        bpm: currentGrid.bpm,
        barSeconds,
        gridAnchorSeconds: currentGrid.gridAnchorSeconds,

LINE 3125: timeSeconds: startSeconds,
----- CONTEXT -----
      sectionTimeline.push(section);

      if (section.reset) {
        resetPoints.push({
          timeSeconds: startSeconds,
          reason: `${type}_section_start`
        });
      }

      cursorSeconds = endSeconds;
      cursorBars += bars;
      return section;
    }

LINE 3426: includeAudioByGlobalDecision({
----- CONTEXT -----

    for (const key of foundationCandidates) {
      const entry = getCatalogEntry(key);

      includeAudioByGlobalDecision({
        random,
        globalInclusionState,
        requiredActivationState,
        selectedAudio,
        key,
        entry,
        fallbackChance: 0.65,
        reason: "foundation_candidate"

LINE 3479: const included = includeAudioByGlobalDecision({
----- CONTEXT -----
      for (const entry of shuffled.slice(0, 12)) {
        const chanceMultiplier = section.type === "normal" ? 0.65 : 1.0;
        const p = Math.min(0.9, getBaseActivationChance(entry) * chanceMultiplier);

        const included = includeAudioByGlobalDecision({
          random,
          globalInclusionState,
          requiredActivationState,
          selectedAudio,
          entry,
          fallbackChance: p,
          reason: `section_selection:${section.type}`
        });

LINE 3599: const jazzIncluded = includeMidiByGlobalDecision({
----- CONTEXT -----
      }

      if (jazzHatsActiveForSection) {
        for (const jazzPattern of [...jazzHatPatternsForSection, jazzRideWithHatsPattern, jazzRideWithHatsAndCrashPattern, jazzGhostRidePattern].filter(Boolean)) {
          const jazzIncluded = includeMidiByGlobalDecision({
            random,
            globalInclusionState,
            requiredActivationState,
            selectedMidi,
            pattern: jazzPattern,
            fallbackChance: 1,
            force: true,
            reason: "jazz_hats_takeover"

LINE 3724: const included = includeMidiByGlobalDecision({
----- CONTEXT -----
          chosenPatterns = chooseOne(random, [...companionGroups.values()]) || [];
        }

        for (const pattern of chosenPatterns.filter(Boolean)) {
          const included = includeMidiByGlobalDecision({
            random,
            globalInclusionState,
            requiredActivationState,
            selectedMidi,
            pattern,
            fallbackChance: 1,
            force: true,
            reason: `normal_hat_group:${chosenChoice}`

LINE 3763: const included = includeMidiByGlobalDecision({
----- CONTEXT -----
            const messyHatsAddinFile = "midi files/messy_hats_addin_metal_odd_ch.mid";
            const messyHatsAddinPattern = midiPatternPool.find(pattern => pattern.file === messyHatsAddinFile);

            if (messyHatsAddinPattern) {
              const included = includeMidiByGlobalDecision({
                random,
                globalInclusionState,
                requiredActivationState,
                selectedMidi,
                pattern: messyHatsAddinPattern,
                fallbackChance: 1,
                force: true,
                reason: "messy_hats_addin_progression"

LINE 3822: const included = includeMidiByGlobalDecision({
----- CONTEXT -----
        if (isBeepipes1MidiPattern(pattern)) {
          p *= getBeepipes1DensityChanceMultiplier(section);
        }

        const included = includeMidiByGlobalDecision({
          random,
          globalInclusionState,
          requiredActivationState,
          selectedMidi,
          pattern,
          fallbackChance: p,
          reason: `section_selection:${section.type}`
        });

LINE 3848: const hatIncluded = includeMidiByGlobalDecision({
----- CONTEXT -----
              if (!hatPattern) {
                continue;
              }

              const hatIncluded = includeMidiByGlobalDecision({
                random,
                globalInclusionState,
                requiredActivationState,
                selectedMidi,
                pattern: hatPattern,
                fallbackChance: 1,
                force: true,
                reason: "beepipes_2_matching_hats"

LINE 3873: const included = includeMidiByGlobalDecision({
----- CONTEXT -----

    if (selectedMidi.size === 0) {
      const fallback = midiPatterns.patterns.find(pattern => pattern.file === "midi files/main_hats_ch_metal_ch.mid");
      if (fallback) {
        const included = includeMidiByGlobalDecision({
          random,
          globalInclusionState,
          requiredActivationState,
          selectedMidi,
          pattern: fallback,
          fallbackChance: 1,
          force: true,
          reason: "forced_midi_fallback"

LINE 3935: function scheduleBuffer(offlineContext, destination, buffer, startTime, gainValue = 1, offset = 0) {
----- CONTEXT -----
    const arrayBuffer = await response.arrayBuffer();
    return await offlineContext.decodeAudioData(arrayBuffer);
  }

  function scheduleBuffer(offlineContext, destination, buffer, startTime, gainValue = 1, offset = 0) {
    if (!buffer) return null;
    if (startTime >= offlineContext.length / offlineContext.sampleRate) return null;

    const source = offlineContext.createBufferSource();
    const gain = offlineContext.createGain();

    const safeStartTime = Math.max(0, startTime);
    const safeOffset = Math.max(0, offset);

LINE 4010: const scheduleHandle = scheduleBuffer(
----- CONTEXT -----
    entry = null,
    section = null,
    family = ""
  } = {}) {
    const scheduleHandle = scheduleBuffer(
      offlineContext,
      destination,
      buffer,
      startTime,
      gainValue,
      offset
    );


LINE 4076: const scheduled = scheduleBuffer(offlineContext, destination, sampleBuffer, start, gainValue * velocityGain);
----- CONTEXT -----
      }

      const start = barStart + note.beats * beatSeconds;
      const velocityGain = Math.max(0.05, note.velocity01 ?? 0.7);
      const scheduled = scheduleBuffer(offlineContext, destination, sampleBuffer, start, gainValue * velocityGain);

      if (scheduled) {
        scheduledCount += 1;

        registerScheduledPlaybackHandle(playbackState, {
          kind: "midi",
          key: pattern.file,
          entry,

LINE 4269: applyCutoffRulesForAllowedDecision(playbackState, {
----- CONTEXT -----
    };

    const lyrixProfile = getRuleProfileForEntry(entry);

    applyCutoffRulesForAllowedDecision(playbackState, {
      entry,
      section,
      startSeconds: startTime
    }, lyrixProfile);

    return scheduleAudioBufferWithPlaybackState({
      offlineContext,
      destination,

LINE 4272: startSeconds: startTime
----- CONTEXT -----

    applyCutoffRulesForAllowedDecision(playbackState, {
      entry,
      section,
      startSeconds: startTime
    }, lyrixProfile);

    return scheduleAudioBufferWithPlaybackState({
      offlineContext,
      destination,
      buffer,
      startTime,
      gainValue,

LINE 4293: let start = section.startSeconds;
----- CONTEXT -----
  function scheduleExplicitLyrixSection({ offlineContext, destination, section, random, playbackState = null, buffers }) {
    const lyrixSection = section.lyrixSection;
    if (!lyrixSection?.parts?.length) return false;

    let start = section.startSeconds;
    const leadIn = lyrixSection.leadIn || null;

    const shouldSkipLeadIn =
      section.suppressLyrixLeadIn ||
      (leadIn?.firstActivationOnly && Number(section.lyrixActivationNumber || 1) > 1);

    if (leadIn && !shouldSkipLeadIn) {
      const leadInChance = leadIn.chance === undefined ? 1 : Number(leadIn.chance);

LINE 4304: const leadInStart = Math.max(0, section.startSeconds - (Number(leadIn.startsBeforeBars) || 0) * section.barSeconds);
----- CONTEXT -----
    if (leadIn && !shouldSkipLeadIn) {
      const leadInChance = leadIn.chance === undefined ? 1 : Number(leadIn.chance);

      if (chance(random, leadInChance)) {
        const leadInStart = Math.max(0, section.startSeconds - (Number(leadIn.startsBeforeBars) || 0) * section.barSeconds);
        const leadInGain = Number(leadIn.gain) || 0.72;

        if (leadIn.file) {
          const leadInBuffer = buffers.get(leadIn.file);
          scheduleLyrixPathWithPlaybackState({
            offlineContext,
            destination,
            path: leadIn.file,

LINE 4422: adlibStart = section.startSeconds;
----- CONTEXT -----

      if (adlib.part) {
        adlibStart = partStartTimes.get(Number(adlib.part));
      } else if (adlib.startsWhen && String(adlib.startsWhen).includes("_lyrix_starts")) {
        adlibStart = section.startSeconds;
      }


      if (typeof adlibStart !== "number") continue;

      const activationChance = adlib.activationChance === undefined ? 1 : Number(adlib.activationChance);
      if (!chance(random, activationChance)) continue;


LINE 4492: let start = section.startSeconds;
----- CONTEXT -----
      });

    if (!groupKeys.length) return false;

    let start = section.startSeconds;

    for (const groupKey of groupKeys) {
      const buffer = buffers.get(groupKey);
      if (!buffer) continue;

      scheduleLyrixPathWithPlaybackState({
        offlineContext,
        destination,

LINE 4514: function scheduleAudioStemInSection({ offlineContext, destination, key, buffer, random, plan = null, lifecycleStates = null, playbackState = null, section }) {
----- CONTEXT -----
    }

    return true;
  }
  function scheduleAudioStemInSection({ offlineContext, destination, key, buffer, random, plan = null, lifecycleStates = null, playbackState = null, section }) {
    const entry = getCatalogEntry(key);
    if (!entry || !buffer) return 0;
    if (!audioMatchesSection(entry, section)) return 0;

    const keyLower = key.toLowerCase();
    const gain = sectionGainForAudio(entry, section);

    if (entry.folder === "alternate downloads") {

LINE 4528: startTime: section.startSeconds,
----- CONTEXT -----
        return scheduleAudioBufferWithPlaybackState({
          offlineContext,
          destination,
          buffer,
          startTime: section.startSeconds,
          gainValue: 0.8,
          playbackState,
          key,
          entry,
          section
        }) ? 1 : 0;
      }
      return 0;

LINE 4544: startTime: section.startSeconds,
----- CONTEXT -----
      return scheduleAudioBufferWithPlaybackState({
        offlineContext,
        destination,
        buffer,
        startTime: section.startSeconds,
        gainValue: gain,
        playbackState,
        key,
        entry,
        section
      }) ? 1 : 0;
    }


LINE 4559: startTime: section.startSeconds + localBar * section.barSeconds,
----- CONTEXT -----
      return scheduleAudioBufferWithPlaybackState({
        offlineContext,
        destination,
        buffer,
        startTime: section.startSeconds + localBar * section.barSeconds,
        gainValue: gain,
        playbackState,
        key,
        entry,
        section
      }) ? 1 : 0;
    }


LINE 4573: startTime: section.startSeconds,
----- CONTEXT -----
      return scheduleAudioBufferWithPlaybackState({
        offlineContext,
        destination,
        buffer,
        startTime: section.startSeconds,
        gainValue: gain,
        playbackState,
        key,
        entry,
        section
      }) ? 1 : 0;
    }


LINE 4588: startTime: section.startSeconds + localBar * section.barSeconds,
----- CONTEXT -----
      return scheduleAudioBufferWithPlaybackState({
        offlineContext,
        destination,
        buffer,
        startTime: section.startSeconds + localBar * section.barSeconds,
        gainValue: gain,
        playbackState,
        key,
        entry,
        section
      }) ? 1 : 0;
    }


LINE 4612: const allowedBars = getAllowedLocalBarIndexesForKey(key, section);
----- CONTEXT -----

    const audioProfile = getRuleProfileForEntry(entry);

    if (isLikelyOneShot(entry)) {
      const allowedBars = getAllowedLocalBarIndexesForKey(key, section);
      const oneShotBaseChance = getActivationChance(audioProfile, 0.12);
      let scheduledCount = 0;

      for (const localBarIndex of allowedBars) {
        const startSeconds = section.startSeconds + localBarIndex * section.barSeconds;
        const audioDecisionResult = resolveRuleProfileDecision({
          random,
          plan,

LINE 4616: for (const localBarIndex of allowedBars) {
----- CONTEXT -----
      const allowedBars = getAllowedLocalBarIndexesForKey(key, section);
      const oneShotBaseChance = getActivationChance(audioProfile, 0.12);
      let scheduledCount = 0;

      for (const localBarIndex of allowedBars) {
        const startSeconds = section.startSeconds + localBarIndex * section.barSeconds;
        const audioDecisionResult = resolveRuleProfileDecision({
          random,
          plan,
          playbackState,
          kind: "audio",
          key,
          entry,

LINE 4617: const startSeconds = section.startSeconds + localBarIndex * section.barSeconds;
----- CONTEXT -----
      const oneShotBaseChance = getActivationChance(audioProfile, 0.12);
      let scheduledCount = 0;

      for (const localBarIndex of allowedBars) {
        const startSeconds = section.startSeconds + localBarIndex * section.barSeconds;
        const audioDecisionResult = resolveRuleProfileDecision({
          random,
          plan,
          playbackState,
          kind: "audio",
          key,
          entry,
          section,

LINE 4618: const audioDecisionResult = resolveRuleProfileDecision({
----- CONTEXT -----
      let scheduledCount = 0;

      for (const localBarIndex of allowedBars) {
        const startSeconds = section.startSeconds + localBarIndex * section.barSeconds;
        const audioDecisionResult = resolveRuleProfileDecision({
          random,
          plan,
          playbackState,
          kind: "audio",
          key,
          entry,
          section,
          lifecycleStates,

LINE 4628: startSeconds,
----- CONTEXT -----
          entry,
          section,
          lifecycleStates,
          localBarIndex,
          startSeconds,
          baseChance: oneShotBaseChance,
          profile: audioProfile
        });

        if (audioDecisionResult.allowed) {
          applyCutoffRulesForAllowedDecision(playbackState, audioDecisionResult.context, audioProfile);
          const scheduled = scheduleAudioBufferWithPlaybackState({
            offlineContext,

LINE 4633: if (audioDecisionResult.allowed) {
----- CONTEXT -----
          baseChance: oneShotBaseChance,
          profile: audioProfile
        });

        if (audioDecisionResult.allowed) {
          applyCutoffRulesForAllowedDecision(playbackState, audioDecisionResult.context, audioProfile);
          const scheduled = scheduleAudioBufferWithPlaybackState({
            offlineContext,
            destination,
            buffer,
            startTime: startSeconds,
            gainValue: gain,
            playbackState,

LINE 4634: applyCutoffRulesForAllowedDecision(playbackState, audioDecisionResult.context, audioProfile);
----- CONTEXT -----
          profile: audioProfile
        });

        if (audioDecisionResult.allowed) {
          applyCutoffRulesForAllowedDecision(playbackState, audioDecisionResult.context, audioProfile);
          const scheduled = scheduleAudioBufferWithPlaybackState({
            offlineContext,
            destination,
            buffer,
            startTime: startSeconds,
            gainValue: gain,
            playbackState,
            key,

LINE 4639: startTime: startSeconds,
----- CONTEXT -----
          const scheduled = scheduleAudioBufferWithPlaybackState({
            offlineContext,
            destination,
            buffer,
            startTime: startSeconds,
            gainValue: gain,
            playbackState,
            key,
            entry,
            section
          });

          if (scheduled) {

LINE 4657: const allowedPhraseBars = getAllowedLocalBarIndexesForKey(key, section);
----- CONTEXT -----
      return scheduledCount;
    }

    const phraseRepeats = section.type === "normal" ? 1 : 2;
    const allowedPhraseBars = getAllowedLocalBarIndexesForKey(key, section);

    if (!allowedPhraseBars.length) return 0;

    const phraseBaseChance = getActivationChance(audioProfile, 0.45);
    let scheduledCount = 0;

    for (let i = 0; i < phraseRepeats; i++) {
      const localBar = chooseOne(random, allowedPhraseBars);

LINE 4659: if (!allowedPhraseBars.length) return 0;
----- CONTEXT -----

    const phraseRepeats = section.type === "normal" ? 1 : 2;
    const allowedPhraseBars = getAllowedLocalBarIndexesForKey(key, section);

    if (!allowedPhraseBars.length) return 0;

    const phraseBaseChance = getActivationChance(audioProfile, 0.45);
    let scheduledCount = 0;

    for (let i = 0; i < phraseRepeats; i++) {
      const localBar = chooseOne(random, allowedPhraseBars);
      const startSeconds = section.startSeconds + localBar * section.barSeconds;
      const audioDecisionResult = resolveRuleProfileDecision({

LINE 4665: const localBar = chooseOne(random, allowedPhraseBars);
----- CONTEXT -----
    const phraseBaseChance = getActivationChance(audioProfile, 0.45);
    let scheduledCount = 0;

    for (let i = 0; i < phraseRepeats; i++) {
      const localBar = chooseOne(random, allowedPhraseBars);
      const startSeconds = section.startSeconds + localBar * section.barSeconds;
      const audioDecisionResult = resolveRuleProfileDecision({
        random,
        plan,
        playbackState,
        kind: "audio",
        key,
        entry,

LINE 4666: const startSeconds = section.startSeconds + localBar * section.barSeconds;
----- CONTEXT -----
    let scheduledCount = 0;

    for (let i = 0; i < phraseRepeats; i++) {
      const localBar = chooseOne(random, allowedPhraseBars);
      const startSeconds = section.startSeconds + localBar * section.barSeconds;
      const audioDecisionResult = resolveRuleProfileDecision({
        random,
        plan,
        playbackState,
        kind: "audio",
        key,
        entry,
        section,

LINE 4667: const audioDecisionResult = resolveRuleProfileDecision({
----- CONTEXT -----

    for (let i = 0; i < phraseRepeats; i++) {
      const localBar = chooseOne(random, allowedPhraseBars);
      const startSeconds = section.startSeconds + localBar * section.barSeconds;
      const audioDecisionResult = resolveRuleProfileDecision({
        random,
        plan,
        playbackState,
        kind: "audio",
        key,
        entry,
        section,
        lifecycleStates,

LINE 4677: startSeconds,
----- CONTEXT -----
        entry,
        section,
        lifecycleStates,
        localBarIndex: localBar,
        startSeconds,
        baseChance: phraseBaseChance,
        profile: audioProfile
      });

      if (audioDecisionResult.allowed) {
        applyCutoffRulesForAllowedDecision(playbackState, audioDecisionResult.context, audioProfile);
        const scheduled = scheduleAudioBufferWithPlaybackState({
          offlineContext,

LINE 4682: if (audioDecisionResult.allowed) {
----- CONTEXT -----
        baseChance: phraseBaseChance,
        profile: audioProfile
      });

      if (audioDecisionResult.allowed) {
        applyCutoffRulesForAllowedDecision(playbackState, audioDecisionResult.context, audioProfile);
        const scheduled = scheduleAudioBufferWithPlaybackState({
          offlineContext,
          destination,
          buffer,
          startTime: startSeconds,
          gainValue: gain,
          playbackState,

LINE 4683: applyCutoffRulesForAllowedDecision(playbackState, audioDecisionResult.context, audioProfile);
----- CONTEXT -----
        profile: audioProfile
      });

      if (audioDecisionResult.allowed) {
        applyCutoffRulesForAllowedDecision(playbackState, audioDecisionResult.context, audioProfile);
        const scheduled = scheduleAudioBufferWithPlaybackState({
          offlineContext,
          destination,
          buffer,
          startTime: startSeconds,
          gainValue: gain,
          playbackState,
          key,

LINE 4688: startTime: startSeconds,
----- CONTEXT -----
        const scheduled = scheduleAudioBufferWithPlaybackState({
          offlineContext,
          destination,
          buffer,
          startTime: startSeconds,
          gainValue: gain,
          playbackState,
          key,
          entry,
          section
        });

        if (scheduled) {

LINE 4705: function schedulePlan({ offlineContext, destination, buffers, plan, random, duration }) {
----- CONTEXT -----

    return scheduledCount;
  }

  function schedulePlan({ offlineContext, destination, buffers, plan, random, duration }) {
    const sections = plan.sectionTimeline || [];
    const lifecycleStates = createLifecycleMapFromPlan(plan);
    const playbackState = createPlaybackRuleState();

    function scheduleMidiPatternInSectionWithRules(pattern, section) {
      const midiLifecycleId = getMidiLifecycleId(pattern.file);

      if (!isLifecycleIdEligible(lifecycleStates, midiLifecycleId)) return 0;

LINE 4710: function scheduleMidiPatternInSectionWithRules(pattern, section) {
----- CONTEXT -----
    const sections = plan.sectionTimeline || [];
    const lifecycleStates = createLifecycleMapFromPlan(plan);
    const playbackState = createPlaybackRuleState();

    function scheduleMidiPatternInSectionWithRules(pattern, section) {
      const midiLifecycleId = getMidiLifecycleId(pattern.file);

      if (!isLifecycleIdEligible(lifecycleStates, midiLifecycleId)) return 0;

      const repeatEveryBars = getMidiRepeatEveryBars(pattern);
      const repeatEverySeconds = section.barSeconds * repeatEveryBars;
      let totalScheduledCount = 0;


LINE 4719: for (let t = section.startSeconds; t < section.endSeconds; t += repeatEverySeconds) {
----- CONTEXT -----
      const repeatEveryBars = getMidiRepeatEveryBars(pattern);
      const repeatEverySeconds = section.barSeconds * repeatEveryBars;
      let totalScheduledCount = 0;

      for (let t = section.startSeconds; t < section.endSeconds; t += repeatEverySeconds) {
        const localBarIndex = Math.round((t - section.startSeconds) / section.barSeconds);

        if (!isBarOpportunityAllowedForKey(pattern.file, section, localBarIndex)) {
          continue;
        }

        const midiProfile = getRuleProfileForMidiPattern(pattern);
        const midiBaseChance = getActivationChance(midiProfile, 0.7);

LINE 4720: const localBarIndex = Math.round((t - section.startSeconds) / section.barSeconds);
----- CONTEXT -----
      const repeatEverySeconds = section.barSeconds * repeatEveryBars;
      let totalScheduledCount = 0;

      for (let t = section.startSeconds; t < section.endSeconds; t += repeatEverySeconds) {
        const localBarIndex = Math.round((t - section.startSeconds) / section.barSeconds);

        if (!isBarOpportunityAllowedForKey(pattern.file, section, localBarIndex)) {
          continue;
        }

        const midiProfile = getRuleProfileForMidiPattern(pattern);
        const midiBaseChance = getActivationChance(midiProfile, 0.7);


LINE 4722: if (!isBarOpportunityAllowedForKey(pattern.file, section, localBarIndex)) {
----- CONTEXT -----

      for (let t = section.startSeconds; t < section.endSeconds; t += repeatEverySeconds) {
        const localBarIndex = Math.round((t - section.startSeconds) / section.barSeconds);

        if (!isBarOpportunityAllowedForKey(pattern.file, section, localBarIndex)) {
          continue;
        }

        const midiProfile = getRuleProfileForMidiPattern(pattern);
        const midiBaseChance = getActivationChance(midiProfile, 0.7);

        const midiDecisionResult = resolveRuleProfileDecision({
          random,

LINE 4729: const midiDecisionResult = resolveRuleProfileDecision({
----- CONTEXT -----

        const midiProfile = getRuleProfileForMidiPattern(pattern);
        const midiBaseChance = getActivationChance(midiProfile, 0.7);

        const midiDecisionResult = resolveRuleProfileDecision({
          random,
          plan,
          playbackState,
          kind: "midi",
          pattern,
          section,
          lifecycleStates,
          localBarIndex,

LINE 4738: startSeconds: t,
----- CONTEXT -----
          pattern,
          section,
          lifecycleStates,
          localBarIndex,
          startSeconds: t,
          baseChance: midiBaseChance,
          profile: midiProfile
        });

        if (midiDecisionResult.allowed) {
          applyCutoffRulesForAllowedDecision(playbackState, midiDecisionResult.context, midiProfile);
          const scheduledCount = scheduleMidiPattern({
            offlineContext,

LINE 4743: if (midiDecisionResult.allowed) {
----- CONTEXT -----
          baseChance: midiBaseChance,
          profile: midiProfile
        });

        if (midiDecisionResult.allowed) {
          applyCutoffRulesForAllowedDecision(playbackState, midiDecisionResult.context, midiProfile);
          const scheduledCount = scheduleMidiPattern({
            offlineContext,
            destination,
            pattern,
            buffers,
            barStart: t,
            beatSeconds: section.barSeconds / 4,

LINE 4744: applyCutoffRulesForAllowedDecision(playbackState, midiDecisionResult.context, midiProfile);
----- CONTEXT -----
          profile: midiProfile
        });

        if (midiDecisionResult.allowed) {
          applyCutoffRulesForAllowedDecision(playbackState, midiDecisionResult.context, midiProfile);
          const scheduledCount = scheduleMidiPattern({
            offlineContext,
            destination,
            pattern,
            buffers,
            barStart: t,
            beatSeconds: section.barSeconds / 4,
            gainValue: 0.62,

LINE 4789: let t = section.startSeconds, localBarIndex = 0;
----- CONTEXT -----
      const midiBaseChance = getActivationChance(midiProfile, 0.7);
      let totalScheduledCount = 0;

      for (
        let t = section.startSeconds, localBarIndex = 0;
        t < section.startSeconds + section.duration - 0.001;
        t += repeatEverySeconds, localBarIndex += repeatEveryBars
      ) {
        const midiDecisionResult = resolveRuleProfileDecision({
          random,
          plan,
          playbackState,
          kind: "midi",

LINE 4790: t < section.startSeconds + section.duration - 0.001;
----- CONTEXT -----
      let totalScheduledCount = 0;

      for (
        let t = section.startSeconds, localBarIndex = 0;
        t < section.startSeconds + section.duration - 0.001;
        t += repeatEverySeconds, localBarIndex += repeatEveryBars
      ) {
        const midiDecisionResult = resolveRuleProfileDecision({
          random,
          plan,
          playbackState,
          kind: "midi",
          pattern: driverPattern,

LINE 4793: const midiDecisionResult = resolveRuleProfileDecision({
----- CONTEXT -----
        let t = section.startSeconds, localBarIndex = 0;
        t < section.startSeconds + section.duration - 0.001;
        t += repeatEverySeconds, localBarIndex += repeatEveryBars
      ) {
        const midiDecisionResult = resolveRuleProfileDecision({
          random,
          plan,
          playbackState,
          kind: "midi",
          pattern: driverPattern,
          section,
          lifecycleStates,
          localBarIndex,

LINE 4806: if (!midiDecisionResult.allowed) continue;
----- CONTEXT -----
          baseChance: midiBaseChance,
          profile: midiProfile
        });

        if (!midiDecisionResult.allowed) continue;

        applyCutoffRulesForAllowedDecision(playbackState, midiDecisionResult.context, midiProfile);

        for (const pattern of patternPair) {
          const scheduledCount = scheduleMidiPattern({
            offlineContext,
            destination,
            pattern,

LINE 4808: applyCutoffRulesForAllowedDecision(playbackState, midiDecisionResult.context, midiProfile);
----- CONTEXT -----
        });

        if (!midiDecisionResult.allowed) continue;

        applyCutoffRulesForAllowedDecision(playbackState, midiDecisionResult.context, midiProfile);

        for (const pattern of patternPair) {
          const scheduledCount = scheduleMidiPattern({
            offlineContext,
            destination,
            pattern,
            buffers,
            barStart: t,

LINE 4835: expirePlaybackItemsAtTime(playbackState, section.startSeconds);
----- CONTEXT -----

      return totalScheduledCount;
    }
    for (const section of sections) {
      expirePlaybackItemsAtTime(playbackState, section.startSeconds);
      const sectionMidiKeys = Array.isArray(section.selectedMidi) ? section.selectedMidi : plan.selectedMidi;
      const sectionMidi = sectionMidiKeys
        .map(file => {
          const pattern = midiPatterns.patterns.find(item => item.file === file);
          const noteLimit = section.midiNoteLimits?.[file];

          if (!pattern || !Number.isFinite(noteLimit)) {
            return pattern;

LINE 4863: scheduleMidiPatternInSectionWithRules(pattern, section);
----- CONTEXT -----
        .filter(pattern => !isHookJazzHatsPattern(pattern));


      for (const pattern of sectionMidiBeforeAudio) {
        scheduleMidiPatternInSectionWithRules(pattern, section);
      }

      if (hookJazzHatsPair.length) {
        scheduleHookJazzHatsPairInSectionWithRules(hookJazzHatsPair, section);
      }

      if (section.lyrixSectionId) {
        scheduleExplicitLyrixSection({

LINE 4931: const scheduledCount = scheduleAudioStemInSection({
----- CONTEXT -----
          if (scheduledLyrixGroupIds.has(groupId)) continue;
          scheduledLyrixGroupIds.add(groupId);
        }

        const scheduledCount = scheduleAudioStemInSection({
          offlineContext,
          destination,
          key,
          buffer: buffers.get(key),
          random,
          plan,
          lifecycleStates,
          playbackState,

LINE 4955: scheduleMidiPatternInSectionWithRules(jazzRidePatternForSection, section);
----- CONTEXT -----
      if (sectionHasJazzRideWithHatsVariant) {
        const jazzRidePatternForSection = chooseJazzRideWithHatsVariantForSection(section, sectionMidi);

        if (jazzRidePatternForSection) {
          scheduleMidiPatternInSectionWithRules(jazzRidePatternForSection, section);
        }
      }

    }

    expirePlaybackItemsAtTime(playbackState, duration);

    plan.playbackRuleDebug = {

LINE 5018: schedulePlan({
----- CONTEXT -----
currentRenderBuffers = buffers;

    setStatus(`RENDERING ${format.toUpperCase()} / AUDIO ${plan.selectedAudio.length} / MIDI ${plan.selectedMidi.length}`);

    schedulePlan({
      offlineContext,
      destination: masterGain,
      buffers,
      plan,
      random,
      duration
    });


----- MAIN FUNCTION ANCHORS -----
LINE 41: function getLyrixSectionLengthBars(section) {
LINE 44: function getFirstPassLyrixSections() {
LINE 49: function chooseFirstPassLyrixSection(random, lyrixSectionUsage = new Map()) {
LINE 66: function getBridgeLyrixGlobalRules() {
LINE 70: function getBridgeLyrixSections() {
LINE 83: function getBridgeLyrixDropoutChance(section) {
LINE 87: function getBridgeLyrixActivationChance(section) {
LINE 92: function canBridgePlayBeforeLyrixSection(section) {
LINE 107: function chooseBridgeLeadInBars(random) {
LINE 111: function getLyrixSectionParityRequirement(section) {
LINE 120: function doesBarOffsetPreserveLyrixParity(section, offsetBars) {
LINE 128: function chooseSafeBridgeLeadInBars(random, targetLyrixSection) {
LINE 138: function selectIncludedBridgeLyrixSections(random) {
LINE 150: function chooseBridgeLyrixSection(random, includedBridgeLyrixSections, lyrixSectionUsage = new Map()) {
LINE 172: function getLyrixSectionAudioFiles(section) {
LINE 197: function setStatus(message) {
LINE 202: function makeSeed() {
LINE 211: function mulberry32(seed) {
LINE 222: function chance(random, probability) {
LINE 226: function chooseOne(random, items) {
LINE 231: function getOpportunityIntervalFromKey(key) {
LINE 236: function getTrackBarNumber(section, localBarIndex = 0) {
LINE 241: function getValidOpportunityIndexForKeyAtBar(key, trackBarNumber) {
LINE 256: function isBarOpportunityAllowedForKey(key, section, localBarIndex = 0) {
LINE 265: function getAllowedLocalBarIndexesForKey(key, section) {
LINE 278: function chooseAllowedLocalBarIndexForKey(random, key, section) {
LINE 283: function keyHasFilenameToken(key, token) {
LINE 289: function isHookSection(section) {
LINE 296: function isHookKey(key) {
LINE 299: function createLifecycleState(id) {
LINE 313: function getLifecycleState(lifecycleStates, id) {
LINE 321: function includeLifecycleItem(lifecycleStates, id) {
LINE 327: function introduceLifecycleItem(lifecycleStates, id) {
LINE 334: function setLifecycleAvailability(lifecycleStates, id, available) {
LINE 340: function activateLifecycleItem(lifecycleStates, id, opportunityId = null) {
LINE 348: function dropoutLifecycleItem(lifecycleStates, id) {
LINE 355: function withdrawLifecycleItem(lifecycleStates, id) {
LINE 362: function getAudioLifecycleId(key) {
LINE 366: function getMidiLifecycleId(key) {
LINE 370: function createLifecycleMapFromPlan(plan) {
LINE 382: function writeLifecycleMapToPlan(plan, lifecycleStates) {
LINE 387: function clampProbability(value, fallback = 0) {
LINE 397: function calculateLifecycleDropoutChance(state, options = {}) {
LINE 406: function shouldDropoutLifecycleItem(random, lifecycleStates, id, options = {}) {
LINE 420: function isLifecycleItemEligible(state) {
LINE 430: function isLifecycleIdEligible(lifecycleStates, id) {
LINE 434: function setLifecycleEligible(lifecycleStates, id) {
LINE 441: function normalizeRuleDecisionToken(value) {
LINE 449: function addRuleDecisionTags(targetSet, values, prefix = "") {
LINE 463: function getEntryRuleDecisionTags(entry) {
LINE 479: function getSectionRuleDecisionTags(section) {
LINE 491: function getRuleDecisionItemKey({ key = "", entry = null, pattern = null } = {}) {
LINE 495: function createRuleDecisionContext({
LINE 536: function createRuleDecisionResult(context, options = {}) {
LINE 559: function addRuleDecisionReason(decision, code, details = {}) {
LINE 571: function blockRuleDecision(decision, code, details = {}) {
LINE 581: function multiplyRuleDecisionChance(decision, multiplier, code, details = {}) {
LINE 597: function markRuleDecisionDropout(decision, code, details = {}) {
LINE 606: function finalizeRuleDecision(random, decision) {
LINE 630: function recordRuleDecisionDebug(plan, decision) {
LINE 645: function createPlaybackRuleState() {
LINE 656: function getPlaybackItemId(kind, key) {
LINE 660: function registerActivePlaybackItem(playbackState, {
LINE 701: function unregisterActivePlaybackItem(playbackState, idOrItem) {
LINE 714: function expirePlaybackItemsAtTime(playbackState, timeSeconds) {
LINE 730: function getEntryPrimaryFamily(entry) {
LINE 742: function getEntryPlaybackTags(entry) {
LINE 750: function registerScheduledPlaybackHandle(playbackState, {
LINE 772: function getActivePlaybackItems(playbackState, filter = {}) {
LINE 789: function doesPlaybackItemOverlapTime(item, timeSeconds) {
LINE 801: function getActivePlaybackItemsAtTime(playbackState, timeSeconds, filter = {}) {
LINE 806: function getFuturePlaybackItemsAfterTime(playbackState, timeSeconds, filter = {}) {
LINE 812: function cutOffPlaybackItem(playbackState, item, cutTimeSeconds, options = {}) {
LINE 849: function cutOffPlaybackItemsAtTime(playbackState, filter = {}, cutTimeSeconds = 0, options = {}) {
LINE 862: function cutOffFuturePlaybackItemsAfterTime(playbackState, filter = {}, cutTimeSeconds = 0, options = {}) {
LINE 875: function applyGenericCutoffAction(playbackState, {
LINE 926: function cutOffMatchingPlaybackItems(playbackState, filter = {}, cutTimeSeconds = 0, options = {}) {
LINE 939: function addActivationBlockWindow(playbackState, {
LINE 970: function getMatchingActivationBlockWindow(playbackState, context) {
LINE 991: function applyActivationBlockWindows(playbackState, context, decision) {
LINE 1003: function isFamilyLockedByOtherItem(playbackState, family, itemId) {
LINE 1012: function applyFamilyLock(playbackState, context, decision, family) {
LINE 1024: function getRuleProfilePool() {
LINE 1035: function getRuleMapByName(pool, names) {
LINE 1048: function getRuleObjectFromMap(map, key) {
LINE 1072: function mergeRuleProfiles(...profiles) {
LINE 1100: function getRuleProfileForEntry(entry) {
LINE 1146: function getMidiPatternSampleTag(pattern) {
LINE 1153: function getMidiPatternRuleEntry(pattern) {
LINE 1189: function getRuleProfileForMidiPattern(pattern) {
LINE 1193: function getRuleChance(profile, names, fallback = 0) {
LINE 1207: function getGlobalInclusionChance(profile, fallback = 0) {
LINE 1216: function getActivationChance(profile, fallback = 0) {
LINE 1225: function getDropoutChance(profile, fallback = 0) {
LINE 1232: function getRuleValue(profile, names, fallback = null) {
LINE 1246: function getRuleArray(profile, names) {
LINE 1254: function getRuleObject(profile, names, fallback = {}) {
LINE 1264: function getDropoutRuleConfig(profile) {
LINE 1288: function getCutoffRules(profile) {
LINE 1297: function getHardClashRules(profile) {
LINE 1306: function getSoftMultiplierRules(profile) {
LINE 1316: function getGroupedActivationRules(profile) {
LINE 1326: function getTimedBlockRules(profile) {
LINE 1336: function getFamilyLockRules(profile) {
LINE 1345: function getDependencyRules(profile) {
LINE 1354: function getDensityRules(profile) {
LINE 1362: function getTensionRules(profile) {
LINE 1370: function getCrescendoRules(profile) {
LINE 1377: function normalizeRuleTargetFilter(target = {}) {
LINE 1410: function getRuleTargets(rule) {
LINE 1431: function doesRuleTargetMatchEntry(target, entry) {
LINE 1446: function doesRuleTargetMatchContext(target, context) {
LINE 1465: function doesRuleTargetMatchPlaybackItem(target, item) {
LINE 1478: function getPlaybackFilterFromRuleTarget(target) {
LINE 1489: function findActivePlaybackItemsForRuleTargets(playbackState, targets, timeSeconds = null) {
LINE 1508: function applyHardClashRulesToDecision(playbackState, context, decision, rules = []) {
LINE 1537: function applySoftMultiplierRulesToDecision(playbackState, context, decision, rules = []) {
LINE 1569: function getGroupedActivationDecisionId(rule, context) {
LINE 1604: function applyGroupedActivationRulesToDecision(playbackState, context, decision, rules = [], random = null) {
LINE 1680: function applyDependencyRulesToDecision(playbackState, context, decision, rules = []) {
LINE 1704: function applyTimedBlockRulesToDecision(playbackState, context, decision, rules = [], random = null) {
LINE 1745: function applyFamilyLockRulesToDecision(playbackState, context, decision, rules = []) {
LINE 1767: function applyCutoffRulesForAllowedDecision(playbackState, context, profile = {}) {
LINE 1803: function applyRuleProfileToDecision(playbackState, context, decision, profile = {}, random = null) {
LINE 1836: function resolveRuleProfileDecision({
LINE 1899: function createGlobalInclusionState() {
LINE 1906: function getGlobalInclusionId(kind, key) {
LINE 1910: function hasGlobalInclusionDecision(globalInclusionState, kind, key) {
LINE 1915: function getGlobalInclusionDecision(globalInclusionState, kind, key) {
LINE 1920: function setGlobalInclusionDecision(globalInclusionState, decision) {
LINE 1935: function resolveGlobalInclusionDecision({
LINE 1977: function writeGlobalInclusionDebugToPlan(plan, globalInclusionState) {
LINE 1984: function createRequiredActivationState() {
LINE 1991: function getRequiredActivationId(kind, key) {
LINE 1995: function addRequiredActivationObligation(requiredActivationState, {
LINE 2038: function getMatchingRequiredActivationObligations(requiredActivationState, context) {
LINE 2060: function markRequiredActivationSatisfied(requiredActivationState, context) {
LINE 2083: function getUnfulfilledRequiredActivations(requiredActivationState) {
LINE 2090: function writeRequiredActivationDebugToPlan(plan, requiredActivationState) {
LINE 2101: function getRequiredActivationRules(profile) {
LINE 2112: function normalizeRequiredActivationRule(rule, {
LINE 2140: function addRequiredActivationRulesFromProfile(requiredActivationState, {
LINE 2175: function applyLifecycleDropoutToDecision(random, lifecycleStates, context, decision, profile = {}) {
LINE 2214: function getSectionEnergyContext(section = {}) {
LINE 2229: function getDensityBandFromScore(score) {
LINE 2237: function attachSectionEnergyContext(section, energy = {}) {
LINE 2253: function getEnergyRuleMultiplier(rule, energyContext) {
LINE 2271: function applyEnergyRulesToDecision(context, decision, rules = [], reasonCode = "energy_multiplier") {
LINE 2290: function applyDensityRulesToDecision(context, decision, profile = {}) {
LINE 2299: function applyTensionRulesToDecision(context, decision, profile = {}) {
LINE 2308: function applyCrescendoRulesToDecision(context, decision, profile = {}) {
LINE 2316: function includeAudioByGlobalDecision({
LINE 2372: function includeMidiByGlobalDecision({
LINE 2423: function forceIncludeAudioSelection({
LINE 2446: function shuffle(random, items) {
LINE 2455: async function loadJson(path) {
LINE 2461: function toAssetUrl(path) {
LINE 2470: function applyRandomColourScheme() {
LINE 2484: function getCatalogEntry(key) {
LINE 2488: function entryHas(entry, text) {
LINE 2492: function isAudio(entry) {
LINE 2496: function isMidi(entry) {
LINE 2500: function isMidiSample(entry) {
LINE 2504: function isLikelyOneShot(entry) {
LINE 2519: function isLyrix(entry) {
LINE 2523: function isTrueBass(entry) {
LINE 2534: function isDrumMidiPattern(pattern) {
LINE 2546: function getMidiPatternRuleFamily(pattern) {
LINE 2551: function getMidiPatternRuleTags(pattern) {
LINE 2556: function getMidiPatternBaseId(pattern) {
LINE 2591: function isJazzRideWithHatsVariantPattern(pattern) {
LINE 2596: function isHookJazzHatsPattern(pattern) {
LINE 2601: function getHookJazzHatsPairFromSectionMidi(sectionMidi) {
LINE 2607: function sectionHasScheduledJazzCrash(section) {
LINE 2623: function chooseJazzRideWithHatsVariantForSection(section, sectionMidi) {
LINE 2630: function isJazzMidiHatPattern(pattern) {
LINE 2646: function isNormalMidiHatPattern(pattern) {
LINE 2653: function getNormalMidiHatCompanionGroupId(pattern) {
LINE 2658: function getNormalHatPatternDropoutChance(choiceGroupId) {
LINE 2670: function getNormalHatChoiceWeight(choiceGroupId, section) {
LINE 2698: function isRimMidiPattern(pattern) {
LINE 2708: function isExtraRimMidiPattern(pattern) {
LINE 2715: function isRimsDrumsRimMidiPattern(pattern) {
LINE 2722: function midiFileTriggersRimsDrumsRimFirstNoteMute(file) {
LINE 2736: function shouldSkipMidiPatternNote(pattern, noteIndex, section) {
LINE 2752: function getMidiPatternRuleTagList(pattern) {
LINE 2769: function isBeepipes1MidiPattern(pattern) {
LINE 2781: function getBeepipes1DensityChanceMultiplier(section = {}) {
LINE 2794: function isBeepipesMidiPattern(pattern) {
LINE 2806: function isSnarePatternMidiPattern(pattern) {
LINE 2813: function midiPatternClashesWithSelectedSection(pattern, selectedMidiFiles, midiPatternPool) {
LINE 2880: function isBeepipesGhostsMidiPattern(pattern) {
LINE 2892: function isBeepipes2MidiPattern(pattern) {
LINE 2903: function normalHatChoiceClashesWithMidiPattern(choiceGroupId, pattern) {
LINE 2936: function shouldExcludeNormalMidiHatFromGenericPicker(pattern, choiceGroupId = null) {
LINE 2962: function getNormalMidiHatChoiceGroupId(pattern) {
LINE 2982: function getBaseActivationChance(entry) {
LINE 3014: function expandSelectedWetDryPairs(selectedAudio, random, globalInclusionState, requiredActivationState) {
LINE 3042: function recordSectionPlannedAudio(section, entry) {
LINE 3923: async function fetchAndDecode(offlineContext, path) {
LINE 3935: function scheduleBuffer(offlineContext, destination, buffer, startTime, gainValue = 1, offset = 0) {
LINE 3967: function recordSectionScheduledAudio(section, {
LINE 3997: function scheduleAudioBufferWithPlaybackState({
LINE 4037: function getMidiRepeatEveryBars(pattern) {
LINE 4052: function scheduleMidiPattern({
LINE 4163: function midiMatchesSection(pattern, section) {
LINE 4206: function sectionGainForAudio(entry, section) {
LINE 4221: function getLyrixGroupKeys(entry) {
LINE 4247: function scheduleLyrixPathWithPlaybackState({
LINE 4289: function scheduleExplicitLyrixSection({ offlineContext, destination, section, random, playbackState = null, buffers }) {
LINE 4476: function scheduleLyrixGroupInSection({ offlineContext, destination, key, random, playbackState = null, section, buffers }) {
LINE 4514: function scheduleAudioStemInSection({ offlineContext, destination, key, buffer, random, plan = null, lifecycleStates = null, playbackState = null, section }) {
LINE 4705: function schedulePlan({ offlineContext, destination, buffers, plan, random, duration }) {
LINE 4976: async function renderTrack(format) {
LINE 5044: function audioBufferToWavBlob(buffer) {
LINE 5087: function writeString(view, offset, string) {
LINE 5093: async function ensureLameJs() {
LINE 5105: function audioBufferToMp3Blob(buffer) {
LINE 5134: function floatTo16Bit(floatArray) {
LINE 5145: function downloadBlob(blob, filename) {
LINE 5158: async function playButtonSound() {
LINE 5185: async function init() {
